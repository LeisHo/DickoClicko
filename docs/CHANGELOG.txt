DICKO CLICKO -- CHANGELOG
================================================================================

PROJECT START: 2026-09-01  -  10:33 PM EDT

--------------------------------------------------------------------------------
2026-09-01  -  10:33 PM - 10:49 PM EDT

Initial build, from a spec given as a *DEV*/*COL* shorthand list (documented
in CLAUDE.md). Single-file HTML/canvas app: a circle anchors a rope
simulated with verlet-integration point-chain physics (gravity, distance
constraints, per-segment thickness/curved end cap). All position/size dev
controls use %/vmin of the viewport rather than px, per explicit
requirement, so the layout stays proportional on both desktop and mobile.

Added:
- Circle rendering (%-based size/position, color picker).
- Rope rendering hanging from the circle center, curved (rounded-cap +
  filled end-arc) rather than a flat line.
- Verlet rope physics: gravity strength, rope weight, and animation-speed
  dev controls; the anchor point is re-pinned to the circle's position
  every frame.
- Click/punch interaction: clicking on or near the rope applies a
  directional impulse (away from the click point, tapered across
  neighboring points) so the rope deforms and swings back to equilibrium
  pendulum-style, rather than moving as a rigid stick.
- Click-and-hold to continuously grow the rope's length at a dev-controlled
  rate.
- Double-click to cut the rope at that point; the lower piece becomes an
  independent falling physics body inheriting velocity at the moment of the
  cut. Double-click is hand-rolled (setTimeout-based, keyed off the
  Double Click Threshold dev control) rather than the native dblclick
  event, specifically so a double click never fires a single-click punch
  first, per explicit requirement.
- Floor: toggle, color, thickness, and height dev controls; cut pieces
  collide with the floor and pile on each other via pairwise point
  repulsion across all fallen pieces.
- Full dev panel: collapsible *COL* groups, every *DEV* from the spec
  wired to a live effect, color pickers for color-only controls, and every
  slider's numeric readout independently editable to any value beyond the
  slider's own min/max (the slider handle visually clamps; the real value
  is used by the sim).
- Dev-mode gating: shows the panel on file://, localhost/127.0.0.1, or
  ?dev=1; hidden otherwise.
- Project docs (README, PROJECT_SUMMARY, CODE_SUMMARY, PROJECT_PROGRESS)
  and a project CLAUDE.md documenting the *DEV*/*COL* prompt shorthand and
  the future-default-value +20%-max-expansion convention.

Fixed:
- A cut rope piece falling from a perfectly vertical, undisturbed hanging
  rope has no asymmetry to topple over on -- it was standing on the floor
  as a rigid straight column instead of settling into a heap. Found via
  direct physics-state inspection (stepping the simulation and reading
  point positions), not visually. Fixed by giving each cut piece a small
  random initial tilt (rotated around its own cut-point end) at the moment
  of cutting, so gravity and the floor collision naturally fold it into a
  heap.
- The Rope Length slider had no effect once the rope's segment length was
  intentionally decoupled from cfg.ropeLength (needed so hold-to-grow and
  cutting could change the rope's effective length without a later
  slider-default read snapping it back to the full default) -- added an
  onChange hook so manually dragging the slider still rescales the rope's
  current length correctly, including on an already-cut remainder.

Verified:
- All physics behaviors (anchor pin/stability, punch in both directions
  described in the spec, hold-to-grow rate matching the dev control
  exactly, double-click-cut point count and no spurious single-click punch
  firing first, floor collision, multi-piece pile repulsion pushing
  overlapping points apart) via direct calls into the running page's
  physics functions through javascript_tool, stepping frames manually --
  this session's headless Browser-pane tab doesn't composite frames, which
  throttles/pauses requestAnimationFrame entirely, so the live animation
  loop itself could not be watched directly in this environment. A
  temporary debug hook (window.__debugTMP) used for this testing was
  removed before commit (grep-confirmed clean).
- %-based responsive layout confirmed correct via read-page/JS inspection
  at both desktop (1280x720) and mobile (375x812 emulated) viewport sizes
  -- circle/rope/floor positions and vmin-based circle radius all scaled
  proportionally, dev panel remained readable and fit the smaller
  viewport.
- node --check clean on the extracted inline script, both before and after
  removing the debug hook.
- Not verified: real touch input on an actual mobile device (viewport-size
  emulation only), and the ?dev=1/production-hostname branch of the
  dev-mode gate (only the localhost branch was exercised live).

--------------------------------------------------------------------------------
2026-09-01  -  11:15 PM - 11:34 PM EDT

Brought the dev panel in line with a new workspace-wide standard (parent
CLAUDE.md Section 12, added after the initial build, generalizing this
project's own original dev-panel work) and added a new directional cut
animation, per explicit request.

Added:
- Dev panel: resizable from all 4 edges and all 4 corners (was one corner
  handle); a separate collapse-whole-panel button (top-right) distinct from
  D-key hide; titled plainly "DEV"; Copy/Save/Reset buttons (+R key),
  persisting to localStorage; drag-to-reorder for both collapsible groups
  and individual settings within a group, with that order captured by
  Copy/Save/Reset.
- Desktop/Mobile tabs on the dev panel (per an update to CLAUDE.md Section
  12f partway through this session): the panel's own size/position is
  independent per tab and switches immediately on tab click, regardless of
  the browser's actual current width -- lets the mobile panel layout be
  previewed/tuned without physically resizing the window. Every functional
  setting in this project is judged shared between the tabs (not
  device-split), since §12f's rationale for splitting is mainly fixed-px
  values, and every control here is already %/vmin-based.
- Every slider's typed-input auto-expand (previously max-only, and only for
  future defaults) now expands whichever bound (min or max) is crossed, by
  +/-20% of the typed value, on direct user typing in either direction.
- Cut Speed dev control (ROPE CUT group) and a directional cut-sweep
  animation: double-clicking to cut now freezes the lower piece in place
  and plays a sweep mark across the rope's thickness, in the direction the
  click came from (right-of-rope -> sweeps right-to-left, left-of-rope ->
  left-to-right, determined via the signed perpendicular-to-tangent side of
  the click so it's correct even if the rope is swung off-vertical), before
  releasing the piece to fall -- rate set by Cut Speed.

Fixed:
- `setPointerCapture` can throw `NotFoundError` for a pointerId the browser
  doesn't consider "active" -- hit directly while testing the new
  drag-to-reorder feature (synthetic PointerEvents dispatched for testing
  all triggered it) and confirmed via MDN to also be a real possibility in
  production. An uncaught throw there was aborting the rest of the
  pointerdown handler before its move/up listeners were even attached,
  silently breaking the feature. Fixed by wrapping every capture call in a
  shared `tryCapture()` helper and moving every drag/resize/reorder
  handler's move/up listeners onto `window` (not the captured element), so
  the feature keeps working even when capture itself fails.

Verified:
- Every new dev-panel behavior (8-direction resize, D-key hide with an
  input-focus guard so it doesn't hijack typing, whole-panel collapse,
  group and setting drag-reorder, Copy/Save/Reset round-tripping both
  values+order and per-tab panel geometry, Desktop/Mobile tab switching
  changing the live panel geometry independent of actual viewport width,
  both-directions auto-expanding slider range) via direct DOM/localStorage
  inspection and dispatched PointerEvent/KeyboardEvent sequences through
  javascript_tool -- this session's headless Browser-pane tab doesn't
  composite frames (requestAnimationFrame throttled), so a temporary debug
  hook (window.__debugTMP) exposing internal state/functions was used again
  for the cut-animation physics specifically, then removed before commit
  (grep-confirmed clean).
- Cut-sweep direction confirmed both ways (right-click -> sweeps right to
  left, left-click -> left to right) and confirmed the frozen piece truly
  doesn't move (position identical across many stepped frames) until
  cutProgress reaches 1, then releases and falls normally; confirmed Cut
  Speed scales the release time proportionally (5x speed released in ~4
  frames vs ~20 frames at the default speed).
- node --check clean on the extracted inline script throughout, both before
  and after removing the debug hook.
- Not verified: real touch input on an actual mobile device, and the
  ?dev=1/production-hostname branch of the dev-mode gate (unchanged from
  the initial build's same limitation).

--------------------------------------------------------------------------------
2026-09-01  -  11:36 PM - 11:42 PM EDT

Added the built-in "Dev Panel" settings group, per an update to the parent
CLAUDE.md's §12 standard (Section 12i, added mid-session after the previous
entry's retrofit was already pushed).

Added:
- A "Dev Panel" collapsible group, always first, in both Desktop and Mobile
  tabs: 9 controls for the panel's own chrome -- Dev Panel Title / Tab /
  Group Title / Settings Title / Body font sizes, Dev Panel Opacity, and
  Background / Title Text / Non-Title Text colors. Applied live via CSS
  custom properties (--dp-title-size, --dp-bg, etc.) set on #devPanel.
  Judged device-specific (independent per Desktop/Mobile tab), for the same
  "positioned within two different-shaped viewports" reasoning §12f already
  applies to the panel's own geometry -- so panelStyle{} persists inside the
  same per-tab geometry blob (getPanelGeometry()'s new .style field) rather
  than in cfg.

Fixed:
- Switching to a Desktop/Mobile tab with nothing saved yet was silently
  keeping the *other* tab's live panel geometry/style rather than resetting
  to defaults, because applyPanelGeometry(null) used to just return early.
  Found immediately while verifying the new Dev Panel group (set a red,
  20px title on the Desktop tab, saved, switched to Mobile, and it was
  still showing red/20px instead of the default). Fixed by making
  applyPanelGeometry(null) a real "reset to defaults" call instead of a
  no-op, for both position/size and panelStyle -- this also makes the
  Reset button/R-key correctly clear an unsaved manual panel move/resize
  now, which it should always have done.

Verified:
- Live style application (font-size and color changes take effect
  immediately) and the full Copy/Save/Reset round-trip for the new group's
  values, confirmed independently per Desktop/Mobile tab (including the
  no-saved-state-yet case, post-fix) -- via direct DOM/computed-style/
  localStorage inspection through javascript_tool, same as the rest of this
  session's dev-panel testing.
- Background-color + opacity combine correctly into the panel's actual
  rendered rgba() background.
- The new group participates correctly in drag-to-reorder and appears first
  by default, alongside the existing project-specific groups.
- node --check clean on the extracted inline script.

--------------------------------------------------------------------------------
2026-09-01  -  11:48 PM - 11:55 PM EDT

Fixed 3 real issues from the user's own hands-on testing of the previous
entries' work.

Fixed:
- Double-click-to-cut intermittently "just stopped working" after a few
  uses. Root cause: onPointerDown never cleared a prior holdTimer before
  starting a new one -- a pointerdown that never got a matching pointerup
  (any dropped/non-round-tripping event) orphaned its timer once the next
  pointerdown overwrote the holdTimer variable; the orphaned timer still
  fired later against whatever downInfo was current *then*, marking an
  unrelated in-progress click as a false hold and silently swallowing it
  (both punch and cut return early on info.isHold). Fixed by always
  clearing holdTimer at the top of onPointerDown.
- Cutting the rope while it was already mid-swing caused a visible freeze
  for a split moment before the swing continued -- reported directly by the
  user ("it should be getting cut while swinging, that shouldn't happen").
  Root cause: the cut-sweep animation added in the previous entry froze the
  cut piece's physics (skipped integration/floor-collision/pile-repulsion)
  for the sweep's duration. Fixed by making the sweep purely cosmetic --
  cutRopeAt() now applies the toppling tilt immediately (rotating both the
  current AND previous-frame position around the same pivot, so the tilt
  adds a topple bias without zeroing out whatever velocity the piece
  already had from the swing) and the piece integrates normally from the
  very next frame; the sweep mark (renderCutSweep()) now tracks the piece's
  own live points[0] position every frame instead of a static remembered
  point, since the piece is moving under it the whole time.
- The rope was rendering behind the circle; swapped render() to draw the
  circle first and the rope on top, per explicit request.

Verified:
- Render order: sampled the actual canvas pixel at the circle's center
  after one manual render -- came back exactly the rope's color (was
  exactly the circle's color before the fix), while a pixel at the circle's
  edge (off the rope's line) still came back the circle's color, confirming
  the rope draws on top only where they actually overlap.
- Mid-swing cut: punched the rope, let it build up real swing velocity
  (confirmed non-zero via the verlet position-delta), cut it mid-swing, and
  traced the cut piece's position for 10 consecutive frames immediately
  after -- smooth continuous movement every frame, no plateau, confirming
  the freeze is gone and the piece genuinely inherits its swing momentum.
- Stale-timer race: reproduced the exact scenario in an isolated
  self-timed async test (an orphaned pointerdown with no matching pointerup,
  a real quick click shortly after, then the orphaned timer's ~180ms window
  elapsing, then a real double-click) and confirmed the double-click still
  resolves correctly into a cut afterward. The pre-fix code was not
  separately re-tested to directly confirm it reproduces the corruption --
  this verifies the fix, not a before/after repro.
- node --check clean on the extracted inline script.
- Not verified: real touch input on an actual mobile device, and the
  ?dev=1/production-hostname branch of the dev-mode gate (unchanged
  limitation carried from earlier entries).

--------------------------------------------------------------------------------
2026-09-02  -  1:43 AM - 1:49 AM EDT

Fixed 2 more real issues from the user's own hands-on testing, added a
Minimum Rope Length control, and prepared 2 new sliders (Click Hold Max
Duration, Intensity Ceiling) for a charged-punch-on-release mechanic whose
exact interaction with the existing click-hold-to-grow behavior is being
clarified with the user before implementation.

Fixed:
- The rope's grown/extended portion behaved like a stiff stick instead of a
  flexible rope, and couldn't be cut at all. Root cause: mainRope's point
  count was fixed at 14 for the rope's entire life -- growing it just
  stretched those same 14 points further and further apart, so a long rope
  had very few "joints" relative to its length (looks/behaves stiff) and
  its visually-smoothed curve (drawn between increasingly sparse, distant
  points) increasingly diverged from the straight-line segments
  hitTestRope() actually tests clicks against. Fixed by replacing the
  direct segLen-only growth/resize path with setMainRopeTotalLength(),
  which adds (or removes) points to hold a roughly constant segment density
  (TARGET_SEG_LEN_VH, matching the rope's own default density) as the total
  length changes -- new points extend along the current tip's tangent
  direction rather than appearing at the same spot. Both growRope() and the
  Rope Length slider's onChange now go through this.
- Growing the rope until it hit the floor just piled through it with no
  reaction -- the main rope (still attached to the circle) never had floor
  collision or pile-repulsion at all, only cut-off pieces did. Fixed by
  including mainRope's own points in the same floor-clamp and
  pileRepulsion() pass every frame, alongside every fallen piece's points
  (pileRepulsion() now takes an explicit points array instead of building
  one from fallenPieces internally).

Added:
- Minimum Rope Length dev control (ROPE CUT group): cutRopeAt() now refuses
  a cut entirely (no change at all, rope stays whole) if it would leave the
  anchor-side remainder shorter than this value, per explicit request.
- Click Hold Max Duration and Intensity Ceiling dev controls, requested
  for a new mechanic: hold-and-release scales punch intensity by how long
  the click was held (up to the ceiling at the max duration). Not yet
  wired to any behavior -- clarifying with the user first how this
  interacts with the existing click-hold-to-grow gesture, since as
  literally described both are triggered by the same click-and-hold input.

Verified:
- Grew the rope by 2 simulated seconds (default rate) and confirmed point
  count increased from 14 to 23 while segLen stayed close to its original
  ~24.9px density (24.5px) -- density held roughly constant instead of the
  segments stretching.
- Cut a point well within the newly-grown extension (index 20 of 23,
  outside the original 14-point span) and confirmed it registered as a hit
  and cut correctly -- previously unreachable.
- Grew the rope far past the floor, ran it to rest, and confirmed every
  point clamped to floorY (max overshoot ~0.17px) with points spreading
  across a ~250px range of x-positions at the floor rather than stacking in
  a rigid vertical line -- confirms real piling, not clipping through.
- Confirmed Minimum Rope Length actually refuses a too-short cut (identical
  fallen-piece count and main-rope point count before/after) and still
  allows a cut beyond the minimum on the same rope.
- node --check clean on the extracted inline script; debug hook removed
  before commit (grep-confirmed).

--------------------------------------------------------------------------------
2026-09-02  -  1:55 AM - 2:00 AM EDT

Wired up the Click Hold Max Duration / Intensity Ceiling mechanic from the
previous entry, per the user's clarification: hold-to-grow now only
triggers when the hold starts on the circle; a hold starting on the rope
instead charges punch intensity, fired on release.

Added:
- Gesture dispatch now branches at pointerdown on WHERE the hold starts
  (isOnCircle()), not just whether it's on the rope. A hold on the circle
  grows the rope exactly as before. A hold on the rope charges instead:
  no punch fires until release, intensity scaled linearly from 0 up to
  Intensity Ceiling as total hold time goes from 0 to Click Hold Max
  Duration (clamped at the ceiling beyond that) -- fires immediately on
  release, skipping the double-click wait entirely, since a genuine hold
  was never a double-click attempt. A quick tap on the rope is completely
  unaffected -- still goes through the existing punch/double-click-to-cut
  disambiguation.

Verified:
- Holding on the circle grows the rope (ropeLength increased by exactly the
  expected amount over a measured duration) and does not charge a punch.
- Holding on the rope does NOT grow it (growing stayed false throughout).
- A hold released well past Click Hold Max Duration produced an impulse
  matching vh(1.0)*intensityCeiling to the exact decimal (-28.8 for
  ceiling=4 at 720px height) -- confirms the ceiling clamp is exact, not
  approximate.
- A partial hold (400ms of an 800ms max) produced a real, smaller,
  correctly-directional impulse, consistent with the same linear formula
  already confirmed exact at the ceiling.
- Regression: quick double-click on the rope still cuts correctly,
  unaffected by the gesture-dispatch changes.
- node --check clean; debug hook removed before commit (grep-confirmed).

--------------------------------------------------------------------------------
2026-09-02  -  2:11 AM - 2:17 AM EDT

Fixed the real root cause of a "cutting/holding bounces the rope like a
punch" report, made holding-to-charge work regardless of distance from the
rope, fixed a general "everything's slightly jumpy" report, and baked in a
user-provided settings dump as the new defaults.

Fixed:
- Charging's own eligibility timer used HOLD_THRESHOLD_MS (180ms) -- a real
  double-click's second press can easily be held longer than that, which
  hijacked the release into a charged punch instead of completing the cut
  (or, for hold-to-grow, if the press landed just off the circle). Changed
  the eligibility gate to cfg.doubleClickThreshold instead. This is
  provably safe against ever overlapping a valid double-click, not just
  empirically tuned: if a release's own press duration already exceeds
  doubleClickThreshold, the gap from the FIRST click's release (necessarily
  earlier) must exceed doubleClickThreshold too, so it could never have
  passed the double-click gap check either way. Reproduced the original bug
  directly (a second click held 200ms -- past the old 180ms threshold, well
  within the double-click gap -- used to fire a punch instead of cutting)
  and confirmed the fix resolves it.
- All animations (rope swing, growth rate, cut-sweep speed) were slightly
  jumpy because update() was stepped by the raw, jittery per-frame
  requestAnimationFrame delta. Switched to a fixed-timestep accumulator:
  update() now always advances by exactly 1/60s, called as many times as
  needed to catch up to real elapsed time each frame; render() still runs
  once per real frame.

Added:
- Holding to charge a punch is no longer limited by Click Distance, per
  explicit request -- press down anywhere and the hold still charges,
  aimed at whichever rope point ends up nearest. Only a quick tap (single
  click or either half of a double-click) still needs real proximity to
  the rope, since that's what targets a specific punch/cut point.
- Baked in a full Copy-Settings dump (values, group/setting order) the user
  captured from a live-tuned session as the new hardcoded defaults:
  Circle Size 8->4.5, Rope Thickness 1.4->2.8, Rope Length 45->53.972788461538556
  (%vh; the exact captured value, not rounded), plus the Minimum Rope
  Length/Rope Fall Speed/Cut Speed order in ROPE CUT and the Click Hold Max
  Duration/Intensity Ceiling/Double Click Threshold order in MECHANICS
  reordered to match. Every provided value was already within its slider's
  existing min/max range, so no bound-expansion was needed.

Verified:
- Reproduced the exact original cut-bounce scenario (click 1 quick, click 2
  held 200ms, ~215ms total gap -- comfortably inside the 300ms double-click
  window) and confirmed it now cuts cleanly (fallen pieces 0->1, main rope
  point count reduced) with no separate punch firing.
- A hold started ~162px from the rope (7.5x the ~21.6px Click Distance
  threshold) still correctly charged and fired a punch on release; an
  otherwise-identical QUICK tap at the same far distance produced exactly
  zero movement, confirming quick taps still respect Click Distance while
  holds don't.
- Regression: hold-to-grow on the circle still grows the rope by exactly
  the expected amount and stops cleanly on release.
- Fresh (cleared localStorage) page load confirmed Circle Size, Rope
  Thickness, and Rope Length now read the new default values exactly.
- node --check clean; debug hook removed before commit (grep-confirmed).

--------------------------------------------------------------------------------
2026-09-02  -  2:22 AM - 2:27 AM EDT

Fixed a real, reproduced bug in vertical dev-panel resizing, per user
report ("vertical resizing of Dev panel is buggy").

Fixed:
- initResizeHandles()'s move() math for the n/s resize handles never
  clamped the requested height to match #devPanel's own CSS
  max-height:88vh -- reproduced directly before touching any code:
  dragging the bottom (s) handle past the cap went silently unresponsive
  (JS kept accumulating a far-larger style.height with no visible effect,
  e.g. requesting 710px while the actual rendered box stayed clamped at
  633.6px on a 720px-tall viewport); dragging the top (n) handle was worse
  -- since n also recomputes top independently, pushing it up 150px set
  top to -140px (off-screen above the viewport) while the uncompensated
  height stayed clamped at 633.6px, so the panel visibly shrank from the
  BOTTOM edge while its top flew off-screen, instead of the top edge
  moving up and the bottom staying put. Fixed by adding a matching
  maxH = window.innerHeight * 0.88 clamp for both directions, and by
  deriving n's height FROM its own (separately clamped) top around a
  fixed bottomEdge point, rather than computing top and height
  independently the way s always could safely afford to.

Verified:
- Reproduced the exact bug first (both directions) via dispatched
  PointerEvent drags before writing the fix, confirming the top-going-
  negative/bottom-shrinking behavior for n and the requested-vs-actual
  height mismatch for s.
- Post-fix: dragging n 150px up on an already-max-height panel moves top
  by only the ~10px of real headroom available and leaves the bottom edge
  fixed (643.59 -> 643.58, sub-pixel float noise only) instead of jumping
  off-screen.
- Post-fix: dragging s to request more height than the cap allows leaves
  panel.style.height exactly equal to the actual rendered height (both
  633.6px), no silent mismatch; shrinking back down afterward remains
  fully responsive.
- Regression: horizontal (e) resize unaffected -- still correctly bounded
  by the viewport's right edge exactly as before.
- node --check clean; no debug hook needed for this fix (tested via
  dispatched PointerEvents directly against the shipped handlers).

--------------------------------------------------------------------------------
2026-09-02  -  2:36 AM - 2:47 AM EDT

Fixed hold-to-grow missing the circle and firing a charge-punch instead,
fixed real (and self-introduced) growth jitter/stall bugs, added a Click
And Hold Distance control, and baked in 2 more rounds of user-provided
settings dumps as defaults.

Fixed:
- isOnCircle() used the circle's bare visual radius, which a real user
  found unreliable to hit precisely, especially at the current small
  Circle Size default (4.5%vmin) and with the rope rendering visually
  through/over the circle right where it emerges -- a hold "clearly meant
  for the circle" could land just outside it and get treated as a rope
  hold instead, firing an unwanted charge-punch on release. Fixed by
  adding a generous vmin(4) margin to the circle's effective hit-radius,
  used consistently for both hold-to-grow eligibility and the rope's own
  exclusion zone.
- The rope jittered slightly even when hanging still, specifically while
  growing. Root cause: setMainRopeTotalLength() re-derived segLen from
  newTotalPx/pointCount every call -- every time growth added a new point,
  pointCount jumped, so segLen (the rest-length EVERY existing segment is
  solved against) silently changed too, in the same instant, by an amount
  that didn't match the continuously-growing newTotalPx. The constraint
  solver then had one frame to correct that whole-chain mismatch, which
  read as a small bounce. Fixed by pinning segLen to exactly
  TARGET_SEG_LEN_VH permanently -- new points are already inserted exactly
  that far from their neighbor, so there's nothing left to correct.
- That fix introduced a second bug, caught by direct reproduction before
  calling it done: with segLen now constant, growRope()'s old baseline
  calculation (segLen * pointCount) stopped changing between point
  insertions, so sustained growth silently stalled for however long it
  took to cross the next whole-point threshold (confirmed directly:
  ropeLength climbed once, then sat completely frozen for 270+ simulated
  frames despite the hold still being active). Fixed by adding
  mainRope.totalLength, a persisted real-length value growRope() actually
  accumulates against, decoupled from the quantized segLen*pointCount
  product.
- cutRopeAt() wasn't updating mainRope.totalLength (or the Rope Length
  slider's displayed value) after truncating the rope -- found while
  verifying the above fix. Without this, growth after a cut would have
  resumed from the stale PRE-cut length, and nudging the Rope Length
  slider post-cut would have jumped the rope back out near its old length
  instead of adjusting the actual (shorter) current one.

Added:
- Click And Hold Distance dev control (CLICK group): gates whether
  releasing a charged hold actually fires a punch, separately from Click
  Distance (which still governs quick-tap targeting) and separately from
  the now-unlimited-by-Click-Distance charging itself -- a hold released
  too far from the rope now does nothing, rather than punching wherever
  the nearest rope point happened to be.
- Baked in 2 more rounds of user-provided Copy Settings dumps as the new
  hardcoded defaults (Rope Length, Click Distance, Click Intensity, floor
  color/thickness/height, and the built-in Dev Panel group's row order).
  Rope Length's new default (100.16051775147857 %vh) exceeded its prior
  max (90) -- raised to value*1.2 (120.19262130177428) per the project's
  standing over-max-default rule; every other provided value already fit
  its slider's existing range.
- Refactored the built-in "Dev Panel" appearance group from two separate
  arrays (sliders, then colors) into one unified, arbitrarily-orderable
  array -- needed to actually honor the requested new row order, which
  interleaves slider and color rows in a way the old two-array split could
  never represent.

Verified:
- A hold 5px past the circle's raw visual edge (outside the old radius,
  inside the new margin) now correctly registers as on-circle and grows
  the rope; released cleanly with growing stopping and no punch fired.
- Sustained growth (900 simulated frames, ~66 real point-insertion events,
  both with and without gravity) now shows zero lateral (x-axis) deviation
  on an initially-vertical rope throughout -- confirms the jitter fix
  holds even under real, continuous, incremental growth, not just the
  edge case that first exposed the stall.
- Rope Length climbs smoothly and continuously frame-to-frame post-fix
  (verified via periodic snapshots showing steady progression), not the
  climb-once-then-freeze pattern the stall bug produced.
- Cut-then-regrow: cut the rope, confirmed the displayed length dropped to
  match the shorter remainder immediately, then grew it again and
  confirmed growth resumed from that (correct, shorter) baseline rather
  than the stale pre-cut value.
- Click And Hold Distance: a hold released 2x beyond the threshold fired
  nothing (zero movement); a hold released within it fired a real,
  correctly-directional impulse.
- Fresh (cleared localStorage) load confirmed every new default value and
  the Dev Panel group's new row order exactly.
- node --check clean throughout; debug hook removed before commit
  (grep-confirmed).

--------------------------------------------------------------------------------
2026-09-02  -  03:30 AM EDT

Fixed:
- Dev panel could be dragged partially (or almost fully) off-screen: the
  drag clamp in initPanelDrag() used a hardcoded 40px margin
  (window.innerWidth/Height - 40) instead of the panel's own actual
  width/height, so dragging toward an edge let most of the panel -- including
  the resize corners, which sit right at its own edges -- go off-screen well
  before the drag actually stopped. Fixed by clamping left/top against the
  panel's real getBoundingClientRect() dimensions captured at drag start
  (maxLeft = innerWidth - panel width, maxTop = innerHeight - panel height,
  both floored at 0).

Verified:
- A drag from the header 2000px past the bottom-right corner (1280x720
  viewport, panel 320x633.59375px) landed exactly flush: left:960,
  top:86.40625, right:1280, bottom:720 -- fully in frame.
- A drag 3000px past the top-left corner landed exactly flush: left:0, top:0.
- node --check clean.

--------------------------------------------------------------------------------
2026-09-02  -  03:32 AM EDT

Added:
- Documented a standing convention (CLAUDE.md + a comment above DEV_GROUPS
  in index.html): every X/Y position slider pair shares one global origin
  -- (0,0) at the viewport's top-left corner, 100 = full width (X, %vw) /
  full height (Y, %vh), matching vw()/vh() exactly. circleX/circleY
  (currently the app's only X/Y pair) already conform, so no code/behavior
  changed; this pins the scheme so any future position slider follows it
  too, keeping raw values copy/paste-compatible between same-axis sliders.

--------------------------------------------------------------------------------
2026-09-02  -  04:19 AM EDT

Fixed:
- Setting Rope Length shorter caused a visible bounce/glitch live, and
  reloading after Save Settings made the rope glitch right at boot.
  Root-caused precisely before fixing: resetMainRope() (boot only) built
  the initial point chain with its own segLen formula
  (totalLen / (POINT_COUNT-1)) instead of the pinned TARGET_SEG_LEN_VH
  constant that setMainRopeTotalLength() uses everywhere else. The two
  only happened to agree back when Rope Length's default was still 45
  (the exact value TARGET_SEG_LEN_VH was computed from); an earlier
  "set defaults" round changed the default to 100.16051775147857 without
  updating TARGET_SEG_LEN_VH to match, so they silently diverged
  (segLen ~55.47 vs targetSeg ~24.92 at 720px viewport height). Any call
  to setMainRopeTotalLength() -- dragging the Rope Length slider at all,
  or resetSettings() reapplying a saved ropeLength value at boot --
  unconditionally overwrote segLen to targetSeg for every existing point
  in one frame, a whole-chain rest-length jump the constraint solver had
  to violently correct on the next iterations. Fixed by having
  resetMainRope() build from the same targetSeg constant (and the same
  point-count formula) setMainRopeTotalLength() already uses, so the two
  can never diverge again regardless of what Rope Length's default
  becomes in the future.

Verified:
- Fresh boot (localStorage cleared, no saved settings): mainRope.segLen
  measured exactly equal to vh(TARGET_SEG_LEN_VH) immediately after
  resetMainRope() -- confirmed 24.923076923076923 === 24.923076923076923.
- Simulated a live shrink via the real onChange path
  (setMainRopeTotalLength, as dragging the slider calls it): segLen
  stayed exactly at targetSeg, no jump introduced.
- Simulated the real boot sequence with a saved shorter Rope Length
  (20 %vh) persisted via saveSettings(): resetMainRope() and the
  subsequent applyValues()-triggered setMainRopeTotalLength() produced
  IDENTICAL segLen (24.923076923076923) and point count (7) -- reapplying
  is now a true no-op.
- Real page load (navigate, not simulated) with that same saved shorter
  value: mainRope booted directly at segLen=24.923076923076923,
  pointCount=7, totalLength=144 (=vh(20)), matching the saved value
  exactly; calling setMainRopeTotalLength() again with the identical
  total produced 0 positional displacement on every point.
- Confirmed resetMainRope() is called exactly once (boot only), so this
  fix cannot affect growth/cut/live-resize behavior at runtime.
- node --check clean; debug hook (window.__debugTMP) removed before
  commit (grep-confirmed 0 references).

--------------------------------------------------------------------------------
2026-09-02  -  04:34 AM EDT

Fixed:
- Rope extension animation "stepping" -- growth previously only added a
  whole new point once accumulated growth crossed a full segment length
  (~25px at current defaults), so the rope sat visibly still for several
  frames and then snapped a whole segment into place, instead of looking
  like continuous motion. Added mainRope.tipGrowLen, tracking how far the
  LAST segment currently reaches toward a full targetSeg (rising every
  frame during growth, reset to segLen -- fully committed -- after any
  direct/instant length change). integrateChain() gained an optional
  tipSegLen override for the last segment's rest length; the existing
  distance-constraint solver then naturally pulls the tip out to match the
  rising target each frame, the same mechanism already used for every
  other segment, so there's nothing new to fight or correct. When
  tipGrowLen reaches a full targetSeg, growRope() commits: the just-
  finished segment is fixed at exactly targetSeg (clamping any single-
  frame overshoot), a fresh zero-length tip point is pushed, and the
  overshoot carries forward into the new tip's own tipGrowLen so no
  accumulated growth is lost.
- Click-and-hold not firing when pressed away from the rope and moved
  toward it before releasing. The Click-And-Hold-Distance gate checked
  info.hit, which is captured once at onPointerDown and never updated --
  so it reflected the PRESS position, not the release position, meaning
  moving the pointer during the hold had no effect on whether it fired.
  Fixed by computing a fresh nearestPointOnRope() from the actual release
  coordinates (e.clientX/e.clientY) in onPointerUp and using that for both
  the distance gate and the punch's aim.

Added:
- Baked in another user-provided Copy Settings dump as new hardcoded
  defaults: Rope Length 100.16051775147857 -> 17.30769230769231, Click And
  Hold Distance 10 -> 40, Rope Animation Speed 1 -> 1.4, Minimum Rope
  Length 8 -> 1, Cut Speed 1 -> 1.6. Every value already fit its slider's
  existing min/max range (holdDistance's new default sits exactly at its
  existing max of 40, minRopeLength's exactly at its existing min of 1) --
  no range expansion was needed. Group/setting order in the dump matched
  the existing DEV_GROUPS/PANEL_STYLE_CONTROLS order exactly, so no
  reordering was needed either.

Verified:
- Isolated integrateChain() unit test (synthetic 3-point chain, no
  gravity): the tip segment's actual distance tracked a rising tipSegLen
  argument exactly at every value from 10 to 100 (of a 100 rest length).
- Full-rope growth test with gravity/swing on and the floor disabled (to
  isolate the mechanism from floor/pile physics): the tip segment's
  measured length tracked the exact per-frame growth amount (1.8px/frame
  at defaults) frame by frame in a clean sawtooth 0->targetSeg->0->...,
  zero frames where it held still while tipGrowLen was still rising, and
  point count incremented by exactly 1 at each sawtooth reset.
- Click-and-hold, via dispatched PointerEvents with real elapsed hold
  time: press far away + release near the rope now fires (previously did
  not); press far + release far still correctly does not fire; press near
  + release near (regression) still fires, matching prior behavior for
  the common case.
- Fresh (cleared localStorage) load confirmed all 5 new numeric defaults
  exactly.
- 0 console errors on a clean shipped-code reload.
- node --check clean; debug hook (window.__debugTMP) removed before
  commit (grep-confirmed 0 references).

--------------------------------------------------------------------------------
2026-09-02  -  05:22 AM EDT

Fixed:
- Rope extension going wild after several seconds of continuous
  hold-to-grow, reported with a user-supplied screen recording
  (datalog/Recording 2026-09-02 050757.mp4) -- the rope visibly whipped/
  tangled around the anchor briefly, then partially self-corrected;
  extensions after a cut acted "more normal" but still showed a faint
  bounce on the growing tip. Root cause: the immediately-prior smooth-
  extension fix (previous entry) fed the growing tip's continuously-rising
  target length into integrateChain()'s ordinary iterative distance-
  constraint solver as an override for the last segment's rest length.
  Over a sustained hold this acted as a persistent forcing function --
  CONSTRAINT_ITERATIONS is fixed at 6 and DAMPING=0.99 barely dissipates
  anything per frame, so residual correction error compounded over
  hundreds of frames. Reproduced directly: a 900-frame (15s) continuous-
  growth simulation showed segment lengths reaching 2.93x rest length and
  points folding back on themselves in Y (a genuinely tangled chain, not
  normal swinging).

  Fixed by never letting the growing tip enter the constraint solver at
  all. integrateChain() gained a skipLastSegment param that shortens its
  constraint loop by one, excluding the tip's segment entirely; a new
  positionGrowingTip() (called after integrateChain() in update()) places
  the tip directly every frame instead, using a new tipGrowDirection()
  helper that reads the SETTLED chain's own local tangent (the segment
  before the growing one -- never the tip's own current, about-to-be-
  overwritten position) and sets the tip's oldx/oldy to match its new
  position (zero implied velocity, so it can never accumulate momentum of
  its own). The rest of the chain (everything up to the second-to-last
  point) is completely unaffected and behaves exactly as before.

Verified:
- Re-ran the same 900-frame/15s stress test that exposed the bug (with
  cfg.ropeLength explicitly reset to a small value first -- an earlier
  attempt at this same check left a stale, much larger ropeLength from a
  prior test call and produced a misleading "still bad" reading before
  this was caught): 0 non-monotonic points across the full run (every
  point strictly further from the anchor than the last), vs. many with
  the constraint-based approach. Worst single segment-length ratio
  dropped to a smooth, gradually-rising 1.36x by 15s -- a separate, much
  milder, likely pre-existing property of a fixed-iteration solver on a
  very long chain, not a tangle.
- Re-verified the smoothness fix itself still holds with the new
  kinematic approach: the tip segment's length tracks the exact per-frame
  growth amount every single frame (an exact sawtooth), including with an
  initial sideways swing velocity applied.
- Verified via a REAL dispatched pointerdown/pointerup gesture (not just
  direct growRope()/update() calls), held for a simulated 15s: 0
  non-monotonic points, and the growing flag correctly turns off on
  release.
- 0 console errors on a clean shipped-code reload.
- node --check clean; debug hook (window.__debugTMP) removed before
  commit (grep-confirmed 0 references).

--------------------------------------------------------------------------------
2026-09-02  -  05:37 AM EDT

Added:
- Rope endcaps: 2 user-supplied SVG designs (data/Rope/End1.svg,
  End2.svg), selectable via a new "Endcap Design" dropdown in the ROPE
  group ("None (plain round cap)" / "End 1" / "End 2"). Added dropdown as
  a new dev-panel control type (buildDevPanel(), applyValues()) -- the
  first setting this project has needed with a fixed set of named choices
  rather than a continuous/boolean value; other control types (slider,
  color, checkbox) were unaffected. The selected cap draws at the free/tip
  end of the main rope and every fallen piece (render() calls the same
  drawEndcap() for both -- no special-casing needed for a cut piece's own
  tip to pick it up automatically), REPLACING the plain round cap entirely
  rather than stacking with it (endArcMult forced to 0 when a design is
  selected). Embedded both SVG paths as Path2D objects with their real
  bounding-box geometry (measured via a real browser DOM's getBBox(), not
  the SVGs' own far-larger 129.7x183.44 viewBox) so the cap scales to
  match the rope's current thickness exactly by width, and rotates to
  continue in the tip segment's actual current direction (not just
  straight down) via translate/rotate/scale/anchor-shift in that order.

Verified:
- Live in the Browser pane: the Endcap Design dropdown renders with all 3
  options and updates cfg on change.
- Direct pixel inspection of the rendered canvas (getImageData, since
  requestAnimationFrame doesn't run in this testing environment): with
  the rope hanging straight down, a vertical scan below the tip showed
  rope-brown color transitioning cleanly to solid white (the cap) then to
  background -- confirming correct anchor position, extension direction,
  and color. With the tip segment artificially forced to point sideways
  instead, a horizontal scan showed the same white region now extending
  in that direction -- confirming the rotation math correctly reorients
  the cap to the rope's actual current direction, not a fixed "down".
- Regression: with Endcap Design left at "None", the tip still shows the
  original rope-colored round cap (unaffected pixel colors), confirming
  the SVG addition didn't change existing default behavior.
- 0 console errors on a clean shipped-code reload.
- node --check clean; debug hook (window.__debugTMP) removed before
  commit (grep-confirmed 0 references); scratch probe file
  (svgprobe_tmp.html, used only to read getBBox() for both SVGs) deleted
  before commit.

--------------------------------------------------------------------------------
2026-09-02  -  06:07 AM EDT

Fixed:
- Dev panel resize floor was hardcoded (minW=220, minH=140 in JS, mirrored
  in CSS #devPanel{min-width:220px;min-height:140px}) instead of derived
  from what the panel's own chrome actually needs, per a workspace
  CLAUDE.md §12c requirement added since the panel first shipped. Added
  computeMinPanelSize(), which measures the header's title scrollWidth +
  collapse button width + padding/gap (for width) and the header's own
  offsetHeight (for height) live from the DOM -- computed fresh at the
  start of every resize-drag (not cached), since the title's rendered
  width depends on the Dev Panel Title Font Size slider. Removed the
  hardcoded CSS min-width/min-height entirely so JS is the sole source of
  truth (the same two-sources-of-truth pattern that caused the earlier
  max-height:88vh bug). Real measured floor: 81x34px.
- Charged-punch intensity (Click-And-Hold on the rope) previously ranged
  from 0 to Intensity Ceiling as hold duration went 0 -> Click Hold Max
  Duration, meaning an immediately-released hold punched at zero. Changed
  to clickIntensity + t*intensityCeiling per explicit request -- Intensity
  Ceiling now stacks ON TOP of Click Intensity, reaching their sum exactly
  at Click Hold Max Duration. Verified via a real dispatched hold: a
  release at Click Hold Max Duration produced exactly clickIntensity +
  intensityCeiling (measured 6 = 2+4) worth of displacement.

Added:
- Rope endcap SVGs are now colored to match cfg.ropeColor instead of
  their authored white, per explicit request. Added an "Endcap Height"
  slider (ROPE group) that stretches the cap along its own local "down"
  axis independently of the width-matching scale, with the top edge
  staying exactly pinned at the rope's tip regardless of the multiplier
  (safe by construction: the anchor-shift translate is the last call in
  drawEndcap()'s transform chain, so it acts first on the raw path
  coordinates, landing the anchor at local (0,0) where no scale factor
  can move it). Verified: at 2x height, the cap's measured pixel extent
  was exactly 2x; the exact tip pixel stayed cap-colored at every tested
  multiplier (0.5x-3x).
- Baked in another user-provided Copy Settings dump as new hardcoded
  defaults: Rope Length -> 8.653846153846153, Endcap Design -> "end2",
  Click Distance -> 8.9, Click Intensity -> 1.6, Click And Hold Distance
  -> 22, Rope Animation Speed -> 1, Double Click Threshold -> 1000
  (exactly at its existing max, no expansion needed), Cut Speed -> 1.
  Reordered the ROPE group to match the dump (Endcap Design, Endcap
  Height, Rope Thickness, Rope Length, Rope Color, Rope End Curve Arc).
  Minimum Rope Length was deliberately kept at 0.5 rather than the dump's
  1 -- the dump appears to have been captured before the explicit "make
  min rope length .5" request earlier in the same turn took effect;
  flagged to the user rather than silently going either way.

Diagnosed (no code change):
- Double-clicking just outside the circle's hold-to-grow margin (isOnCircle
  false, but still visually close to the circle) is treated as a normal
  rope double-click targeting whichever point is nearest -- which, that
  close to the anchor, is index 1-2. Reproduced directly: a double-click
  3px past the circle's margin cut the rope from totalLength 124.6 down to
  49.8 (pointCount 3). This is the cut mechanism working exactly as
  designed; it now SUCCEEDS where it would previously have been rejected
  because Minimum Rope Length is 0.5 (down from 1 or 8 in earlier rounds)
  -- a direct, expected consequence of that setting, not a separate bug.
  No "no-cut zone near the circle" was added, since that would be a new,
  unrequested feature rather than a fix.

Verified:
- 0 console errors on a clean shipped-code reload (network log confirmed
  the actual page responses were 200 OK throughout; a couple of
  ERR_CONNECTION_RESET console lines on 2 earlier reloads were a Browser-
  pane-side transient, not present on the final reload).
- node --check clean; debug hook (window.__debugTMP) removed before
  commit (grep-confirmed 0 references).

--------------------------------------------------------------------------------
2026-09-02  -  07:01 AM EDT

Fixed:
- Real tangling bug: cutting the rope down to just 2 points (a near-anchor
  cut) and then repeatedly using hold-to-grow produced a tight,
  physically-wrong knot right at the circle -- reported with a
  user-supplied screen recording (datalog/Recording 2026-09-02
  064728.mp4). Root-caused precisely: tipGrowDirection() computes a
  direction from prev/beforePrev, but for a chain this short (n<=2),
  pts[n-3] underflows past index 0 and falls back to pts[0] -- the SAME
  point prev already resolves to -- making dx=dy=0. The old `|| 1`
  fallback only guarded the divisor, not this case, so it silently
  returned direction (0,0). positionGrowingTip() and growRope()'s commit
  then placed new points at EXACTLY prev's position (the anchor),
  collapsing multiple chain points onto the same coordinate;
  integrateChain()'s distance constraint, solving a real segLen against a
  ~0 measured distance next frame, produced an effectively
  arbitrary-direction correction (the 0.0001 distance floor amplifies
  near-zero dx/dy float noise into a near-random kick) -- exactly the
  observed knot. Fixed by having tipGrowDirection() fall back to straight
  down (the rope's own authored default) whenever there's no real local
  tangent yet (dlen < 1e-6), covering both call sites (growth-in-progress
  positioning and point-commit) with one change.
- Double-clicking near (but not literally on) the circle could still cut
  the rope very short -- reported twice ("double click within the bounds
  of the circle... cut at the shortest length possible"); the first
  round's diagnosis (this was just the cut mechanism reacting correctly
  to the newly-lowered Minimum Rope Length) turned out to be incomplete.
  cutRopeAt() now also refuses a cut whose TARGET point (hit.x/hit.y, not
  the press position) falls within isOnCircle()'s margin -- a double-click
  offset mostly sideways from the circle registers as a normal rope click
  at press time (isOnCircle false there), but its nearest rope point can
  still project to well within the circle's margin (the rope hangs
  roughly vertical, so a click offset in X still finds its nearest point
  at roughly the same Y). Reuses the existing isOnCircle margin rather
  than inventing a new threshold.

Clarified (not a bug):
- Copy Settings and Save Settings capturing each collapsible group's
  collapsed/expanded state is the workspace CLAUDE.md SS12e standard, not
  a bug -- "whichever groups were open or collapsed at Save time come
  back in that same state," explicitly by design.

Verified:
- Degenerate-direction fix: forced a real 2-point rope, ran 5 separate
  hold-to-grow cycles via dispatched pointer events with manually-stepped
  physics (requestAnimationFrame doesn't run in this testing
  environment) -- 0 non-monotonic points and minPairDist never dropped
  below ~22px (targetSeg ~32px) across all 5 cycles, vs. immediate
  point-collapse before the fix.
- Circle-cut-exclusion fix: a click offset sideways from the circle
  (isOnCircleAtClick:false) whose nearest rope point lands within the
  margin (hitWasOnCircle:true) now correctly produces cutHappened:false;
  a normal double-click well clear of the circle still cuts successfully
  (regression-checked). Also re-confirmed (twice, with realistic click
  timing) that a double-click dead-center on the circle was already safe
  before this fix and remains so.
- 0 console errors on a clean shipped-code reload.
- node --check clean; debug hook (window.__debugTMP) removed before
  commit (grep-confirmed 0 references).

--------------------------------------------------------------------------------
2026-09-02  -  7:14 AM - 7:21 AM EDT

Implemented §12l/§12m, new sections in the workspace CLAUDE.md since this
project's dev panel last caught up with it -- discovered by re-reading it
fresh (per explicit request) and diffing against the project's own current
state, since everything through §12k turned out already covered by
concurrent work earlier this session.

Added:
- Save Settings now also writes {values, order} through to a git-tracked
  data/processed/dev-panel-settings.json (§12l), not just localStorage.
  A static page can't silently write an arbitrary disk path, so this uses
  the File System Access API: writeGitSettingsLog() /
  getGitSettingsFileHandle(), with the resulting handle persisted in
  IndexedDB (idbGet()/idbSet()) so only the FIRST save on a given browser
  needs the native picker dialog (navigate to data/processed/, keep the
  suggested filename) -- every later save reuses the stored handle
  silently. Chromium-only (Firefox/Safari lack the API); feature-detected
  and fully best-effort -- localStorage remains the complete baseline
  regardless of whether this succeeds.
- Documented §12m's "set defaults" merge workflow (a pasted Copy Settings
  dump merged against the git-tracked log per-field, per the exact O/G/P
  rule §12m specifies) in this project's own CLAUDE.md as a standing
  behavioral convention -- not page code, a rule for how a future request
  to "set defaults" should be handled.
- data/processed/ directory created for the new settings-log file.

Verified:
- IndexedDB read/write layer directly (idbSet/idbGet round-trip on plain
  serializable data).
- getGitSettingsFileHandle() calls showSaveFilePicker with the correct
  suggestedName/type when no stored handle exists yet.
- The full failure path end-to-end: a mocked handle that IndexedDB can't
  structured-clone (a real FileSystemFileHandle can -- this is a
  documented platform guarantee, not something this project's code
  controls) is caught gracefully, writeGitSettingsLog() returns false, and
  saveSettings() reflects that honestly in the Save button's flash text
  ("Saved (local only)") rather than claiming success.
- The property that actually matters: localStorage's own save happens
  first and synchronously in saveSettings(), and completed correctly
  (confirmed via direct localStorage inspection) regardless of the git-log
  write's success or failure -- the enhancement can never block or corrupt
  the existing baseline.
- node --check clean; debug hook (window.__debugTMP) removed before
  commit (grep-confirmed 0 references).
- Not verified: an actual successful write through a real (non-mocked)
  native save dialog -- this headless environment can't safely drive a
  real OS file picker, so the write() call itself was exercised only via
  documented API usage patterns, not a live end-to-end file write.

--------------------------------------------------------------------------------
2026-09-02  -  07:29 AM EDT

Fixed:
- Double-clicking near (but not literally on) the circle could still cut
  the rope very short -- reported a THIRD time after 2 earlier fix
  attempts. Round 2's fix (cutRopeAt() reusing isOnCircle()'s own margin
  to reject a too-close cut target) turned out to still not be generous
  enough for the user's real clicks. Replaced the implicit reuse with a
  dedicated, independently user-tunable "Circle Cut Distance" dev slider
  (ROPE CUT group, default 10 %vmin, measured directly from the anchor
  rather than derived from isOnCircle()'s own radius) per explicit
  request. Verified the same targeted click-offset-sideways reproduction
  used in round 2 still correctly gets rejected under the new mechanism,
  a normal cut well clear of the circle still succeeds, and a genuine
  dead-center double-click on the circle remains safe (unaffected,
  confirmed by regression check -- that path never reaches cutRopeAt()
  at all).

Added:
- "Click And Hold Flick Distance Threshold" slider (CLICK group, default
  0 = no minimum, preserving prior behavior exactly): gates a charged
  hold's release on how far the pointer actually traveled from press to
  release position, independent of the existing Click And Hold Distance
  (which gates on distance from the rope at release, not on drag
  distance). Verified: a ~1px release is blocked once the threshold is
  raised above it (0 displacement, vs. the expected punch); a release
  whose drag distance exceeds the threshold still fires with the same
  displacement magnitude measured before this gate existed.
- Minimum Rope Length's slider range widened to 0.1-15 %vh (previously
  0.4-30), default left unchanged at 0.5, per explicit request.
- Moved Floor Enabled/Color/Thickness/Height out of the ROPE CUT group
  into their own new "FLOOR" group, per explicit request. Purely a
  DEV_GROUPS reorganization -- cfg population and floor collision logic
  are unaffected (both are keyed by setting name, not by which group
  declares it).

Verified:
- Live in the Browser pane: all 3 new/moved controls render in their
  correct groups (CLICK: flickDistanceThreshold; ROPE CUT: minRopeLength,
  circleCutDistance; new FLOOR group: floorEnabled/floorColor/
  floorThickness/floorHeight) via a direct DOM query of every
  .dp-group's .dp-row children, not just visual inspection.
- 0 console errors on a clean shipped-code reload (a network-request
  check confirmed the final reload was 200 OK; console noise from 2
  earlier flaky navigate() calls was the same already-documented
  Browser-pane transient, not a real app error).
- node --check clean; debug hook (window.__debugTMP) removed before
  commit (grep-confirmed 0 references).

--------------------------------------------------------------------------------
2026-09-02  -  07:45 AM EDT

Removed:
- The "Click And Hold Flick Distance Threshold" slider (CLICK group,
  added the immediately-prior round) -- removed one message later per
  explicit request. Both the DEV_GROUPS entry and its onPointerUp()
  gating check (dragDist < vmin(cfg.flickDistanceThreshold)) were
  deleted entirely; the corresponding CODE_SUMMARY.md Gotchas entry was
  removed too rather than left describing a control that no longer
  exists.

Changed (reorganization only, per explicit request -- no cfg keys,
wiring, or behavior changed, only which DEV_GROUPS group declares each
control):
- The MECHANICS group is gone. Click Hold Max Duration and Intensity
  Ceiling moved into ROPE GROWTH (alongside Rope Growth Rate). Double
  Click Threshold moved into ROPE CUT.
- Click Distance moved from CLICK into ROPE CUT, joining the other
  double-click-related controls there (Minimum Rope Length, Circle Cut
  Distance, Double Click Threshold) -- "stick the double click sliders
  all in the rope cut group," per explicit request. CLICK is now just
  Click Intensity and Click And Hold Distance.

Verified:
- Live DOM query of every .dp-group's .dp-row children confirmed the
  final grouping exactly: CLICK -> [clickIntensity, holdDistance]; ROPE
  GROWTH -> [ropeGrowthRate, clickHoldMaxDuration, intensityCeiling];
  ROPE CUT -> [minRopeLength, circleCutDistance, clickDistance,
  doubleClickThreshold, ropeFallSpeed, cutSpeed]; no MECHANICS group;
  flickDistanceThreshold absent everywhere (grep-confirmed 0 references
  in index.html).
- node --check clean.
- 0 console errors on a clean shipped-code reload (network-request
  check confirmed 200 OK; console noise from flaky navigate() calls was
  the same already-documented Browser-pane transient).

--------------------------------------------------------------------------------
2026-09-02  -  07:48 AM - 08:00 AM EDT

Changed (correction -- the workspace CLAUDE.md's §12d/§12l wording was
explicitly revised since this project's dev panel last implemented them):
- Save Settings now writes ONLY to the git-tracked settings log
  (data/processed/dev-panel-settings.json via the File System Access API)
  -- the previous dual-write to localStorage as a parallel "baseline" is
  removed entirely. Reset now reads from that same git-tracked log only,
  not from any local default.
- getGitSettingsFileHandle() gained a `mode` ('read' | 'readwrite')
  parameter: boot-time/Reset reads use only the non-prompting
  queryPermission() and silently fall back to hardcoded defaults with no
  saved log yet reachable (there's no user gesture at boot to back a
  native permission prompt); Save always runs from a real click/keydown,
  so it can fall through to requestPermission()/showSaveFilePicker() when
  needed.
- Added readGitSettingsLog() (mirrors writeGitSettingsLog()) and an
  in-memory lastLoadedSnapshot, which copySettings() and the Desktop/
  Mobile tab-switch handler now use to preview the "other" device's saved
  panel geometry, replacing the removed localStorage reads.
- resetSettings() is now async.
- Updated the stale §12l source comment and the DICKOCLICKO CLAUDE.md
  Gotchas entry, both of which previously described the old dual-write
  design.

Verified:
- node --check clean on the extracted script.
- grep-confirmed 0 remaining `localStorage` references anywhere in
  index.html (only a comment noting its deliberate absence).
- Live-tested via a temporary debug hook (window.__debugTMP, removed
  before commit, grep-confirmed 0 references) against a standalone static
  file server (the shared dev-server port was occupied by another
  concurrent session): cold-boot Reset with no git log reachable yet
  leaves cfg/geometry at defaults without throwing; Save without a
  gesture-backed handle available fails gracefully ("Save failed" flash,
  no localStorage keys written); Desktop/Mobile tab-switching and Copy
  Settings still function correctly off lastLoadedSnapshot with no
  persisted snapshot yet (Copy's clipboard write was blocked by this
  headless browser's "document not focused" restriction -- a pre-existing
  environment limitation unrelated to this change, confirmed by
  reproducing the same block via a direct clipboard.writeText() probe).
  A real showSaveFilePicker()-backed round trip (native picker dialog,
  actual disk write, actual permission grant) could not be exercised in
  this headless environment -- this is the same limitation already
  documented for the original §12l implementation (a FileSystemFileHandle
  mock with function properties is not structured-clone-able, so it can't
  survive the real idbSet() call under test).

--------------------------------------------------------------------------------
2026-09-02  -  08:07 AM EDT

Investigated (no code fix -- see next entry for the endcap work that WAS
shipped this round):
- 4th round of a recurring "rope physics is erratic" complaint, this time
  with no fresh video. Ran an extensive reproduction sweep (settled rope
  + hold-to-grow exactly as described, a single strong punch traced for
  10s of settling, 5 rapid repeated punches without full settling between
  them) via real dispatched PointerEvents + manually-stepped update()
  calls (requestAnimationFrame doesn't run in this testing environment).
  Every REPRODUCIBLE run showed 0 NaN, 0 sustained non-monotonic points,
  the same already-known ~1.02-1.03x steady-state segment-length ratio
  (not diverging), and kinetic-energy-proxy decaying normally after
  repeated hits rather than accumulating. One run did show a severe
  60-frame-long anomaly (54-130px per-frame steps) but could not be
  reproduced again across 3 follow-up attempts with the identical script
  -- logged as an unexplained one-off, not trusted as evidence either way.
  Asked the user to clarify what "crazy" means rather than guess-tuning
  DAMPING/CONSTRAINT_ITERATIONS blindly on the 4th round of a vague
  repeat; answer: "jitters at rest, as well as jitters drastically when
  triggered to move... sometimes the end of the rope will swing wildly."
  "Jitters at rest" specifically has not yet been directly reproduced --
  next step before any tuning change.

Added:
- 3rd rope endcap design (data/Rope/End3.svg) plus a new shared scale/
  anchor reference for ALL 3 designs (ENDCAP_ALIGNMENT, from a new
  data/Rope/End Alignment.svg -- a plain <line x1="41.57" y1="80.26"
  x2="96.06" y2="80.26">), replacing the previous per-design getBBox()
  measurement, per explicit request: End3 was authored to start exactly
  at the alignment line's own (x1,y1), confirming it's meant as the
  shared reference every design should scale/anchor against, so swapping
  designs in the dropdown changes only shape, never apparent size or
  attachment point. drawEndcap() now computes scale/anchor from
  ENDCAP_ALIGNMENT.width/topCenterX/topY unconditionally (outside any
  per-design branch), so this is correct by construction, not something
  that needs re-verifying per design.

Verified:
- End3 renders with a reasonable, non-degenerate scale/extent (~29px tall
  at ~20px rope thickness, consistent with its own path geometry scaled
  by thicknessPx/54.49) and 0 console errors.
- node --check clean; both temporary debug hooks used this round
  (window.__debugTMP2 for the endcap check, plus the earlier physics
  investigation's hook) removed before commit (grep-confirmed 0
  references to either name). Left another session's own separate,
  unrelated, still-in-progress debug hook (window.__debugTMP, for a
  concurrent §12d/§12l settings-persistence revision) untouched -- not
  this round's to clean up.

Note: another session made a real, substantial, uncommitted revision to
this same index.html concurrently (dropping the localStorage settings
fallback entirely in favor of the git-tracked log only, correcting
§12d/§12l) while this round's work was in progress. Confirmed via diff
hunk-header inspection that none of it overlaps the endcap/physics code
touched this round.

--------------------------------------------------------------------------------
2026-09-02  -  08:18 AM EDT

Fixed:
- Resolved a 4th-round "rope physics is erratic/jitters/swings wildly"
  report. Extended the prior round's investigation with a precise
  at-rest test: a fully-settled rope's tip position was traced with full
  floating-point precision (not rounded) across 120 real physics frames
  and showed EXACTLY 0 movement, bit-for-bit identical every frame --
  conclusively ruling out a "jitter at rest" bug in the solver itself.
  Separately measured how long a single punch takes to visibly settle at
  the OLD defaults (DAMPING=0.99, CONSTRAINT_ITERATIONS=6): ~3.65s of
  real, visible swinging before dropping below a 0.3px/frame movement
  threshold. Concluded this long, very-lightly-damped settling time is
  the likely real source of the "erratic" perception, especially under
  frequent real-world interaction (the rope rarely gets a chance to
  fully settle between clicks).

Changed:
- DAMPING and CONSTRAINT_ITERATIONS were hardcoded module constants;
  converted to cfg.damping / cfg.constraintIterations dev-panel sliders
  (ROPE ANIMATION group) so the feel can be tuned live going forward
  without another code round-trip. Swept several damping/iteration
  combinations, measuring settle time AND worst-case segment-stretch
  ratio together (not one at the expense of the other): the new default
  0.85/10 settles a real punch in ~1.77s (vs 3.65s at the old 0.99/6)
  while ALSO reducing worst-case stretch (~1.07x vs ~1.15x), and a
  fully-settled rope under the new tuning still shows essentially zero
  residual movement (~0.001px/frame -- below any visible threshold).
  Re-verified the 2-point-rope degenerate-direction fix (2 rounds ago)
  still holds under the new tuning: 0 non-monotonic points, no near-zero
  point spacing, across a real 5-second hold-to-grow cycle.

Added:
- 4th endcap design (data/Rope/End4.svg), added to ENDCAP_DESIGNS/the
  Endcap Design dropdown using the same shared ENDCAP_ALIGNMENT reference
  as End1-3 -- a mechanical, same-pattern addition; per explicit request
  ("don't verify"), not re-verified live in the browser this round
  (node --check stayed clean).

Note: this round's index.html commit also carries a real, substantial,
concurrent revision from another session (further work on the §12d/§12l
settings-persistence layer) that was present in the working tree
alongside this round's own changes.

--------------------------------------------------------------------------------
2026-09-02  -  08:20 AM - 08:27 AM EDT

Added:
- "Endcap At Cut End" checkbox (ROPE CUT group, cfg.endcapAtCutEnd,
  default off), per explicit request: when on, each fallen (cut-off)
  rope segment also gets the selected endcap shape at its severed/cut
  edge -- not just its original tip end, which already had one. Reuses
  drawEndcap() as-is by passing the fallen piece's points array reversed
  (`[...piece.points].reverse()`), which puts the cut point at the index
  drawEndcap always caps and its neighbor at the index it uses for
  orientation -- no new drawing code needed. mainRope itself needed no
  change: its own tip already IS the cut point right after a cut (the
  piece that falls away keeps the other end), so it was already correctly
  capped by the existing call. Only takes effect when an Endcap Design
  other than "None" is selected, same gate as the existing tip-end cap.

Verified:
- node --check clean on the extracted script.
- Live-tested via a temporary debug hook (window.__debugTMP, removed
  before commit, grep-confirmed 0 references) against a standalone
  static file server (this project's shared dev-server port was occupied
  by a different concurrent session): a synthetic fallen piece rendered
  with the checkbox off showed only the baseline round line-cap pixel
  coverage at its cut end (666 magenta pixels in a 50x50px sample); with
  the checkbox on, coverage rose to 855 pixels at the same spot,
  consistent with the SVG endcap shape actually being drawn there; the
  piece's unrelated tip end was unaffected either way (847 vs 850,
  within anti-aliasing noise), confirming the existing tip-cap behavior
  wasn't disturbed. Also confirmed the checkbox renders in the ROPE CUT
  group (live DOM query) and that a real checkbox `change` event
  correctly toggles `cfg.endcapAtCutEnd`.
- This session's headless preview pane needed 2 environment workarounds
  unrelated to the feature itself, both confirmed as pre-existing
  environment quirks rather than app bugs: `canvas.width`/`height` were
  still 0 at the moment of the first direct `render()` call (the
  boot-time `resizeCanvas()` call had already run correctly, but the
  pane's own uncomposited/non-visible state meant the canvas element's
  backing store hadn't picked it up yet) -- patched directly for the
  test; and `mainRope.points` was empty for the same reason
  (`resetMainRope()` ran before `window.innerHeight` had a real value in
  this pane) -- patched with 2 placeholder points so `render()` wouldn't
  throw on the unrelated main-rope draw call.
- Noted, not acted on: a different concurrent session is actively adding
  DEV_MODE click-diagnostics logging (`logClick()`, new console.log calls
  in `onPointerDown`) to the same file's working tree, uncommitted at the
  time of this commit -- present already-integrated cleanly (verified via
  `node --check` on the combined file) and unrelated to this task, so it
  rides along in the same commit rather than being split out, per this
  same file's own established precedent 2 entries up.

--------------------------------------------------------------------------------
2026-09-02  -  08:28 AM - 08:35 AM EDT

Changed (§12l revision -- the workspace CLAUDE.md gained a genuinely new
paragraph this round, not just a wording tweak: an explicit "no server,
opened directly as a local file" exception, located via a byte-size/
line-count delta against the version already read this session, 85283->
86645 bytes / 719->725 lines):
- Added a "no File System Access API at all" fallback to
  saveSettings()/resetSettings(): a new GIT_LOG_WRITABLE constant
  ('showSaveFilePicker' in window, checked once) now gates between the
  existing git-log-only path and a new SESSION_FALLBACK_KEY sessionStorage
  fallback. Scoped deliberately narrow -- this only engages when the API
  is structurally absent (Firefox/Safari, or this page opened as a bare
  local file in one of those), not for an ordinary picker cancel or
  permission denial in a capable browser, which still just shows "Save
  failed" as before. sessionStorage was chosen over localStorage on
  purpose: it's gone the moment the tab closes, so it can never become
  the persistent "separate browser-local default" §12d/§12l otherwise
  bans across this whole spec -- it exists only so a Save made in a
  capability-less browser has anything for that same tab's own Reset to
  read back. The Save button says "Saved (session only)" in this mode,
  never "Saved!", so it's never mistaken for a real git-log write.

Verified:
- node --check clean.
- Live-tested both branches via a temporary debug hook
  (window.__debugTMPropo, removed before commit, grep-confirmed 0
  references) against a standalone static file server: (1) a throwaway
  test copy of index.html with `delete window.showSaveFilePicker;`
  injected before the main script (never touching real index.html)
  confirmed GIT_LOG_WRITABLE correctly reads false, Save writes to
  sessionStorage and shows "Saved (session only)", and a subsequent
  Reset correctly restores the saved marker value from sessionStorage;
  (2) the real, unmodified index.html confirmed GIT_LOG_WRITABLE reads
  true when the API is genuinely present, sessionStorage is never
  touched (null before and after a Save attempt), and Save falls through
  to the pre-existing git-log path exactly as verified 2 rounds ago
  (fails gracefully with "Save failed" when no gesture-backed handle is
  available in this headless environment) -- confirming zero regression
  to the normal-capability path.
- Noted, not acted on: a different concurrent session is still actively
  developing DEV_MODE debug scaffolding (window.__debugTMP2, growing
  since the previous entry) in the same file's uncommitted working tree
  -- left untouched and disclosed in the commit, same as the prior
  entry's logClick() situation.

--------------------------------------------------------------------------------
2026-09-02  -  08:49 AM - 08:54 AM EDT

Fixed (correction to the immediately-preceding entry's §12l fallback,
per explicit user clarification: "it should no longer trigger a local
file save and only a cache save when its opened locally not on a
server"):
- GIT_LOG_WRITABLE was gating purely on File System Access API
  capability (`'showSaveFilePicker' in window`), which stays true even
  when this page is opened as a bare local file in a browser that
  happens to support the API -- meaning Save would have still triggered
  the native save-file picker/disk write in exactly the case the §12l
  exception exists to avoid. Fixed by gating on PROTOCOL first:
  `location.protocol !== 'file:' && 'showSaveFilePicker' in window`
  (matching DEV_MODE's own existing protocol check) -- now false both
  for a bare local file AND for a browser lacking the API, true only
  when actually served (http/https, including a local dev server) with
  the API present.

Verified:
- node --check clean.
- Live-tested server-mode (http://) is unaffected: GIT_LOG_WRITABLE
  still reads true, sessionStorage stays untouched, Save still attempts
  the git-log path exactly as the prior entry verified.
- Could not exercise a genuine file:// load for DICKOCLICKO directly in
  this sandbox (confirmed again: navigating to file:// for a project
  outside this session's own project root silently stays on the
  previous page, not even reaching a real DOM) -- verified the new
  boolean's correctness instead via a truth-table check of the exact
  expression shape shipped (`protocol !== 'file:' && hasPicker`) against
  all 4 relevant input combinations, confirming file://+API-present now
  correctly evaluates false (the bug just fixed) while the other 3 cases
  match prior, already-verified behavior. The `GIT_LOG_WRITABLE===false`
  fallback branch itself (sessionStorage write/read round-trip, button
  text) was already fully round-trip tested in the immediately preceding
  entry and is unchanged by this fix -- only the boolean feeding into it
  changed.
- Noted, not acted on: a different concurrent session landed a large,
  coherent, well-commented feature addition in the same file's working
  tree while this fix was in progress -- new Cut Sweep Color/Thickness
  sliders, a tip-position-smoothing fix for the growing rope tip, a
  shared topplePiece() helper, and a new cutPieceAt() letting an already
  -fallen piece be cut a second time, with cutSweep generalized to a
  per-entity object so mainRope and every piece can each animate their
  own independent cut-sweep mark. Read through the full diff before
  committing (not just diffed size) to confirm it's complete/coherent,
  not a broken mid-edit state; left entirely untouched and disclosed
  here rather than reviewed/altered, since it's outside this task's
  scope and not this session's to judge.

--------------------------------------------------------------------------------
2026-09-02  -  08:24 AM - 09:00 AM EDT

Added / Fixed (4 requests handled this round: a standing click diagnostic,
a follow-up rope-extension-jitter report, an exactly-backwards cut-sweep
placement bug + new color/thickness controls, and double-click-cuttable
fallen pieces -- plus one "set defaults" save):
- Added logClick() (DEV_MODE-gated console.log('[click]', event, data)),
  a permanent diagnostic firing at every click/hold/double-click state
  transition in onPointerDown/onPointerUp. Verified via dispatched
  synthetic pointer events + read_console_messages: a real tap logs
  down:rope -> up:tap-pending -> up:tap-punch in order with correct
  hitDist/intensity.
- Resolved "rope extension still isn't smooth, jittery" (reported right
  after the immediately-preceding entry's damping/iterations fix had
  already resolved the separate, larger "erratic swinging" complaint).
  Root-caused via direct instrumentation (manual step-by-step
  reconstruction of update()'s own call order proved unreliable across
  repeated attempts -- landed on driving growth through the real update()
  and reading pts[pts.length-1].y every frame, exactly what
  strokeRopeCurve() renders): the real physics point behind the growing
  tip still "breathes" slightly every frame (ordinary fixed-iteration-
  solver residual) even at visual rest, and that reference point's
  IDENTITY switches to a newly-committed point every time a segment
  completes -- positionGrowingTip() anchored the tip's absolute position
  to it directly, showing both through 1:1. Fixed with 2 changes: (1)
  growRope()'s commit code now gives a newly-finalized point (and the
  fresh tip started right after it) its neighbor's CURRENT velocity
  instead of zero -- measured negligible effect alone (the ~+14.9px
  step at each commit boundary was ~unchanged, 14.82px vs 14.90px) but
  kept as independently correct; (2) positionGrowingTip() now lerps
  toward its target instead of snapping (smoothing factor 0.25, chosen
  by sweeping values on a clean 180-frame hold-to-grow trace and
  counting backward-motion frames -- the only genuinely visible
  "jitter," since the discrete forward step is monotonic with the
  overall growth direction and lasts one 16.7ms frame every ~14 frames:
  no smoothing -> 19-20 backward frames/180, worst -2.94px; 0.4 -> 20
  frames, worst -0.82px; 0.25 -> 14 frames, worst -0.67px). Growth
  accounting (tipGrowLen/totalLength) is untouched; only the rendered
  pixel position lags slightly, imperceptible against ~2px/frame growth.
- Fixed a real, exactly-backwards bug: the double-click-cut sweep-mark
  was staying with the FALLING piece, not the remaining rope still held
  by the circle ("the white line... should stay with the rope instead
  of the cut segment"). Generalized cutSweep from a per-piece property
  (cutProgress/cutNx/cutNy/cutStartOffset/cutEndOffset on the falling
  piece) to a per-entity object (mainRope.cutSweep, or
  fallenPieces[i].cutSweep) belonging to whichever entity keeps its own
  identity through a cut -- renderCutSweep(entity) is now generic;
  update()/render() advance/draw mainRope's AND every piece's cutSweep
  independently. Added cfg.cutSweepColor/cfg.cutSweepThickness dev
  sliders (ROPE CUT group), replacing the previously hardcoded
  '#ffffff' / Math.max(2, vmin(cfg.ropeThickness)*0.35), per the same
  request. Verified via a scripted cut (grew a rope past the circle-cut
  exclusion zone, dispatched a real double-click): the remaining rope
  carries mainHasSweep:true, the newly-detached piece carries
  pieceHasSweep:false.
- Added double-click-to-cut on already-fallen pieces (cutPieceAt),
  splitting one piece into two independent pieces at the click point --
  per explicit request ("I can also double click on the cut segments to
  cut them into 2 segments"). Same tangent/normal/toppling geometry as
  the original rope-cut's own piece-detach, factored into a shared
  topplePiece() helper. hitTestAny(x,y) returns whichever of the main
  rope or the nearest in-range fallen piece is closer (hitTestPieces
  checks every piece), tagged rope/piece -- used only for double-click
  targeting, resolved fresh at release time since a piece may have
  moved since the press. Single-tap punch and hold-to-charge
  deliberately stayed rope-only (unchanged scope, matching the
  request's own wording) -- the quick-tap branch now arms pendingClick
  off hitTestAny() (so a piece-aimed double-click still registers), but
  the single-tap-punch timeout still gates on the original press-time
  rope-only hit, unchanged from before. Verified via a scripted
  double-click on a fallen piece: 1 piece -> 2 pieces, the kept-identity
  half carries the new sweep mark, the split-off half doesn't.
- "Set defaults": wrote a user-provided Copy Settings dump as
  data/processed/dev-panel-settings.json -- this file didn't exist yet,
  so this was a first save (no §12m per-field merge needed, nothing to
  merge against).

Also present in this round's commits (see below): a concurrent session's
own settings-persistence work (§12l file:// protocol gating), landed in
the same file at the same time. Confirmed via `git diff`/`git show`
inspection that this session's own edits (all of the above) and that
session's edits (GIT_LOG_WRITABLE's protocol check, an "Endcap At Cut
End" checkbox) ended up swept into the SAME few commits by the other
session's own `git add`/`git commit` -- both sessions were editing
index.html concurrently with no file-locking, so a broad `git add`
picked up whichever edits were present in the working tree at that
moment, regardless of which session made them. Nothing was lost or
overwritten; verified this session's own features work correctly via
direct state inspection (per-feature results above) regardless of which
commit they ended up landing in. This session's only own commit this
round is a small trailing cleanup (removing a temporary debug hook) --
the substantive work above already existed in the working tree by the
time this session got to `git commit`.

Verified:
- node --check clean throughout every intermediate edit.
- Click diagnostics: dispatched synthetic pointer events, confirmed the
  exact expected log sequence via read_console_messages.
- Extension smoothing: quantitative before/after comparison on identical
  180-frame traces (see numbers above), not just visual impression.
- Cut-sweep placement + piece-splitting: scripted end-to-end tests via
  dispatched pointer events through the real onPointerDown/onPointerUp
  handlers (not calling cutRopeAt/cutPieceAt directly), confirming both
  the cut geometry AND the sweep-mark ownership.
- Fresh page load: no console errors beyond the known
  ERR_CONNECTION_RESET Browser-pane transient; dev panel renders with
  the new Cut Sweep Color/Cut Sweep Thickness controls present.
- Did not live-verify: the actual visual smoothness improvement in a
  rendered frame (rAF doesn't fire in this Browser pane environment) --
  verification was via the underlying position data the render loop
  reads from, not a rendered screenshot.

--------------------------------------------------------------------------------
2026-09-02  -  09:07 AM - 09:29 AM EDT

Fixed the real root cause behind 2 physics complaints, plus 3 smaller
fixes (5 requests handled this round, one of which arrived as 2 new
user-recorded videos mid-turn rather than text):
- Found and fixed the ACTUAL root cause behind both "extension still
  jittery" (a follow-up after the immediately-preceding entry's
  damping/iterations fix had already resolved the larger "erratic
  swinging" complaint) and a separately-reported "crazy movement when a
  single click is applied": the rope had ZERO resistance to bending.
  Pure distance (PBD-style) constraints only check inter-point distance,
  never the angle between consecutive segments -- a segment pair folded
  fully back on itself (180°) satisfies the solver exactly as well as if
  it were straight. applyPunch()'s Gaussian-falloff push (different
  points get pushed by different amounts in the same direction) can
  invert local point order near the free/unanchored tip end, and the
  distance solver alone has no way to ever recover from that -- it's a
  stable configuration, not an unstable one. Diagnosed from 2 new
  user-recorded videos (frame-extracted via Python + OpenCV, since
  ffmpeg isn't installed in this environment): the single-click video
  showed the rope visibly curl into a hook/knot shape immediately after
  the click and stay visibly kinked for several real seconds (still
  kinked ~5s later, not a normal decaying swing). Directly reproduced by
  replicating applyPunch()'s own math against a live rope: a 2-segment
  span (points[i] to points[i+2], should be ~2*segLen=55.38px if
  reasonably straight) stuck at 23.28px after 3 full simulated seconds
  with the old constraint-only solver -- a persistent ~42%-of-expected
  fold, not a transient. This same "the solver doesn't care about local
  angle" instability was ALSO the real source of the extension-jitter
  video's symptom: the endcap's rotation (tipDirection(), a separate
  function from the growing-tip's own position smoothing already shipped
  last round) was flipping by up to a full 180° between frames during
  growth. Fixed with a bending constraint: cfg.bendStiffness (new dev
  slider, ROPE ANIMATION group, default 0.15) pulls every non-pinned
  interior point toward the midpoint of its two immediate neighbors, once
  per constraint iteration (same cadence as the existing distance pass).
  0.15 was the smallest value found to fully resolve the fold
  reproduction (23.28px -> 55.396px, vs expected 55.385px -- essentially
  exact) and the endcap-angle reproduction (max frame delta 180° ->
  0.014°) together; a settle-time regression check (punch a fresh rope,
  measure time to a sustained sub-0.3px/frame tip speed) showed settle
  time actually IMPROVED slightly (~1.35s vs the previously-measured
  ~1.77s) rather than the rope looking stiffer. Re-verified end-to-end via
  the REAL onPointerDown/onPointerUp handler path (not just the direct
  synthetic-math reproduction): a dispatched punch on a live rope settled
  to a fold-free 2-span ratio of exactly 1.000 after 5 real simulated
  seconds.
- Fixed the cut-sweep mark's perpendicular direction being frozen at the
  moment of the cut (stored as nx,ny on the cutSweep object) instead of
  following the entity's rotation afterward -- per explicit report ("the
  cut line should follow the rotation and position of the end of the line
  it was cut from instead of staying static"). A separate, earlier report
  in the same message ("the cut line max length is the width of the
  rope... should start at the edge of the rope to the other edge") turned
  out to be the SAME root cause described differently: the mark's actual
  world-space length was always exactly ropeThickness (startOffset/
  endOffset are pure +-magnitudes, unaffected by rotation), but a stale
  frozen direction made its apparent span look mismatched against the
  rope's ACTUAL current edges once the rope had rotated even slightly.
  renderCutSweep() now calls tipDirection(entity.points) fresh every
  frame instead of reading nx,ny off the cutSweep object; removed nx,ny
  from both cutRopeAt()'s and cutPieceAt()'s cutSweep object literals
  (the local nx,ny variables are still used for the side calculation,
  just no longer stored). Verified via tipDirection() directly: forcing
  the last two points into a new relative position changes the returned
  direction immediately.
- Added a visible click/hold/double-click log INSIDE the dev panel itself
  (#dpClickLog, a scrollable area below the settings groups, capped at
  200 entries, a Clear button next to it) -- per explicit follow-up
  ("where is my mouse click data log. it should show in the dev panel").
  The earlier round's logClick() had only written to console.log, which
  was the wrong read of "add a click diagnostic log" -- it needed to be
  visible in the panel UI itself. Verified live via the real
  onPointerDown/onPointerUp path (not calling logClick() directly): a
  dispatched rope tap produced exactly the expected 3 new log lines in
  #dpClickLog's DOM.
- Fixed: clicking Save while viewing a Vercel deployment still triggered
  the native local-file-save prompt -- per explicit report ("When i click
  save, while viewing in vercel, it should also not trigger a local save
  prompt"). GIT_LOG_WRITABLE was gated on `protocol !== 'file:'` alone,
  which stays true for ANY non-file: origin including an ordinary hosted
  deployment (Vercel, GitHub Pages, etc.) that has no locally-tracked
  repo file for that write to meaningfully "track" through. Added a
  DEV_MODE requirement on top (not a replacement -- DEV_MODE alone still
  allows file:, which is exactly the case the existing protocol check
  needs to keep excluding): `DEV_MODE && location.protocol !== 'file:' &&
  'showSaveFilePicker' in window`. Verified via a truth-table check of
  the 3-input boolean across the relevant cases -- only "hosted origin,
  no explicit ?dev=1" flips from the old (wrong) true to the new
  (correct) false; every other case (localhost, bare file://, hosted with
  ?dev=1, no API at all) is unchanged.
- "Set defaults" (§12m): merged a second user-provided Copy Settings dump
  against the git-tracked settings log written earlier this session --
  since nothing else had touched that file since it was first written
  (O===G), the merge reduced to writing the pasted dump directly (P wins
  wherever it differs from O, and where it doesn't, G===O anyway).

Verified:
- node --check clean throughout every intermediate edit.
- Bending constraint: reproduced the fold BEFORE the fix (23.28px vs
  expected 55.38px), confirmed it resolves AFTER (55.396px), confirmed
  the same fix resolves the independently-discovered endcap-rotation
  instability (180° max delta -> 0.014°), confirmed via a settle-time
  regression check that normal swinging isn't made rigid, and confirmed
  end-to-end via the real dispatched-pointer-event handler path, not just
  direct-math reproduction.
- Cut-sweep rotation: confirmed tipDirection() reflects a forced geometry
  change immediately (the same function renderCutSweep() now calls fresh
  every frame).
- Click log UI: confirmed #dpClickLog receives real DOM entries via the
  actual click-handler path, in the expected order, with correct data.
- GIT_LOG_WRITABLE: truth-table verification (this sandbox cannot
  exercise a genuine Vercel-hosted load).
- Fresh page load: no console errors beyond the known
  ERR_CONNECTION_RESET Browser-pane transient; dev panel renders with the
  new Bend Stiffness slider and Click Log section present.
- Did not live-verify: an actual rendered screenshot showing the visual
  smoothness/knot-free result (this Browser pane doesn't run
  requestAnimationFrame) -- verification was via the position/angle data
  the render loop itself reads, and via frame-extraction from the user's
  own screen recordings, not a freshly rendered screenshot from this
  session.

--------------------------------------------------------------------------------
2026-09-02  -  09:30 AM - onward EDT

Added (per explicit request to "reread claude md and implement details in
regards to saving to github with the DEV_PANEL_SAVE_SECRET," followed by
"do both" once asked whether that meant the newly-added §12l local-save-
prompt detail alone, a full Vercel/GitHub-API write path like CLICKO/
BUTTSONS/WALKING built, or both):
- New api/save-settings.js -- a Vercel serverless function committing a
  posted settings dump straight to data/processed/dev-panel-settings.json
  via GitHub's Contents API, adapted directly from CLICKO's already-
  working version (same GITHUB_TOKEN/DEV_PANEL_SAVE_SECRET env-var
  contract, same GITHUB_REPO/GITHUB_BRANCH/SETTINGS_FILE_PATH override
  defaults, just repointed at LeisHo/DickoClicko).
- Save/Reset are now a 3-tier fallback chain instead of 2:
  1. Vercel/GitHub API (new) -- works from ANY device, including a phone
     with no filesystem access, which the existing File System Access
     tier structurally can't cover. Reads are a plain fetch of the
     already-committed static JSON file (works on any server, not just
     Vercel); writes need the actual serverless function.
  2. File System Access API (existing, unchanged) -- direct local disk
     write via a native picker, desktop-only.
  3. Local save prompt (new: downloadSettingsAsFile(), a real browser
     download of the JSON dump) + sessionStorage (existing) -- last
     resort when neither tier above reached the git-tracked log at all.
- Documented the required Vercel setup (GITHUB_TOKEN, DEV_PANEL_SAVE_
  SECRET) in README.md, mirroring CLICKO's own README section, including
  that the client-side DEV_PANEL_SAVE_SECRET value is the SAME one
  CLICKO already uses (treated as one workspace-wide shared anti-abuse
  token per CLAUDE.md's wording, not a per-project one).

Investigated before writing any code (per §0a -- this would have been a
material architectural change if built wrong): confirmed DICKOCLICKO had
no api/, vercel.json, or .vercel dir at all (a plain static repo whose
README explicitly says "no build step"), read CLICKO's already-working
api/save-settings.js as the pattern to follow, and verified the GOTHOT
token in keyps.txt genuinely has read access to LeisHo/DickoClicko via a
direct GitHub API probe (safe, read-only) before relying on it in the
new function. Also checked GitHub's own commit history for CLICKO's
settings file via its public API (avoiding a blocked cross-project git
command) and found only ONE commit there, authored by the human user
directly -- proving CLICKO's own Vercel function has never actually
fired for a real write, meaning the "set the env vars up in Vercel"
step is still outstanding there too, not just theoretically possible.
Surfaced this fork to the user via AskUserQuestion before building
anything, rather than guessing.

Verified:
- node --check clean on both index.html and the new api/save-settings.js
  (worked around a mid-task Bash-classifier block on the usual `node -e`
  extraction one-liner by writing the extraction script to a real file
  first instead -- same result, different invocation).
- Live-tested the full 3-tier chain via a temporary debug hook
  (window.__debugTMPropo, removed before commit, grep-confirmed 0
  references) against a standalone static file server: boot-time Reset
  correctly loaded the REAL committed settings via the new Tier 1 static-
  file fetch (confirmed against the actual data/processed/dev-panel-
  settings.json already in the repo, not a mock); writeSettingsViaApi()
  correctly returned false when POSTing to a plain static server lacking
  the real Vercel function (confirmed via a direct curl probe first: the
  static server returns 501 for POST, not 2xx); a real saveSettings()
  call correctly cascaded through all 3 tiers to the session-only
  fallback (Tier 2's native picker can't fire without a live user
  gesture in this headless environment) with the marker value correctly
  round-tripped into sessionStorage; and a subsequent Reset correctly
  preferred Tier 1's real committed value over the Tier 3 session marker,
  confirming the priority ordering behaves exactly as designed.
- Could NOT verify: an actual live POST through a real deployed Vercel
  function (no Vercel project exists for this repo yet -- see below).

NOT done, and cannot be done from this environment: actually creating a
Vercel project for this repo and setting GITHUB_TOKEN/DEV_PANEL_SAVE_
SECRET as real Vercel environment variables. This requires the user's
own Vercel account access, which this session doesn't have. Until that's
done, Tier 1 writes will always fail (falling through to Tier 2/3
exactly as designed, not a bug) -- see README.md's new "Dev panel
settings sync" section for the exact setup steps.

--------------------------------------------------------------------------------
2026-09-02  -  11:10 AM - 11:11 AM EDT

Fixed: user supplied the workspace's canonical §12l reference
implementation (already built and debugged on another project this
session) as a spec to reconcile DICKOCLICKO's own api/save-settings.js
against. Diffed line-by-line: every piece already matched except the
missing-env-var error message, which reported a vague "missing
GITHUB_TOKEN or DEV_PANEL_SAVE_SECRET" regardless of which ONE was
actually absent -- the reference spec explicitly calls out reporting
exactly which var is missing as "made debugging much faster." Fixed by
building a `missing` array and joining it into the error message, same
pattern as the reference. Re-confirmed the other 2 architecture pieces
(the git-tracked seed file, the client's fetch('/api/save-settings')
call with the X-Dev-Panel-Secret header) were already in place from the
prior round rather than assuming. node --check clean. Only the 1 line
of real logic change; everything else in the function (GET-for-sha,
PUT-to-commit, response shapes) was already an exact match, confirmed by
direct comparison rather than a full rewrite.

--------------------------------------------------------------------------------
2026-09-02  -  11:31 AM - 11:36 AM EDT

Applied §12m's "set defaults" merge workflow to a user-pasted Copy
Settings dump (Circle Size/Color, Rope Thickness/Length, Bend Stiffness,
Rope Animation Speed/Gravity/Weight/Damping/Constraint Iterations,
Minimum Rope Length, Circle Cut Distance, Double Click Threshold, Rope
Fall Speed/Cut Speed, Background Color -- 19 of 35 values changed from
the prior defaults) plus a group/row reorder (UI moved from last to
right after the built-in Dev Panel group; Bend Stiffness moved to first
within Rope Animation, ahead of Rope Animation Speed). Computed the full
O/G/P merge table per-field: O (hardcoded index.html defaults) vs G (the
git-tracked settings log, found stale -- it predated Bend Stiffness
entirely, and several fields like Minimum Rope Length/Rope Fall Speed/
Double Click Threshold had already drifted from O via untracked local
tuning) vs P (the pasted dump) -- every single field resolved to P
either because P deliberately differed from O, or because P already
matched G, so the merged result equals P verbatim for both the values
and the order. Updated both required locations per this project's
established precedent: DEV_GROUPS' `def:` values/order in index.html
(the fallback baseline for a fresh visitor with nothing else reachable
yet) AND data/processed/dev-panel-settings.json (the git-tracked log
itself, which Tier 1's boot-time fetch reads first per the 3-tier
architecture 2 rounds ago). panelGeometry.desktop position/size updated
to the pasted dump's value directly (no hardcoded-JS equivalent exists
for panel position, so no 3-way merge needed there); panelGeometry.style
(Dev Panel appearance) and panelGeometry.mobile were already identical
across O/G/P, untouched.

Verified:
- node --check clean.
- JSON.parse confirmed the rewritten settings log is valid.
- Live-tested via a temporary debug hook (window.__debugTMPropo, removed
  before commit, grep-confirmed 0 references) against a standalone
  static file server: the rendered dev panel's group/row order matched
  the merged order exactly (UI second, Bend Stiffness first in Rope
  Animation, live DOM text extraction); a real boot cycle (Tier 1 fetch
  of the rewritten settings log, not a mock) produced a `cfg` object
  matching all 35 expected merged values exactly, 0 mismatches, 0 extra
  keys.

--------------------------------------------------------------------------------
2026-09-02  -  12:45 PM - 1:11 PM EDT

Fixed the real cause of a still-reported "rope extension is still
jitter" bug, per a user-supplied screen recording showing the repro:
extend from startup, cut the rope, let it settle, click-and-hold to
extend again. Diagnosed methodically rather than guessed at:

- Extracted and montaged frames from the recording (Python + OpenCV/
  PIL, since ffmpeg isn't available in this environment) -- confirmed
  the video showed real, severe self-knotting during the SECOND
  extension (chaotic tight coiling within a few seconds of holding),
  not subtle jitter.
- A direct point-coordinate dump from a synthetic, zero-perturbation
  straight-down growth test showed Y-coordinates going non-monotonic
  (a real fold baked in), which led to 2 real, independently-confirmed
  bugs -- both fixed, but investigation showed NEITHER was the actual
  cause of the reported tangle:
  - integrateChain()'s new bending constraint (from 2 rounds ago) read
    the kinematically-positioned growing tip as a neighbor on its last
    loop iteration, closing a feedback loop with positionGrowingTip()
    (which repositions the tip FROM that same neighbor's position every
    frame). Fixed by excluding the tip from the bend loop while a
    partial segment is growing, matching the distance constraint's own
    existing skipLastSegment handling.
  - tipGrowDirection() used a raw, unsmoothed per-frame direction --
    prev/beforePrev are real solver-participating points with ordinary
    per-frame "breathing" noise, and a single noisy frame at the exact
    instant a segment committed could bake a wrong angle in as a
    PERMANENT kink. Fixed by exponentially smoothing the direction into
    a new, persisted mainRope.growDir (reset on cut and on a fresh
    rope), so single-frame noise gets filtered before it can be
    committed.
- Confirmed neither fix above was the actual cause by reproducing the
  tangle with cfg.bendStiffness forced to 0 -- identical divergence.
- Isolated the real cause via a clean A/B: a static, non-growing
  67-point chain was PERFECTLY stable under gravity with the floor
  disabled (5 simulated seconds, 0 chain-order violations), but severely
  unstable with the floor enabled once long enough to reach it --
  pinning the floor-collision block in update() as the true root cause,
  unrelated to growth, bending, or direction noise. Further isolation
  found pileRepulsion() specifically: it has no concept of chain
  adjacency, so once points pile near the floor it repels ANY two of
  them closer than minSep, including non-adjacent points from the SAME
  chain -- fighting the distance constraint faster than
  cfg.constraintIterations (few, by default) can resolve within a
  frame.
- 2 other fixes were tried and rejected before removing the
  pileRepulsion() call: softening the floor clamp from a hard snap to a
  partial correction (5 rates tested, none reliably helped, worst case
  got WORSE at low rates); re-solving constraints a second time on
  floor-contact frames (measurably worse -- re-running integrateChain()
  re-triggers its own velocity-integration step, amplifying rather than
  resolving the correction, worst ratio hit 16x).

Verified: node --check clean at every step. Live-tested via a temporary
debug hook (window.__debugTMPropo, removed before commit, grep-confirmed
0 references) against a standalone static file server, driving update()
directly with a fixed 1/60s timestep (this pane's own rAF loop proved
unreliable to depend on across separate tool calls -- confirmed directly
by seeing inconsistent, non-reproducible results before switching to
this method) for up to 1800 simulated frames (30s, longer than the
user's own ~24.5s recording) of the exact repro (cut short, then
hold-to-grow). Final result: max segment-length ratio never exceeded
1.201 and converged to a constant 1.074 by ~10s in, vs. the unfixed
version's unbounded climb past 16x and still rising at 30s. Known,
disclosed tradeoff: a heavily-piled rope may now show less visual
separation between overlapping coils than pileRepulsion() was meant to
provide -- a proper fix would make it chain-adjacency-aware rather than
removing it outright; flagged as a follow-up if that becomes its own
complaint.

--------------------------------------------------------------------------------
2026-09-02  -  1:14 PM - 1:20 PM EDT

Added, per explicit request: "Fall Delay" slider (ROPE CUT group,
0-3s, default 0) -- a cut-off rope segment now stays completely frozen
(no gravity, no constraint solving) for this many seconds after being
cut, before falling normally. Captured onto the piece object at cut
time (cutRopeAt()/cutPieceAt()) and counted down in update() against
`dt` (respects Rope Animation Speed); the piece's own toppling tilt and
cut-sweep mark are unaffected, both still play out immediately/on their
own timeline regardless of the delay.

Fixed, per direct report ("the last 20px end seems to be a stiff
piece... entire rope should act like a rope without any stiffening"):
strokeRopeCurve() always rendered its final segment as a straight
ctx.lineTo() while every other segment used a smooth quadratic curve --
a pure rendering artifact (confirmed nothing in the physics singles out
the tip's segment for extra rigidity outside active growth), present
regardless of any dev-panel setting, spanning roughly one segment
length (~20-25px at default Rope Thickness) -- an exact match for the
report. Fixed by curving the final segment through to the actual tip
coordinate instead of stopping at a midpoint and lineTo-ing the rest.

Verified: node --check clean. Live-tested via a temporary debug hook
(window.__debugTMPropo, removed before commit, grep-confirmed 0
references): a synthetic piece with fallDelay=0.5 stayed at its exact
starting Y for the first ~30 frames (0.5s at 60fps), then began falling
immediately once the countdown hit 0 (Y rising 103->151->255->396 over
the next 30 frames, correct accelerating-under-gravity shape); render()
ran clean with no errors for both a 2-point piece (exercising the
new n===2 straight-line branch) and an 8-point piece (exercising the
curve-to-tip branch).

Also clarified via direct user testing, no code change needed:
double-clicking within the circle bounds cuts the rope down to Minimum
Rope Length -- expected given Circle Cut Distance's current default (0
as of the last "set defaults" bake, since raised back up in a later
dump); click-and-hold within the circle bounds correctly triggers
hold-to-grow (initially misreported as doing nothing, then corrected by
the user in the same turn).

--------------------------------------------------------------------------------
2026-09-02  -  1:24 PM EDT

Applied §12m's "set defaults" merge to a fresh pasted Copy Settings dump
-- 8 of 35 values changed from the prior defaults (Rope Length, Gravity
Strength, Rope Weight, Intensity Ceiling, Minimum Rope Length, Circle
Cut Distance, Rope Fall Speed, Cut Speed). O and G were identical for
every field this round, so every changed field resolved directly to the
pasted value P. The new "fallDelay" field (added this same session,
predating this paste) wasn't in P at all -- kept at its current default
(0) rather than touched by the merge, and inserted into the
git-tracked log's ROPE CUT order between Rope Fall Speed and Cut Speed,
matching its position in DEV_GROUPS. Updated both DEV_GROUPS' def:
values in index.html and data/processed/dev-panel-settings.json.
Verified live: all 9 relevant cfg values (8 changed + fallDelay)
matched exactly after a real boot-time load via a temporary debug hook
(window.__debugTMPropo, removed before commit, grep-confirmed 0
references). node --check clean.

--------------------------------------------------------------------------------
2026-09-02  -  1:36 PM - 1:42 PM EDT

Fixed a real bug reported directly: the user confirmed a Vercel
deployment WAS created with GITHUB_TOKEN/DEV_PANEL_SAVE_SECRET properly
set, yet hitting Save Settings there still triggered the native
File-System-Access picker dialog -- exactly the "local save prompt on a
served deployment" behavior CLAUDE.md's §12d/§12l explicitly forbid.

Root cause: GIT_LOG_WRITABLE never actually checked hostname, only
protocol (`location.protocol !== 'file:'`) -- true for ANY non-file
origin, including a real deployed https:// domain, not just
localhost/127.0.0.1. A same-day earlier fix had added a DEV_MODE
requirement, but its own truth-table (see CODE_SUMMARY's Gotchas) only
flipped the "hosted origin, no ?dev=1" case -- which was never actually
reachable as a bug, since that combination hides the dev panel/Save
button entirely. The reachable case ("hosted origin WITH ?dev=1", the
only way to see Save on a real deployment at all) was left unfixed the
whole time.

Fixed with a new IS_LOCAL_CONTEXT constant (protocol === 'file:' OR
hostname is localhost/127.0.0.1) folded into GIT_LOG_WRITABLE, AND --
the other necessary half -- saveSettings() itself now checks
IS_LOCAL_CONTEXT after a Tier 1 (Vercel/GitHub API) failure and stops
there with an honest error, instead of falling through to Tier 2/3 at
all on a non-local origin. Also improved writeSettingsViaApi() to
return {ok, error} instead of a bare boolean, so a real Tier 1 failure
(missing env var, secret mismatch, a GitHub API error) is now visible
in both the Save button's flash text (held 4s for errors) and the
console -- needed since the user's live deployment is STILL failing
Tier 1 for an as-yet-undiagnosed reason; this should surface why on
their next attempt.

Verified: localhost regression-checked (IS_LOCAL_CONTEXT/
GIT_LOG_WRITABLE both still true, Save cascades through all 3 tiers
exactly as before); a throwaway test copy with IS_LOCAL_CONTEXT forced
false (simulating a real deployed hostname, since this environment has
no way to actually navigate to one) confirmed Save now stops
immediately after Tier 1's failure with an honest error message and
never attempts Tier 2/3 at all. node --check clean; temporary debug
hook (window.__debugTMPropo) removed before commit, grep-confirmed 0
references.

--------------------------------------------------------------------------------
2026-09-02  -  1:45 PM - 1:56 PM EDT

Three related changes to Fall Delay and rope-extension smoothness, all
per direct requests:

1. "The new rope ends should only appear the moment the cut segment
   begins to fall" -- the cut-end endcap (Endcap At Cut End, ROPE CUT
   group) is now hidden in render() while piece.fallDelay > 0, and
   appears the instant it hits 0. Purely a render-time gate; doesn't
   touch the delay countdown itself. A no-op with the default Fall
   Delay (0) -- the cap still appears immediately, same as before.

2. "If the rope is mid swing while it gets cut, the delayed cut segment
   will continue to swing with the main rope segment until its time to
   fall" -- a delayed piece was previously frozen motionless in empty
   space for the whole delay. Now tracks whatever it was cut from
   (mainRope for cutRopeAt(), or the upper remaining half of another
   piece for a piece-on-piece cutPieceAt()) via rigid translation: each
   frame, the piece shifts by exactly however far its "delayParent"'s
   own current tip moved that frame (both x/y AND oldx/oldy shift
   together, so no velocity is falsely injected once released).
   Translation only, no rotation -- the piece's own shape (set once at
   topplePiece() time) is unchanged during the delay, only its position.
   Once fallDelay hits 0 the piece decouples completely and falls under
   its own independent physics from that point on.

3. "When I trigger the extension while the rope is mid swing, it still
   extends in a jittery fashion" -- investigated as a DIFFERENT bug from
   the earlier pileRepulsion/floor-related tangling fix (reproduced with
   the floor disabled entirely, isolating it as swing+growth specific).
   Direct frame-by-frame acceleration tracing at the growing tip found
   real, periodic velocity-discontinuity spikes (up to ~50px/frame^2
   against a ~4px/frame^2 baseline) recurring almost exactly at the
   segment-commit interval. Root cause: growRope()'s commit always
   snapped the tip to the mathematically "complete" targetSeg-away
   position, but positionGrowingTip()'s own smoothing (added 2 rounds
   ago, deliberately lagging behind the raw target to filter solver
   noise) could still be meaningfully behind that exact position at the
   moment of commit -- closing that gap in one frame is what produced
   the spike. First attempt (finalizing the tip wherever it already
   visually was, instead of snapping) fixed the spikes but introduced a
   worse regression: without the ramp described below, segment lengths
   fell measurably short over many commits during a real swinging
   session (max ratio ~3.6x by 5s, worse than before). Final fix: ramp
   positionGrowingTip()'s own smoothing factor UP toward 1 as
   tipGrowLen approaches segLen (`Math.max(0.25, tipGrowLen/segLen)`),
   so the tip has essentially already caught up to its target the
   instant before commit -- growRope() then freezing it there (rather
   than snapping) loses none of the segment's real length. Both
   problems resolved together: verified via the same repro (punch to
   induce real swinging, then grow immediately) that max acceleration
   dropped from 50.27 to 9.93 (2 residual spikes vs 8 originally) AND
   max segment-length ratio over a full 5s test came out at 1.274 --
   BETTER than the original pre-any-fix baseline (1.774), not just
   better than the failed first attempt's 3.6-3.9.

Verified: node --check clean at every step. Live-tested via a temporary
debug hook (window.__debugTMPropo, removed before commit, grep-confirmed
0 references) against a standalone static server: a synthetic piece cut
from a mainRope with real swinging velocity tracked the parent's tip
exactly, frame by frame, for the entire delay, then correctly decoupled
and fell independently the instant fallDelay hit 0 (confirmed via
mainRope continuing to oscillate afterward while the released piece's
own points stayed fixed); the acceleration-spike and segment-length-
ratio metrics above were both measured directly, not estimated. No
console errors throughout.

--------------------------------------------------------------------------------
2026-09-02  -  2:48 PM - 3:13 PM EDT

Three related reports, all addressed in one pass:

1. "Collision issues with cutting while the rope is swinging -- why don't
   you initiate the actual separation of rope segments after the fall
   delay" -- the PREVIOUS Fall Delay implementation (prior entry) split
   mainRope's points array into two independent chains immediately at cut
   time, then rigidly translated the falling piece to follow the parent's
   tip each frame during the delay. That translate-to-follow approach
   doesn't reproduce true continuity: a shortened parent rope swings
   differently than the original undivided rope did, so the piece's
   translated position could visibly diverge/collide with where it
   "should" be. Redesigned: cutRopeAt()/cutPieceAt() now defer the actual
   array split via a `pendingCut` ({idx, delay}) stored on mainRope or the
   piece being cut -- the rope stays ONE physics chain, fully solved by
   the normal integrateChain() call, until the delay counts down to 0 in
   update(), at which point the real split (performMainRopeSplit()/
   performPieceSplit()) happens using that frame's already-integrated
   positions. Toppling (the one-time random tumble) now happens at the
   real separation instant too, which is also the only instant it makes
   physical sense (a still-attached piece has no reason to tumble). The
   cosmetic cut-sweep mark still starts immediately at cut time (unchanged,
   purely visual) via a new cutSweep.anchorIndex field so it tracks the
   pending cut point correctly whether or not the real split has happened
   yet. A second cut attempt while one is already pending is rejected.
   Continuity across the eventual split is now correct BY CONSTRUCTION:
   the piece's cloned starting point is an exact copy of the shared
   boundary point's values at split time, so there is no discontinuity to
   produce a collision artifact in the first place.

2. "Cut-end endcap hidden in render() while piece.fallDelay > 0 -- this
   doesn't look implemented" -- the gate itself was correctly wired (code
   inspection confirmed it), but with change #1 above it's now moot: a
   piece can no longer exist in fallenPieces while still delayed (it isn't
   created until the real split happens, which only happens once the delay
   has fully elapsed), so the per-frame fallDelay check in render() was
   dead code and has been removed -- endcapAtCutEnd alone now fully
   controls whether the cap appears, with no additional timing gate needed.

3. "Extension jitter is much better but still noticeable" -- root-caused
   to TWO separate, previously-unaddressed bugs in the growing-tip code,
   found via frame-by-frame tracing of the angle between the newly-
   committed segment and the brand-new growing segment (not just tip
   position/acceleration, which the prior fix already covered):
   (a) integrateChain()'s basic per-point Verlet step excluded only the
   pinned anchor, not the growing tip -- so the fresh, near-zero-length
   tip pushed by growRope() still got a real gravity+inherited-velocity
   kick on its first frame, which positionGrowingTip()'s smoothing then
   only partially corrected. Fixed by also excluding the growing-tip index
   from that loop when skipLastSegment is set, matching what the function
   already claimed to do. This alone reduced the worst-case kink from
   ~180 degrees to ~162 -- better, not fixed.
   (b) The real cause: positionGrowingTip() smoothed the tip's ABSOLUTE
   position toward a target, but the point it's anchored to (dir.prev, the
   just-finalized neighbor) can itself move several px during a commit
   frame (it inherits the outgoing segment's velocity, then gets
   constraint-solved as a real point for the first time). The tip's own
   smoothing, working from its stale pre-move position at a 0.25 factor,
   barely followed that move, leaving the tip's new segment pointing in a
   near-arbitrary direction relative to its anchor. Since drawEndcap()
   orients the endcap graphic directly off this exact segment's direction,
   that reads as the endcap visibly snapping/spinning at the tip on every
   commit during real swinging. Fixed by restructuring positionGrowingTip()
   to smooth a relative OFFSET-from-anchor (mainRope.tipOffset, reset to
   {0,0} by growRope() every time a new tip is pushed) instead of an
   absolute position -- the anchor's own current position is now always
   applied in full, every frame, with zero lag, while only the small
   growing offset itself is smoothed.

Verified via a temporary debug hook (window.__debugTMPropo, removed before
commit, grep-confirmed 0 references) against a standalone static server:
- Change #1: default Fall Delay (0) reproduces the old immediate-split
  behavior exactly (regression check); with Fall Delay > 0, the rope
  measurably stays undivided and tracks a real swing continuously through
  the whole delay (max per-frame jump at the pending cut point ~4.25px,
  matching the applied swing velocity, no discontinuity); a second cut
  attempt mid-delay is correctly rejected; the real split fires at the
  correct countdown boundary and mainRope/piece continuity across it was
  confirmed by object-identity argument (the piece's first point is a
  direct clone of the boundary point's own values at that instant).
- Change #3: the angle-kink metric (degrees between consecutive segment
  directions at the tip, frame to frame) dropped from a worst case of
  179.89 (before any fix this round) to 5.57 over a 2s swinging+growing
  test with the user's own saved settings (bendStiffness 0.07,
  ropeAnimSpeed 1.1, gravityStrength 1.9, ropeWeight 1.9, damping 0.999,
  constraintIterations 6, ropeGrowthRate 15), average kink dropped from
  18.33 to 0.42; tip acceleration and segment-length-ratio stability (max
  ~1.1x) both remained healthy, confirming no regression against last
  round's fixes. A combined 5s stress test (stronger swing + continuous
  growth + a mid-test cut with Fall Delay 0.6s) held max segment ratio
  1.132, max kink 9.30/avg 0.66, and completed the deferred cut/split
  correctly alongside the growth -- all three of today's fixes verified
  working together with no interaction bugs.
- Real-video analysis: the user's own new screencap (datalog/Recording
  2026-09-02 144811.mp4) was frame-extracted and inspected around its
  actual cut (~9.0-9.9s) and growth (~10.3s onward) phases to ground the
  investigation in what the video actually showed, rather than relying on
  the physics simulation alone.

node --check clean throughout. No console errors on a real page load
(only a stale, unrelated ERR_CONNECTION_RESET from an earlier redundant
reload attempt, not from index.html or any of its own resources).

--------------------------------------------------------------------------------
2026-09-02  -  5:14 PM - 5:23 PM EDT

"I removed the End svgs and added a whole set of new ones. Incorporate all
the new End SVGs" -- the user had removed data/Rope/End1.svg through
End4.svg and added a new 12-file set: data/Rope/End_Form1-02.svg through
-06.svg plus End_Form1-C.svg (a "Form 1" family), and End_Form2-07.svg
through -11.svg plus End_Form2-C.svg (a "Form 2" family). Read all 12 new
SVGs directly and extracted each one's <path> `d` attribute verbatim into
a new ENDCAP_DESIGNS entry, keyed by the lowercased file suffix
('form1-02' ... 'form1-c', 'form2-07' ... 'form2-c') -- replacing the
previous end1-end4 keys entirely, per the existing architecture's own
documented rule ("a new design gets only a `path` entry", CLAUDE.md
Gotchas). Confirmed ENDCAP_ALIGNMENT itself needed no change: every one of
the 12 new SVGs embeds the exact same reference <line> (41.57,80.26 to
96.06,80.26) as data/Rope/End Alignment.svg, so they're already authored
against the shared frame drawEndcap()'s transform math expects. Updated
the Endcap Design dropdown's 13 options (None + the 12 new designs,
labeled "Form 1 - 02" etc.) and moved its default from the now-deleted
'end3' to 'form1-c' (both the DEV_GROUPS hardcoded default and
data/processed/dev-panel-settings.json's git-tracked default -- the old
value would have silently no-op'd via drawEndcap()'s `if (!design)
return;` guard rather than crashing, so this needed an explicit fix, not
just leaving it to fail loudly). Verified via a temporary debug hook
(window.__debugTMPropo, removed before commit, grep-confirmed 0
references): requestAnimationFrame doesn't fire in this sandbox's Browser
pane (confirmed via a blank/never-drawn canvas check -- the pane reports
itself as not visually displayed/composited, which Chromium throttles rAF
for), so canvas verification required manually forcing update()+render()
calls rather than relying on the app's own loop. All 12 designs rendered
without throwing, each producing a distinct, non-zero rope-colored pixel
count (sampled across the full canvas) confirming real, differing
geometry rather than a silently-empty path; 'none' mode also verified
still working; a fresh page load confirmed cfg.endcapDesign boots to the
new 'form1-c' default from the git-tracked settings file. Committed
alongside the user's own already-made SVG file changes (4 deletions + 12
additions) rather than leaving them uncommitted -- the code and the
assets it depends on ship together. node --check clean. data/Rope/
Untitled-1.ai (the evident Illustrator source for this design set) and
datalog/ left untouched, per this session's standing practice of not
touching another concurrent session's own working files.

--------------------------------------------------------------------------------
2026-09-02  -  6:11 PM - 6:16 PM EDT

Three explicit, previously-queued requests actioned together (each had
been deferred by direct instruction until "next prompt" -- see the
conversation, not a scope-creep decision made here):

1. Removed the `Rope Weight` dev-panel setting entirely. It and
   `Gravity Strength` were pure multiplicative factors on the same
   acceleration term (`gravityAccel = vh(90) * gravityStrength *
   ropeWeight`, used for both mainRope and fallen pieces) -- scaling
   either one had an identical visible effect, so the setting was
   genuinely redundant. Rather than just deleting it (which would have
   silently HALVED the rope's actual gravity, since the product would no
   longer be computed), folded its value into Gravity Strength's own
   default: new default = old Gravity Strength x old Rope Weight (1.9 x
   1.9 = 3.61, already within Gravity Strength's existing 0-5 range) --
   applied to both the DEV_GROUPS hardcoded default and
   data/processed/dev-panel-settings.json's git-tracked default, so the
   rope's actual on-screen behavior is unchanged, not just similar.
2. Moved the Damping slider's minimum from 0.5 to 0.7. The git-tracked
   default (0.999) was already comfortably above the new floor, so
   nothing needed clamping.
3. Updated 4 of the 12 endcap SVGs with new path data from the user's
   own revised source files (found via `git status` on data/Rope/, not
   announced individually): End_Form1-06.svg, End_Form2-09.svg,
   End_Form2-10.svg, End_Form2-11.svg. Same ENDCAP_DESIGNS keys, just new
   `d` path strings -- no other code changes needed since each design's
   identity (dropdown key/label) is unchanged.

Verified: a temporary debug hook (window.__debugTMPropo, removed before
commit, grep-confirmed 0 references) confirmed the Rope Weight row is
gone from the live dev panel, Damping's range.min reads "0.7", Gravity
Strength boots to 3.61, and all 4 updated endcap designs render without
error with real (non-zero, mutually distinct) rope-colored pixel counts
via a forced update()+render() pass (this sandbox's Browser pane still
doesn't composite frames, so requestAnimationFrame doesn't fire here --
same limitation as the original 12-design swap). node --check clean;
settings JSON validated with a real JSON parser (not just eyeballed); 0
console errors on a fresh page load.

--------------------------------------------------------------------------------
2026-09-02  -  6:26 PM - 6:29 PM EDT

"I added 2 new SVGs. incorporate and push" -- found data/Rope/
End_Form3-01.svg and End_Form3-02.svg (a new "Form 3" family, first of
its kind) via `git status`. Added both to ENDCAP_DESIGNS ('form3-01',
'form3-02') and the Endcap Design dropdown (2 new options appended after
Form 2 - C, total now 15: None + 14 designs). Verified via a temporary
debug hook (removed before commit, grep-confirmed 0 references): both
render without error with distinct non-zero rope-colored pixel counts.
Also checked each design's actual bounding-box top edge against the
ENDCAP_ALIGNMENT reference line (80.26) directly, same as the original
12-design verification -- found a real, worth-noting difference from
every prior design: both Form 3 shapes' top edge sits ~3.93 SVG units
ABOVE the alignment line (offset -3.926), not flush with it like all 12
Form1/Form2 designs were. At default Rope Thickness this scales to only
~2-3px of overlap into the rope's own rendered stroke, comfortably inside
the rope's round line-cap radius (~19px), so it should still blend
seamlessly rather than showing a seam -- reported to the user as a
factual observation, not silently smoothed over or treated as a defect
to fix, since it may simply be this design's intentional shoulder shape.
node --check clean; 0 console errors.

--------------------------------------------------------------------------------
2026-09-02  -  6:57 PM - 7:30 PM EDT

A large bundled turn covering 6 explicit requests, all approved to proceed
together ("do the other stuff and push"), plus one exploratory question
answered without implementing (per its own "how difficult would it be"
framing):

1. Dev-panel group collapse/expand state is now captured and restored by
   Save/Copy/Reset -- it wasn't before, despite the workspace's own §12e
   spec requiring it (confirmed via direct code reading when asked, in
   the prior turn). getPanelOrder() now records `collapsed:
   g.classList.contains('collapsed')` per group; applyOrder() re-applies
   it. Verified: collapsing 2 groups, capturing the order, expanding
   everything back, then re-applying the captured order correctly
   re-collapsed exactly those 2 groups.

2. Removed Fall Delay as a standalone setting. Per explicit request
   ("set the fall delay as the time it takes the cut line to cut through
   the entire rope"), the pendingCut deferral (from 2 rounds ago) now
   always uses `cutThroughDuration()` -- CUT_SWEEP_BASE_SEC / cfg.cutSpeed,
   the exact inverse of how cutSweep.progress itself accumulates -- so
   the piece-release delay can never drift out of sync with what the
   sweep mark is visually showing. Since this duration is always > 0, the
   old "instant split, no delay" code path in cutRopeAt()/cutPieceAt()
   no longer exists at all -- a real simplification. Removed from the
   dev panel and from data/processed/dev-panel-settings.json.

3. Fixed a real, reproduced bug: double-clicking near (not literally on)
   the circle could cut the rope down to only 2-3 segments regardless of
   its total length. Did NOT just take the report at face value --
   pulled the user's own live-saved settings from the git repo
   (circleCutDistance 9.5, circleSize 8) via `git fetch`/`git show
   origin/master:...`, then reproduced it via direct, timing-controlled
   simulation. Hit a real environment issue along the way: this
   sandbox's setTimeout is throttled by ~15-1000x when the tab isn't the
   foreground tab (a requested 60ms gap between two simulated clicks
   measured as ~1000-1850ms of real elapsed time) -- worked around by
   stubbing performance.now() with a fake, manually-incremented clock and
   calling onPointerDown/onPointerUp directly, rather than relying on
   real setTimeout delays between simulated clicks. Root cause: Circle
   Cut Distance (protects the anchor from being cut too close to) and
   isOnCircle()'s own click-exclusion radius are two independent
   thresholds that can drift apart -- with the project's OLD default
   Circle Cut Distance (5%vmin=40px) sitting below isOnCircle's exclusion
   radius (64px), rope-point index 3 (83.1px from anchor) clears the
   (too-small) cut guard while index 2 (55.4px) is still unreachable
   (inside the exclusion zone) -- so a click just past the circle chops a
   potentially-hundreds-of-px rope down to 3 segments. Verified via
   simulation at 6 click offsets (0/16/31.7/63.4/64.6/83.2px from
   anchor): only 83.2px scheduled a cut before the fix. Fixed with a
   shared circleExclusionRadius() function (extracted from isOnCircle()'s
   own inline calc so the two can't independently drift again) and a
   Math.max(circleCutDistance, circleExclusionRadius()) floor in
   cutRopeAt()'s guard. Honestly disclosed a residual gap rather than
   overstating the fix: with the user's OWN live Circle Cut Distance
   (9.5%vmin=76px, already above the 64px exclusion radius), this fix
   changes nothing for their current config -- the same 83.2px/index-3
   reproduction still schedules a cut, since 83.1px already clears their
   own 76px threshold. That residual case comes from segment-length
   granularity (27.7px at default growth), a genuinely different,
   smaller problem than the one just fixed -- flagged in
   docs/PROJECT_PROGRESS.md's Open Questions rather than silently
   left unmentioned.

4. Rope End Curve Arc's minimum (0) now actually renders a flat/straight
   end, per explicit request. strokeRopeCurve()'s ctx.lineCap was
   unconditionally 'round' before -- the canvas stroke's own inherent,
   non-adjustable round cap kept rounding the tip even at Rope End Curve
   Arc 0, since that setting only ever gated a SEPARATE filled arc circle
   drawn past the stroke, never the stroke's own cap. Now lineCap is
   always 'butt', and all roundedness comes from that same filled-arc
   mechanism -- now drawable independently at BOTH ends of the path
   (strokeRopeCurve's new optional startArcMult param), needed (not just
   tidier) because a fallen piece's two ends can require different
   treatment at the same time once End Emerge (below) is active, which
   one shared lineCap could never express.

5. New feature: End Emerge (END EMERGE dev-panel group -- Enabled, Delay,
   Speed, Tweening: Linear/Ease In/Ease Out/Ease In-Out). When enabled, a
   freshly-cut edge's endcap doesn't appear at full size the instant the
   real split happens -- it starts scaled down so its FULL bounding-box
   width (not just its neck) matches the rope's own thickness, i.e.
   visually indistinguishable from ("completely obscured by") the rope's
   own end, waits the configured delay, then scales up to its real size
   over the configured speed's own duration, eased by the selected
   tweening curve. Applies independently to BOTH halves of every cut: the
   remaining rope/piece's own tip (tipEmerge) and the newly-split-off
   piece's own cut edge (cutEdgeEmerge) -- a piece carries both fields,
   since it can later become the "upper" half of a further cutPieceAt()
   split. Added ENDCAP_WIDTHS: each design's TRUE bounding-box width
   (wider than ENDCAP_ALIGNMENT's neck-only measurement), computed ONCE
   at startup from each design's raw `d` string (now kept alongside its
   Path2D in ENDCAP_DESIGNS) via a throwaway real SVG <path> element's
   getBBox() -- deliberately NOT hand-measured/hardcoded, so it stays
   correct automatically no matter how many more times the source SVGs
   get swapped. Verified via direct frame-by-frame simulation (a fast 0.1s
   delay + 2x speed, for a short observable timeline): the emerge factor
   read exactly 0 for the first ~6 frames (matching the 0.1s delay at
   60fps), then ramped smoothly to 1.0 over the next ~10-11 frames
   (matching EMERGE_BASE_SEC/speed = 0.175s), then held at 1 indefinitely.
   A parallel run with the feature OFF confirmed zero behavior change --
   tipEmerge stays null, emergeFactor reads 1 immediately at the same
   split frame. A full 40-frame render() pass covering both a plain cut
   and a subsequent piece-on-piece cut threw no errors.

6. Answered (without implementing, since it was framed as a difficulty
   question, not a build request): progressive cut-falling -- the cut-off
   segment sagging/falling from the cut side while still attached by a
   thinning uncut strip, snapping fully free only once the cut sweep
   completes. Assessed as medium difficulty: weaken (not remove) the
   distance constraint at exactly the cut segment as cutSweep.progress
   advances, reusing the existing constraint-solver code with a
   per-segment stiffness ramp -- flagged that this reintroduces some of
   the "two loosely-connected things swinging together" complexity the
   pendingCut redesign (2 rounds ago) specifically avoided, so it would
   need its own careful swing-testing if built.

Verified throughout via a temporary debug hook (window.__debugTMPropo,
removed before commit, grep-confirmed 0 references) against a standalone
static server. node --check clean at every checkpoint; settings JSON
validated with a real parser. 0 console errors on a real page load (one
unrelated stale ERR_CONNECTION_RESET from an earlier redundant reload
attempt). Rebased/pulled cleanly against 3 concurrent live-Save commits
that landed on the remote mid-turn (the user's own deployment saving
settings) before editing data/processed/dev-panel-settings.json, so the
edits apply on top of their current real values, not a stale local copy.

--------------------------------------------------------------------------------
2026-09-02  -  7:51 PM - 7:56 PM EDT

Added a "Rope Top Curve Arch" setting (*D* shorthand), mirroring the
existing Rope End Curve Arc but for mainRope.points[0] (the anchor),
which previously always rendered as a flat line regardless of any
setting -- strokeRopeCurve()'s startArcMult (added last round for pieces'
own cut edges) was simply never wired up for mainRope's own call, which
always passed 0 there. Checked the actual claim in a comment from last
round before trusting it -- "that end always renders under the circle
graphic so its cap style is never actually visible" was WRONG: render()
draws the circle FIRST and the rope stroke AFTER (on top of it), and
ropeColor/circleColor differ (brown vs black by default), so the anchor
end is genuinely visible and a curved cap there is a real, worthwhile
addition, not a no-op. New default (1, matching Rope End Curve Arc's own
default) makes the change immediately visible per the request's own
phrasing ("make the top end curved instead of a straight line"), not
just a dormant slider defaulting to the old flat look. Verified via a
temporary debug hook (removed before commit, grep-confirmed 0
references): monkey-patched ctx.arc to record every call during a real
render() pass -- confirmed exactly 2 arc calls (the circle graphic itself,
r=32px, and the new rope-top arc, r=19.2px, both centered at the anchor,
matching thickness/2 * ropeTopCurveArc(1) = 19.2 exactly) and zero arc
calls at the tip (correctly suppressed there, since ropeEndCurveArc is 0
in the live settings and an endcap design is active). Regression-checked
the existing Rope End Curve Arc mechanism separately (endcapDesign
'none', ropeEndCurveArc 2): still draws correctly at the tip
(r=38.4=thickness*2), confirming the new setting didn't disturb the
existing one it's modeled on. node --check clean; settings JSON
validated with a real parser. Pulled 1 new live-Save commit from the
remote (a user Save that also captured collapsed:true on 4 dev-panel
groups -- direct, real-world confirmation the collapse-state feature from
2 rounds ago is genuinely working live) before editing the settings file,
same practice as every prior round this session.

--------------------------------------------------------------------------------
2026-09-02  -  8:16 PM - 8:36 PM EDT

Made the rope's base a free physics point instead of rigidly pinned to
the circle's center, per an explicit, detailed request: it should rest
at the bottom of the circle under gravity by default, and be pushed
around by the rope's own momentum -- including, on a hard enough flick,
rising to hit and bounce off the far side of the circle before settling
back at the bottom.

Removed update()'s `mainRope.points[0].x = circleAnchor().x; ...y = ...;`
hard pin. integrateChain()'s pinnedIndex for mainRope is now -1 (no pin
at all), matching how fallenPieces already integrate -- so the anchor now
gets gravity, velocity, and the same distance/bend constraints as every
other point, letting the rope's own swinging motion genuinely transmit
force to it via segment(0,1), exactly as described ("when the rope gets
flicked, the rope start will also feel the effects of that momentum").

Added the circular confinement in two separate pieces, after finding a
real bug in the first, combined attempt: integrateChain() gained an
optional boundaryConstraint param ({index, center, radius}) that clamps
a point's POSITION back inside a circle, applied once per solve
iteration alongside the existing distance/bend constraints. Separately,
a new constrainAnchorToCircle() (called once per frame after
integrateChain()) handles only the BOUNCE -- reflecting the point's
implied velocity off the boundary normal (v' = v - (1+restitution)*(v.n)*n,
restitution 0.4, hardcoded matching the floor collision's own hardcoded
0.3) -- touching only oldx/oldy, never position, and only when the point
is genuinely still moving outward at the boundary.

Why two pieces, not one: the first implementation did the position-clamp
and the bounce together in a single post-pass (same shape as the floor
collision). Verified via direct simulation (a 5-round alternating gentle
flick test, at the user's own live Constraint Iterations of 3) that this
produced a REAL, PERSISTENT instability, not a transient stress
artifact -- segment(0,1)'s length ratio sat at ~1.4-1.7x and never
recovered even after 30 full simulated seconds of otherwise undisturbed
settling (confirmed flat via a 30s timeline sample), while every other
segment settled near a healthy ~1.0-1.1x. Root cause: unconditionally
snapping the point to an exact boundary position every frame discards
whatever the distance constraint had just computed for it that same
frame -- and unlike the floor collision (which only clamps ONE axis, y,
leaving x fully free for the distance constraint to satisfy segLen
through), a circular boundary has no spare axis; clamping position at
all necessarily fixes both x and y simultaneously, leaving the distance
constraint nothing left to negotiate with. Fixed by moving the
position-clamp into the SAME iterative loop the distance/bend
constraints already use, letting all three negotiate a mutually
acceptable position over multiple rounds instead of one unconditionally
overriding the other -- the same general lesson this project already
learned once before with pileRepulsion fighting the distance constraint,
applied to a new site.

After the fix, the same adversarial test's residual dropped to ~1.29x
and plateaus there (confirmed via the same 30s timeline) rather than the
earlier open-ended-looking elevation -- and the residual shrinks
monotonically as constraintIterations increases (3 -> 1.293, 6 -> 1.138,
10 -> 1.076, 20 -> 1.033), the exact same pattern Constraint Iterations
already produces everywhere else in the rope. Concluded this is the
same pre-existing, already-documented, already-user-tunable tradeoff
showing up at a site (the anchor) that never had to negotiate anything
before -- not a new class of bug -- so left the user's own live
Constraint Iterations (3) untouched rather than silently "fixing" it.

Verified the actual described scenario directly: a single hard, sustained
upward drag of the tip well above the circle (held 20 frames, then
released) showed the anchor rise cleanly from resting at the bottom
(dist=R=32px below center) up through the center and out the TOP
(dist=32px ABOVE center -- the exact opposite boundary), held there while
still forced, then fall back through center and resettle at the bottom
after a decaying oscillation -- precisely the described mechanism. Final
settled ratio for this exact scenario: 0.96 (healthy) -- the 1.29
residual is specific to rapid, repeated, uninterrupted re-stressing, not
normal single-flick usage. Growth and cutting both regression-checked
afterward (grew 5->14 points cleanly with the anchor correctly held at
the boundary throughout; a mid-rope cut produced a normal fallen piece,
no errors).

One real test-methodology trap hit and worked around mid-investigation,
worth recording since it wasted real time and produced a convincing false
signal: calling resetMainRope() again from a debug-hook test (after the
hook's own `mainRope` reference was already captured at page load)
silently reassigns the MODULE-LEVEL mainRope variable to a brand-new
object -- the debug hook's own reference still points to the old,
now-abandoned object, so every subsequent d.update() call correctly
simulated the REAL (new) rope while every readback via the hook read the
orphaned old one, which never appeared to move no matter how many frames
ran (looked exactly like "gravity isn't working at all," a much bigger
and scarier-looking bug than the real one). Confirmed as a test artifact,
not a real bug, by re-running the identical test via in-place mutation of
the existing points array instead (the pattern already established all
session), which immediately showed correct movement.

node --check clean at every checkpoint. Live-tested via a temporary debug
hook (window.__debugTMPropo, removed before commit, grep-confirmed 0
references) against a standalone static server throughout. 0 console
errors on a confirmed-fresh real page load (only the same stale,
unrelated ERR_CONNECTION_RESET entries accumulated from earlier
redundant reload attempts this session, not from index.html).

--------------------------------------------------------------------------------
2026-09-02  -  10:34 PM EDT

Anchor-physics tuning controls, plus an End Emerge visual redesign, both
from a 4-note *D* spec.

Added:
- New "ANCHOR PHYSICS" dev group (placed right after CIRCLE):
  - Circle Offset (circleBoundaryOffset, %vmin, -10..10, def 0) -- offsets
    the radius of the invisible boundary circle the rope's anchor point
    (index 0) is confined within, independent of the visible circle's own
    size.
  - Rope Start Bounce Intensity (anchorBounceIntensity, x, 0..1.5, def 0.4)
    -- now feeds the restitution coefficient in
    constrainAnchorToCircle()'s velocity-reflection formula directly
    (previously hardcoded).
  - Rope Start Weight (anchorWeight, x, 0..5, def 1) -- a per-point gravity
    multiplier applied only to the anchor point, via a new weightMult
    field on integrateChain()'s boundaryConstraint param, so the anchor
    can read as heavier/lighter than the rest of the chain even though
    it's no longer rigidly pinned.
- anchorBoundaryRadius() helper: vmin(circleSize)/2 + vmin(circleBoundaryOffset),
  floored at a 2px minimum -- a real bug caught during verification, where a
  large negative offset combined with a small circle size produced a
  negative radius (meaningless for both the `dist > R` check and the
  center + normal*R clamp). Confirmed fixed: circleBoundaryOffset=-5 against
  the live circleSize=8 now correctly returns 2 instead of -8.
- Endcap Starting Scale (endcapStartingScale, x, 0.1..1, def 0.5, added to
  END EMERGE group right after End Emerge Enabled) -- feeds a full redesign
  of drawEndcap()'s spawn-to-settled interpolation: at factor=0 (the instant
  a cut spawns a fresh endcap) both the draw scale AND the local SVG anchor
  point translated to the tip are pulled toward the design's own bbox
  bottom edge (ENDCAP_BOTTOM_Y, computed once at startup via a throwaway
  SVG element + getBBox(), replacing the unused ENDCAP_WIDTHS concept from
  the initial End Emerge build) at endcapStartingScale; as factor rises to
  1 over the End Emerge Delay + End Emerge Speed/Tweening window, both
  interpolate back to the normal full scale and the normal
  ENDCAP_ALIGNMENT.topY anchor -- producing the requested "spawns aligned
  to the cut end at reduced scale, then scales up and slides down the rope
  into its normal position" effect. The X anchor (topCenterX) stays fixed
  throughout; verified all 14 endcap designs are horizontally symmetric
  around it.

Verified (via a temporary debug hook, window.__debugTMPropo, removed
before commit, grep-confirmed 0 references):
- anchorBounceIntensity, isolated directly against constrainAnchorToCircle()
  (bypassing the rest of update()'s pipeline, which was contaminating an
  earlier test -- see below): restitution 0 -> 0 rebound velocity,
  0.4 -> -4, 1.0 -> -10, against a fixed +10 outward test velocity --
  exactly matches the v' = v - (1+r)*(v.n)*n formula. Confirmed correctly
  wired.
- anchorWeight: a from-rest single-frame comparison at weight 1 vs 3 showed
  a real, correctly-directed difference (1.089 vs 1.776 downward velocity;
  not a clean 3x ratio, expected since the boundary clamp and distance
  constraint both interact with the anchor point within the same frame).
- End Emerge redesign: triggered a real mid-rope cut via
  hitTestRope()+cutRopeAt(), then stepped the simulation forward -- factor
  stayed null while the cut was still mid-sweep (pieceCount 0), then rose
  cleanly 0 -> 0.265 -> 1.0 across the delay+emerge window and held at 1.0
  afterward, no thrown errors. Confirmed startAnchorY (157.80, the current
  endcapDesign's own bbox bottom) sits below normalAnchorY (80.26, the
  design's neck/top edge) as expected for the "bottom edge at spawn, top
  edge once settled" interpolation.
- General regression: 5s of settle time post-cut kept segment-length
  ratios bounded at 1.0-1.139x (consistent with the already-documented
  constraintIterations=3 tradeoff, not a new instability), and the anchor
  point settled at exactly circleAnchor + (0, anchorBoundaryRadius) -- dead
  center of the bottom of its boundary circle, the expected resting
  equilibrium.

One test-methodology trap hit and worked around, worth recording: an
earlier combined test that set the anchor's position/velocity directly
and then called the full d.update() (rather than
constrainAnchorToCircle() in isolation) produced an identical -5 result
across bounce intensities 0/0.4/1.0 -- a flat, suspicious result, since
different restitution values should produce different reflected
velocities. Root cause: update() runs integrateChain()'s own per-iteration
boundaryConstraint position-clamp (which does NOT depend on restitution)
BEFORE constrainAnchorToCircle() ever runs, so by the time the bounce
logic executed, the position-clamp (plus the distance constraint pulling
against it) had already overwritten most of the manually-set test
velocity, in a way that happened to look similar across all three
intensities. Resolved by isolating constrainAnchorToCircle() directly via
the debug hook instead of routing through update() -- confirmed the
formula itself was correct all along, per the isolated results above.

node --check clean. A stray double-blank-line left behind by the earlier
debug-hook removal was also cleaned up while removing this turn's hook.

--------------------------------------------------------------------------------
2026-09-02  -  10:59 PM - 11:37 PM EDT

Added Tip Segment Shape -- a user-supplied custom shape rendered on the
rope segment(s) right below the endcap, after an extended design
conversation across many turns:
- Started conversational: user asked how hard 3D would be (physics
  generalizes almost for free, rendering/interaction don't -- answered
  directly, no code touched), then asked how to swap the rope body for a
  custom shape instead. Explained two approaches: repeated stamps
  ("beads on a chain," a decorative/repeating look) vs. a continuous
  variable-width ribbon (a single shape that genuinely tapers). User
  wanted something narrower: just the ONE segment beneath the endcap,
  not the whole body -- clarified that a single stamped shape swings and
  rotates with the rope but does NOT bend within itself (it's rigid,
  just repositioned/rotated each frame), which the user confirmed was
  fine for what they had in mind.
- User handed over data/Rope/RopeEG.svg -- first export contained only
  an alignment-line stub and two degenerate zero-area polygons, no real
  shape (caught and reported before assuming anything about the shape).
  Re-exported version had a real path; hand-traced its bezier vertices
  (no SVG rendering available in this environment at the time -- see
  below) to describe the silhouette back to the user (flared top,
  narrowing to a neck) before they corrected it with an actual reference
  image: it swells back into a full rounded body below the neck, THEN
  narrows to a small two-lobed forked point at the very bottom -- more
  vase-shaped than the hand-trace alone suggested.
- Implemented TIP_SEGMENT_SHAPE (computed once at boot via a throwaway
  <path>+getBBox(), same technique ENDCAP_BOTTOM_Y already uses, since
  this SVG has no explicit alignment <line> of its own -- the user
  specified orientation directly instead: "a vertical line through the
  horizontal center of this svg"), drawTipSegmentShape() (bottom-anchored
  at the tip, same convention as drawEndcap()), and a new Tip Segment
  Shape checkbox (ROPE group). getBBox()'s measured values matched the
  independent hand-trace almost exactly (topCenterX 134.62 vs
  hand-traced 134.62; topY/bottomY 27.32/341.30 vs 27.32/341.3),
  cross-confirming both were done correctly.
- First scale implementation matched the shape's height to the live
  segment's own current length, mirroring how Endcap Height already
  works. Measured (not just eyeballed) as wrong before shipping: the
  shape's natural proportions are ~2.5:1 tall, but one physics segment
  is only ~9% of that natural height at default settings -- a per-row
  filled-width scan across the compressed render showed essentially NO
  width variation (~37px constant), meaning the shape's own
  neck/waist/belly/fork detail had no vertical room left to read as
  anything but a flat blob. Presented the tradeoff directly to the user
  (proportion-preserving scale vs. forced segment-length match vs. a
  multi-segment middle ground) with a stated recommendation rather than
  silently picking one, since it's a genuine visual/behavioral choice,
  not an implementation detail -- user chose proportion-preserving.
  Rewrote drawTipSegmentShape() to scale uniformly from rope thickness
  alone (same formula drawEndcap() already uses), accepting that the
  shape's own top edge no longer generally lands exactly on the
  second-to-last physics point.
- That fix surfaced a SECOND real bug, only visible once the first was
  fixed: the plain rope stroke draws the chain all the way to the true
  tip regardless, in the exact same cfg.ropeColor as the new shape -- so
  the full-width stroke underneath filled in exactly where the shape's
  own narrower silhouette should have shown as a visible cutout, making
  it invisible against its own backdrop despite rendering correctly in
  isolation (confirmed separately by drawing it directly in an
  unmistakable bright test color and finding it exactly where expected
  via pixel sampling). Fixed with pointsExcludingTipSegmentShape():
  converts the shape's own scaled height into a whole number of segments
  via the entity's own segLen, and truncates the points array passed to
  strokeRopeCurve() by that many trailing points (approximate, not exact
  arc-length -- intentional, since over-omitting just shows more of the
  shape's own taper instead of stroke, never a gap). The truncated array
  is used ONLY for the stroke call; drawTipSegmentShape() itself always
  gets the full, untruncated points so its own position/rotation stays
  correct regardless. mainArcMult/tipArcMult (the round-cap-at-the-free-
  end treatment) are also forced to 0 whenever the feature is on, same
  reasoning hasEndcap already uses.
- Re-verified via a width-profile pixel scan after both fixes: a real,
  smooth taper matching the shape's actual silhouette -- 34px near the
  top (flared edge appearing) -> 20px at the waist -> 38px at the belly
  -> down to 6px at the forked bottom tip. Confirmed zero regression to
  the OFF state (perfectly flat, constant-width plain rope, 9/9 samples
  identical).
- This entire feature was verified without a single working screenshot:
  this session's Browser pane intermittently failed to composite fresh
  frames after direct ctx-level draw calls (confirmed via a controlled
  test -- a direct ctx.fillRect and a direct drawEndcap() call with an
  unmistakable bright color produced no visible change across 3
  consecutive screenshots, while ctx.getImageData read back on the SAME
  canvas correctly showed the new pixels were actually there). All
  verification used direct pixel sampling against the live canvas
  instead. Also discovered mid-session that canvas.width vs
  window.innerWidth (the actual backing-store-to-CSS-pixel ratio) is NOT
  reliably equal to window.devicePixelRatio in this environment --
  observed 1600x1000 canvas for a 1280x800 CSS viewport on one page load,
  then 1280x800 (1:1) on another with the identical viewport -- always
  compute the ratio fresh from canvas.width/window.innerWidth when pixel-
  sampling in this environment rather than assuming devicePixelRatio.
- Also fixed, mid-session, per direct report: clicking Save while
  running on Vercel produced "Save failed: GitHub commit failed 404".
  Traced api/save-settings.js's GitHub-Contents-API write to a hardcoded
  DEFAULT_BRANCH='main' -- this repo's actual (and only) branch is
  master (confirmed via `git branch -a`/`git remote show origin`), so
  every commit attempt was targeting a branch that doesn't exist,
  which GitHub's API reports as a plain 404 on the write step. Fixed the
  default to 'master'; committed alone (api/save-settings.js only) since
  index.html had substantial unrelated concurrent work in progress at
  the time.

Verified:
- node --check clean throughout every intermediate edit.
- TIP_SEGMENT_SHAPE's getBBox()-derived geometry cross-confirmed against
  an independent hand-trace of the SVG's own bezier path.
- Both scale-formula bugs (segment-squash, stroke-overlap) caught via
  direct measurement (pixel-width scans) before being reported as fixed,
  not assumed correct from code review alone.
- Direct isolation test proved drawTipSegmentShape() itself was correct
  even while screenshots showed no visible change, correctly identifying
  the Browser pane's compositor (not the app code) as the actual gap.
- Fresh page load: no console errors.
- Did not live-verify the Save-button GitHub-commit fix against a real
  Vercel deployment/GitHub API (no access to the live env vars/token
  from this sandbox) -- reported as the leading, well-evidenced
  hypothesis (a directly-verified branch-name mismatch), not a confirmed
  fix, pending the user's own redeploy.

--------------------------------------------------------------------------------
2026-09-02  -  11:46 PM EDT

A 6-item follow-up on the anchor-physics/End-Emerge work: remove the
anchor bounce, fix double-click-in-circle, redesign Rope End/Top Curve
Arc's actual meaning, and rework End Emerge's scale/hide/easing controls.

Removed:
- Rope Start Bounce Intensity (the dev slider AND the separate
  constrainAnchorToCircle() velocity-reflection step it drove) -- per
  explicit feedback that the bounce read as the anchor "suddenly zooming
  back to its default spot" instead of a natural weighted body rolling
  down the side of the circle. A first fix attempt tried to make
  integrateChain()'s own position-clamp "velocity preserving" by shifting
  oldx/oldy alongside the clamped x/y -- this introduced a REAL,
  measured bug (caught via direct testing before shipping, not assumed
  correct from the math): the clamp can fire on more than one of
  constraintIterations' rounds per frame, and shifting oldx/oldy on EVERY
  firing compounds each round's correction on top of the last -- a
  15px/frame outward test velocity came out as 323px/frame after a single
  update() call. Reverted to a plain position-only clamp (matching how
  the distance/bend constraints in the same loop already work, neither of
  which ever touches oldx/oldy) -- this alone, with the bounce function
  removed entirely, produces the requested natural roll/settle behavior:
  re-verified via 2 isolated tests, a hard outward flick now implies a
  reasonable, non-compounding correction (-20 for a +15 push past the
  boundary, not 323), and a strong tangential kick shows the point
  visibly sliding sideways along the boundary before gravity resettles it
  at the bottom (x drifted from 640 to 643.76 before returning to ~640
  over ~15 frames) -- and a real chain-propagated disturbance (kicking the
  segment next to the anchor) still displaces and resettles it correctly,
  with the same already-documented 1.29x residual ratio as before (no new
  instability).

Fixed:
- Double-clicking inside the circle graphic never cut the rope --
  reported directly ("double click in the circle STILL DOESNT WORK").
  Root cause #1: onPointerDown routed ANY press starting inside
  isOnCircle()'s exclusion zone into a 'circle' (hold-to-grow) mode that
  never checked for a double-click at all. Fixed by adding the same
  pendingClick-based double-click detection already used on the rope-side
  path to the 'circle' branch too, resolving the target via a new
  hitTestAnyIgnoringCircle() (identical to hitTestAny() but resolves the
  rope hit via nearestPointOnRope() directly, skipping isOnCircle()'s own
  exclusion -- used ONLY for this path; normal single-click/hold-to-grow
  on the circle is unaffected).
  Root cause #2, found during verification (not assumed fixed after #1):
  cutRopeAt()'s own "too close to cut" floor was `Math.max(circleCutDistance,
  circleExclusionRadius())`, measured from circleAnchor() (the circle
  GRAPHIC's fixed center) -- since this floor is a MAX against
  circleExclusionRadius(), the exact same threshold isOnCircle() uses to
  route a press into 'circle' mode, it is ALWAYS >= that threshold by
  construction. Meaning: no click that ever reached this function via the
  new circle-mode double-click path could EVER pass this check, regardless
  of how far it actually was from the anchor -- confirmed directly via a
  debug-hook test (a double-click correctly routed through, via a real
  hit from hitTestAnyIgnoringCircle(), was still always rejected). This
  floor's own original reasoning predates the anchor becoming a
  free-moving point (back then, "close to the anchor" and "close to the
  circle's fixed center" were the same thing by definition). Fixed by
  measuring from mainRope.points[0] (the anchor's REAL current position)
  instead of circleAnchor(), and removing the circleExclusionRadius()
  floor-under-the-floor entirely -- Circle Cut Distance (already
  independently tunable, and set noticeably smaller in the user's own
  current settings) is now trusted directly. Verified end-to-end via the
  real onPointerDown/onPointerUp handlers: a double-click on a rope point
  resting visibly inside the circle (confirmed blocked by hitTestRope()'s
  own isOnCircle exclusion beforehand) now correctly schedules AND
  completes a real cut (pendingCut created, then a real fallen piece
  after the delay elapses); a double-click exactly at the anchor's own
  position is still correctly refused (0 distance, always < Circle Cut
  Distance).
- Rope End Curve Arc / Rope Top Curve Arch redesigned per explicit
  clarification: "At 0, the end of the rope is a straight line, at 1 its
  a full semi circle... at 0.5, it woud be halfway between the two." The
  previous implementation drew a filled circle of variable RADIUS at each
  end (strokeRopeCurve()'s old endArcMult/startArcMult block) -- at 0.5
  this just looked like a smaller button sitting on the stroke's own
  flat-cornered tip, not a flattened semicircle. Replaced with a new
  drawEndArc(px, py, dir, thicknessPx, arcMult) that draws a half-ellipse
  via ctx.ellipse(): the flat chord always spans the rope's full width
  (thicknessPx), and the outward protrusion is (thicknessPx/2)*arcMult --
  0 degenerates to a flat line, 1 makes the protrusion equal the chord's
  own half-width (a TRUE semicircle), values between are a proportionally
  flattened arc rather than a smaller full circle. Verified via a
  monkey-patched ctx.ellipse() call during a real render() pass:
  radiusX/radiusY both came out to exactly thicknessPx/2 (19.2px at the
  live 4.8%vmin thickness) at arcMult=1, confirming a true semicircle, not
  an undersized circle.

Changed (End Emerge):
- The spawn/scale effect now scales ONLY the Y (length-along-the-rope)
  axis, per explicit feedback ("can you try scaling it only in the Y
  axis, as in the length axis"). drawEndcap()'s ctx.scale() call now uses
  a fixed normalScale for X and an interpolated yScale for Y (previously
  both axes shared the same interpolated `scale`). Verified via
  monkey-patched ctx.scale() calls: X came out identically 0.367 at both
  factor=0 and factor=1, while Y went 0.0367 (0.367 x the live
  Endcap Starting Scale 0.1) -> 0.367.
- Added "End Emerge Hide Distance" slider (x, 0-3, default 1): replaces
  the previous hardcoded automatic "how far the endcap starts hidden"
  (derived from ENDCAP_BOTTOM_Y, the design's own bbox bottom) with a
  tunable multiplier of that same per-design bottom-minus-top span. 0
  means the spawn anchor is exactly the normal topY -- "already aligned
  with its final position," per explicit spec -- and 1 reproduces the old
  automatic behavior exactly. Verified via monkey-patched ctx.translate()
  calls: hideDistance=0/factor=0 -> anchorY -80.26 (matches -topY exactly);
  hideDistance=1/factor=0 -> -157.804 (matches -ENDCAP_BOTTOM_Y, the old
  default); hideDistance=1/factor=1 -> -80.26 (always settles to normal
  topY regardless of hideDistance).
- Added "End Emerge Easing Strength" slider (x, 1-6, default 2): each
  EMERGE_TWEENS curve now takes the eased exponent as a parameter instead
  of a value hardcoded to 2 (t², 1-(1-t)², etc.) -- default 2 reproduces
  the exact old curves, per explicit feedback that the fixed built-in
  curves "right now its not enough" (too subtle). `linear` ignores the
  strength parameter entirely (stays true linear regardless). Verified:
  easeOut at progress 0.5 produced 0.5/0.75/0.9375 for strength 1/2/4 --
  strength 1 is exactly linear (t=0.5), strength 2 exactly matches the
  original hardcoded formula (1-(1-0.5)^2=0.75, confirming no
  default-behavior regression), strength 4 is visibly more pronounced.

"Set defaults": merged a user-provided Copy Settings dump against the
live git-tracked settings log per this project's own §12m resolution
rule -- computed programmatically (O/G/P per field) rather than by hand,
since the git log had itself moved independently in the interim (a live
client saved a different ropeLength/circleCutDistance sometime during
this session, confirmed via re-reading the settings file immediately
before merging rather than trusting an earlier read). Every differing
field resolved cleanly via the rule's own "P wins whenever P != O,
regardless of G" case -- no field required a judgment call. Also added
2 new keys (endEmergeHideDistance, endEmergeEasingStrength) with their
DEV_GROUPS defaults, since the user's paste predates those settings.

Verified:
- node --check clean at every checkpoint.
- Live-tested via a temporary debug hook (window.__debugTMPropo2, chosen
  to avoid colliding with a concurrent session's own window.__debugTMP5,
  which was read-only reused where convenient rather than touched --
  both hooks fully removed before commit, grep-confirmed 0 references to
  either).
- General regression re-confirmed with all changes combined and the
  final merged settings live: growth/settle stayed stable (segment-length
  ratio bounded at 1.234x, consistent with the tradeoff already documented
  for a lower Constraint Iterations), 0 new console errors on a
  confirmed-fresh page load, and a real screenshot showing correct visual
  rendering (curved top, tapered rope, endcap, Tip Segment Shape all
  compositing together correctly).
- This project's own settings file and index.html were both found to
  have changed on disk mid-session from concurrent activity (another
  Claude session's Tip Segment Shape feature; a live client's own further
  slider tweaks saved to the settings log) -- re-read fresh immediately
  before every write that depended on current state, rather than trusting
  an earlier read, per this project's standing multi-session discipline.

--------------------------------------------------------------------------------
2026-09-03  -  01:39 AM - 01:49 AM EDT

Incorporated 7 user-added/edited endcap SVGs from data/Rope/ into
ENDCAP_DESIGNS -- "I added more end svgs and edited some of the old ones.
incorporate all that then push":
- Inventoried the folder against the currently-registered ENDCAP_DESIGNS
  keys rather than assuming from the request's own wording: found 5
  files with no matching registered key (End_Form1-01.svg,
  End_Form1-15.svg, End_Form2-16.svg, plus 2 new standalone designs
  without the usual dash-number suffix, End_Form4.svg and
  End_Form5.svg), and 2 files at ALREADY-registered filenames whose
  content had changed (End_Form1-02.svg, End_Form1-04.svg).
- Confirmed via direct content comparison, not assumption, that the
  "already registered" edits weren't simple in-place tweaks:
  End_Form1-01.svg's current content is byte-identical to what key
  form1-02 used to hold, and End_Form1-04.svg's current content is
  byte-identical to what form1-05 used to hold -- the user has been
  reshuffling which numbered slot holds which shape, not editing shapes
  in place. Resolved by treating each file's CURRENT on-disk content as
  authoritative for its filename-derived key (End_Form<N>-<M>.svg ->
  form<N>-<M>) unconditionally, rather than trying to infer or preserve
  any renumbering intent -- "incorporate what's there" doesn't require
  understanding why it moved, and guessing at the reshuffling logic
  would only risk getting it wrong.
- Updated ENDCAP_DESIGNS (d string + matching Path2D for all 7) and the
  Endcap Design dropdown's options list (19 designs total afterward, up
  from 14, alphanumerically ordered within each Form family).
  ENDCAP_BOTTOM_Y needed no manual update -- it iterates ENDCAP_DESIGNS
  by key automatically, already established from an earlier round.
- All 7 new/changed files use viewBox "0 0 129.7 183.44" and, where they
  include an ANCHOR_LINES reference group at all (Form4 does; several
  others don't need one since their own path starts exactly at the
  shared alignment line's own coordinates, same established pattern as
  End3 from an earlier round), match ENDCAP_ALIGNMENT's own line exactly
  (x1=41.57, x2=96.06, y=80.26) -- confirmed consistent with the shared-
  alignment convention this project's endcap system already depends on.

Verified:
- node --check clean.
- Live: cycled the Endcap Design dropdown through all 7 new/changed keys
  via a real DOM `change` event (not setting cfg directly) -- confirmed
  no thrown errors and the correct value landing in cfg.endcapDesign each
  time.
- Live: called render() directly for each of 9 checked keys (all 7
  new/changed plus 2 known-good originals as a baseline) with Tip Segment
  Shape disabled to isolate the endcap specifically, then sampled an
  80x70 pixel region around the tip for rope-colored fill -- every key
  produced a similar-magnitude, non-zero, non-degenerate fill count
  (2614-2624 pixels across all 9), catching any silently-broken SVG path
  syntax that node --check cannot validate, rather than trusting the
  syntax check alone.
- Did not visually confirm each new shape's silhouette looks correct
  (this session's screenshot compositor has been unreliable after direct
  ctx-level draws in earlier rounds) -- verification was geometric/
  presence-based (non-empty, plausible-magnitude fill), not a rendered
  visual comparison against the source SVGs.

--------------------------------------------------------------------------------
2026-09-03  -  01:58 AM - 02:11 AM EDT

Startup animation, from the explicit spec: "on startup, there is no rope,
just the circle... 2 seconds pass, a circular shape the color of the rope
appears from 'within' the circle... as this circle rises up, when it
reaches the top point of the Rope Start in its equilibrium position, it
stops climbing, then our rope begins to emerge from this circle downwards,
the same as the extension animation... App resumes as normal. implement
then push", plus a mid-turn follow-up adding *D* controls for the rise
speed and the wait-before-growing pause (set to 0.5s).

Added:
- New dev-panel group STARTUP ANIMATION: Startup Animation Enabled
  (checkbox), Startup Wait Duration (0-10s, def 2s), Startup Rise Speed
  (5-300%vh/s, def 80%vh/s), Startup Pause Duration (0-5s, def 0.5s).
- A 4-phase state machine (introPhase: waiting -> rising -> pausing ->
  growing -> done) driving the whole sequence each frame from update():
  - waiting: mainRope exists and physically integrates (so its anchor
    settles to its real equilibrium position) but isn't drawn -- only the
    background/floor/circle show, per "there is no rope, just the circle".
    cfg.introEnabled is re-checked every frame here (not just once), so a
    late-arriving async settings load (resetSettings() is unawaited) that
    turns the feature off self-corrects immediately to a full-length,
    'done' rope instead of getting stuck mid-animation on a stale flag.
  - rising: a rope-colored dot (radius = rope thickness / 2) climbs from
    just below the circle's edge up to mainRope.points[0]'s LIVE position
    -- read fresh every frame rather than computed from a hardcoded
    circleAnchor()+anchorBoundaryRadius() formula, so the target always
    matches whatever the real anchor physics actually settle to.
  - pausing: the dot holds at that position for Startup Pause Duration.
  - growing: reuses the EXISTING hold-to-grow mechanic verbatim -- sets
    the same module-level `growing` flag the manual click-and-hold
    interaction already drives, so growRope()/positionGrowingTip() run
    completely unmodified. updateIntro() only watches
    mainRope.totalLength against a target (vh(cfg.ropeLength), captured
    at the pausing->growing transition, well after any settings-load
    race could matter) to know when to flip `growing` back off.
- resetMainRope() now takes an optional startingLengthPx override, used
  only by Boot to build a minimal single-segment rope for the animation
  to visibly extend from (every other caller is unchanged and still gets
  the full configured length).
- render() gates mainRope's own stroke/tip-segment-shape/endcap calls
  behind introPhase being 'done' or 'growing' (fallenPieces' rendering is
  untouched and unconditional -- nothing can be cut before the intro
  finishes anyway), and draws the rising/pausing dot in the same spot.
- onPointerDown() now no-ops for the whole animation (any phase other
  than 'done'), so a stray click during waiting/rising/pausing/growing
  can't interrupt or interact with it.

Verified:
- node --check clean after every edit in this round.
- Live, via a temporary window.__debugTMP1() state hook (removed before
  commit; grep -c confirms 0 references remain) polled with
  requestAnimationFrame across a full run: confirmed all 5 phases occur
  in order (waiting -> rising -> pausing -> growing -> done) with
  correct timing (~2.0s wait, pause duration landing at 0.526s against a
  configured 0.5s -- one fixed-timestep's worth of quantization, expected)
  and correct dot behavior (starts below the circle, ends exactly at the
  live anchor position).
- Live, via ctx.getImageData pixel sampling (this session's screenshot
  compositor has been unreliable after direct ctx draws in earlier
  rounds, so pixel sampling is the established verification method):
  confirmed the pixel at the circle's edge is exactly the configured
  background color during 'waiting' (no stray dot/rope pixel), and the
  pixel at the dot's position during 'pausing' is exactly the configured
  rope color.
- Did not live-test the introEnabled=false disabled path (toggling the
  checkbox and reloading) -- confirmed correct by direct code inspection
  instead (a simple, low-risk conditional reusing the already-verified
  setMainRopeTotalLength()), per proportional verification effort.

--------------------------------------------------------------------------------
2026-09-03  -  02:15 AM - 02:36 AM EDT

User reported what read as a regression right after the startup animation
shipped: "when I double click in the circle boundary instead of it cutting
the rope at the minimum length... the entire length of visible rope falls
out of the hole... rope start becomes unattached... and then after the
same amount of time as the startup animation, the startup animation
begins which starts a new rope and so on." Investigated first rather than
assuming it was a bug: grepped for every assignment to `introPhase` (found
exactly one 'waiting' assignment, at Boot -- structurally nothing else in
the app can restart the sequence) and directly invoked the real
`cutRopeAt()`/`performMainRopeSplit()` path via a temporary debug hook
against a live server, confirming a normal minimum-length cut still leaves
a correctly-attached, correctly-rendering (pixel-verified) stub -- no
code-level bug found. Also caught via `git fetch` that the report was
against the live Vercel deployment (2 "Update dev-panel-settings.json via
Save Settings" auto-commits had landed there since this session's last
push), with real settings (`circleSize: 8%vmin`, `circleCutDistance:
3%vmin`, `minRopeLength: 0.5%vh`) small enough that a correct
minimum-length cut leaves a stub just a few px tall, almost entirely
hidden inside the circle graphic. Asked the user directly (AskUserQuestion)
whether the page was actually reloading -- they clarified the described
behavior (full detach + auto-regrow) is not a bug report at all, but a
feature they want built.

Added:
- Double-clicking in the circle now detaches the ENTIRE rope (not a
  minimum-length partial cut) and replays the startup animation to grow a
  fresh one in, repeatably. New `detachEntireRopeAndRestartIntro()`: the
  whole `mainRope.points` array becomes a new fallen piece (via the same
  `topplePiece()`/End-Emerge-state machinery an ordinary cut already
  uses), `mainRope` resets to Boot's own minimal single-segment starting
  rope, and `introPhase` is set back to 'waiting' -- reusing the exact
  same wait/rise/pause/grow sequence and state machine from the startup
  animation feature, not a parallel implementation. Wired in at
  `onPointerUp`'s existing double-click-on-circle branch, replacing the
  `cutRopeAt()` call specifically for a `target.target === 'rope'`
  resolution; double-clicking an already-fallen piece from the circle is
  unaffected (still a normal `cutPieceAt()`), matching the original
  report's own scope (about the live rope specifically).

Verified:
- node --check clean after every edit.
- Live, via a temporary debug hook (removed before commit, grep -c
  confirmed 0 references remain): a real double-click dispatched at the
  circle's center correctly moved the FULL original rope (all 9 points)
  into a new fallen piece, reset mainRope to the minimal 2-point starting
  rope, and set introPhase to 'waiting'; the full replay then correctly
  regrew the rope back to its configured length (198px vs. an original
  196px, matching within the same 1-frame-overshoot tolerance already
  documented for the startup animation's own growth phase). Confirmed
  repeatability ("and so on"): triggering it a second time correctly
  produced a second fallen piece and a second full replay. Confirmed
  interaction is correctly blocked for the whole replay (the existing
  onPointerDown intro-phase guard applies automatically, since no new
  guard was needed) -- a double-click attempted mid-sequence was a
  verified no-op.

--------------------------------------------------------------------------------
2026-09-03  -  02:43 AM - 02:56 AM EDT

A follow-up request that arrived mid-turn while confirming the previous
double-click feature: "and why cant i set the startup rise speed slower
than 5? let me do really slow. *D*Slider for the speed at which the
startup animation rope extends. give me a checkbox to pick the option to
instead of setting the speed the entire extension, i can set a starting
speed, and that speed tweens into the default extension speed as the rope
extends."

Added:
- A permanent, non-interactive "background" rope, per explicit request:
  its own anchor is forced to mainRope's real (post-integration) anchor
  position every frame, but it swings under its own fully independent
  physics (same cfg values -- gravity/damping/constraint iterations --
  as mainRope, "the same physical properties"), and it can never be cut
  or extended, drawn clipped to exactly the circle's own circular shape
  (canvas `ctx.clip()`, a first for this project) so the circle genuinely
  acts as a clipping mask rather than a plain graphic. New `bgRope`
  module state + `resetBgRope()` (built once at Boot, and again at the
  startup animation's own 'pausing'->'growing' transition so its fixed
  length locks in against the real settings-loaded Rope Length rather
  than a possible pre-load default, same race-avoidance pattern the
  animation's own target-length capture already uses). Hidden during
  waiting/rising/pausing, same gate as mainRope itself, so it doesn't
  contradict "no rope, just the circle."
- Startup Rise Speed's slider minimum lowered from 5 to 0.5 %vh/s (step
  also finer, 5->0.5) so a genuinely slow climb is reachable by dragging,
  not just by typing past the old floor.
- A dedicated "Startup Extension Speed" slider, decoupling the startup
  animation's own 'growing' phase from the manual hold-to-grow rate
  (previously silently reused `cfg.ropeGrowthRate`) -- `growRope()` now
  takes an optional rate override, defaulting to `cfg.ropeGrowthRate` so
  every pre-existing caller is unaffected.
- "Startup Extension Speed Tween Enabled" checkbox + "Startup Extension
  Starting Speed" slider: when on, the extension rate linearly tweens
  from the starting speed up to the default extension speed as progress
  goes 0%->100% of the extension's own span (new `currentIntroExtendRate()`
  helper), instead of extending at one flat rate throughout.

Fixed:
- A real, previously-undiagnosed bug found while investigating why the
  background rope's clip looked wrong in early testing: the Rope Length
  slider's `onChange` (`setMainRopeTotalLength()`) fired unconditionally
  whenever `resetSettings()` applied a saved value -- including mid-
  startup-animation -- silently snapping mainRope to full length during
  'waiting', long before the animation's own 'growing' phase ever ran.
  This meant the "grow from nothing" effect had likely never actually
  been visible on a real deployment where a saved Rope Length loads
  successfully (the normal case) since the very first startup-animation
  round, even though every direct code-path test back then passed.
  Fixed by skipping that onChange resize while `introPhase !== 'done'`
  -- the animation already reads `cfg.ropeLength` itself, fresh, at the
  exact moment it needs to, so nothing is lost.

Verified:
- node --check clean after every edit.
- Live, via a temporary debug hook (removed before commit, grep -c
  confirmed 0 references remain): after the onChange fix, a full
  waiting->rising->pausing->growing->done cycle showed mainRope
  genuinely starting at 1 segment (~28px) and growing to the configured
  full length (~252px) over a duration matching Startup Extension
  Speed's own value almost exactly (1863ms observed vs. ~1867ms
  computed from rate*distance) -- confirming both the bug fix and the
  new dedicated rate. bgRope's own anchor point matched mainRope's exactly
  at every sampled frame across a full cycle. Monkey-patched
  ctx.save/clip/restore/stroke confirmed exactly 1 clip() wrapping
  exactly 1 stroke() per render() frame -- the clip path executes
  as written. Toggled the tween checkbox live via real DOM events and
  set distinct start (3) / default (30) speeds, then ran a full detach-
  and-regrow cycle: the sampled rate rose smoothly and monotonically
  from ~3 at the start of 'growing' to exactly 30 at 'done', confirming
  the linear tween. One earlier isolated "Cannot read properties of
  undefined (reading 'x')" console error appeared once, immediately
  after a run of failed/retried navigations to the same URL (a likely
  stale-tab-reload race in this session's own browser tooling, not the
  app) -- did not recur across 4 subsequent clean full-cycle runs with
  an error listener attached, so not treated as a real bug.

--------------------------------------------------------------------------------
2026-09-03  -  04:00 AM EDT

Incorporated user-edited/new endcap SVGs ("i added new svgs and edited
some. incorporate").

Diffed every data/Rope/End_Form*.svg's own <path d="..."> against what's
currently embedded in ENDCAP_DESIGNS programmatically (a small Python
script, not by eyeballing file mtimes) to establish the real scope before
touching anything:
- Changed: form1-01, form4 (path data genuinely differs from what's embedded).
- New: form6, form7, form8, form9 (not in ENDCAP_DESIGNS at all yet).
- Unchanged: all other 17 designs (form1-02 through form3-02, form5) --
  despite several having recent file mtimes, their actual <path d> content
  is byte-identical to what's already embedded, so nothing to update there.

Updated form1-01's and form4's `d`/Path2D entries in ENDCAP_DESIGNS to the
new path data; added form6-9 as new entries (ENDCAP_BOTTOM_Y needed no
change -- it's already computed dynamically by iterating ENDCAP_DESIGNS at
startup, so the 4 new designs picked up correct values automatically);
added all 4 to the Endcap Design dropdown's options list, 24 designs total
now.

Re-checked form1-01's edit against the neck-seam issue reported and
diagnosed 2 rounds ago (see CODE_SUMMARY's Gotchas): **still not fixed**.
Sampled the new path's own fill at the exact anchor line (y=80.26, same
isPointInPath technique as the original diagnosis): still only 41.6-45.8
filled (a ~4-unit sliver), not the expected full 41.6-96 span -- nearly
identical to the pre-edit measurement. The edit that landed (a new
closing segment, `h-50.19Z` replacing the old `h-4.31`) changed how the
path closes, not the shape of its top edge, which is still a narrow peak
at the pin line rather than a flat line spanning the full neck width.
Flagged directly to the user rather than assumed fixed. form4's edit and
all 4 new designs (form6/7/8/9) DO fill the full expected span at that
exact line -- verified the same way, confirming they're correctly
constructed like the project's other already-good designs (form1-02/03/04
originals, form5).

Verified all 6 touched designs (form1-01, form4, form6-9) render via a
real update()+render() pass with 0 thrown errors, and visually via
screenshots showing a clean, seamless rope-to-endcap join for form4 and
form8 (representative samples).

Found and worked around, but did NOT fix (not this session's own work,
not asked for): 2 real bugs in the still-in-progress Startup Animation
feature that blocked even getting a valid rope state to test against.
mainRope.points is empty immediately after boot with the live
introEnabled:true default, and resetMainRope() (called directly) also
produces 0 points/segLen=0 -- integrateChain() then throws
"Cannot read properties of undefined (reading 'x')" trying to read
points[0] for the anchor's boundaryConstraint. Once mainRope was manually
populated in place (mutating the real module-level object via a getter,
avoiding this session's own well-documented "captured stale reference"
trap), the SAME class of error recurred one level down: bgRope.points[0]
is ALSO empty, crashing the line that syncs bgRope's anchor to mainRope's.
Manually populated both to get a clean test environment. Confirmed this
is NOT a test artifact: reproduced identically on a completely fresh,
untouched page reload with zero debug-hook interference, as a real
"loop() error (frame skipped)" on every single animation frame -- meaning
the app currently never renders or updates at all with its own live
default settings. Worth noting: the Startup Animation feature's own
immediately-preceding changelog entry mentions seeing this SAME error
message once and dismissing it as "a likely stale-tab-reload race...not
the app," not recurring across 4 subsequent runs for that session -- this
session's own reproduction was 100% consistent across every fresh reload
tried, suggesting that earlier dismissal may have been premature, or
something changed since. Not investigated further or fixed -- flagged in
PROJECT_PROGRESS's Open Questions instead, since it's clearly someone
else's still-active feature, not this task's scope.

node --check clean throughout. Excluded data/processed/dev-panel-
settings.json from this commit -- it had independently moved with a large
set of unrelated live-Save changes (new gradient/intro-related cfg keys)
from the SAME concurrent Startup Animation work, mid-session; not this
session's own change to make or commit.

--------------------------------------------------------------------------------
2026-09-03  -  03:24 AM - 04:03 AM EDT

A large follow-up round, per explicit request: "okay so for the startup
animation now. replace what you did with this - " followed by a 5-item
spec, plus a "set defaults" settings dump at the end. NOTE ON COMMIT
ATTRIBUTION: this round's own index.html changes ended up landing inside
commit 237288e (the immediately-preceding "Incorporate edited/new endcap
SVGs" entry, from a concurrent session) rather than a commit of this
session's own -- confirmed via `git diff HEAD -- index.html` showing zero
remaining diff once this round's work was done, i.e. that commit's author
picked up this session's in-progress edits from disk when they committed
their own SVG work, without knowing they were there. Disclosed here per
this project's standing multi-session transparency convention rather than
left unstated. Only data/processed/dev-panel-settings.json (the "set
defaults" round) remained to actually commit from this session.

Added, per the 5-item spec:
1. Startup animation rework: the background rope (added last round) is
   now the climbing visual itself, not a separate flat dot -- its own
   rope-start climbs from one full circle-diameter below the circle
   (deliberately NOT starting at the bottom edge itself -- with Startup
   Rise Clear Offset at its own default of 0, that would make the
   "cleared" check trivially true instantly, making the whole climb
   imperceptible despite a slow default Startup Rise Speed; caught during
   this round's own verification, not shipped). The phase-advance trigger
   is now geometric -- new Startup Rise Clear Offset slider (%vmin from
   the circle's bottom edge) -- instead of "reaches mainRope's own
   settled anchor position". bgRope's anchor is driven directly during
   waiting/rising (not synced to mainRope), then resyncs to mainRope
   exactly at the rising->pausing handoff (also rebuilding its fixed
   length there, same race-avoidance reasoning as introTargetLengthPx's
   own capture).
2. Rope Gradient Enabled + Rope Gradient Colors (ROPE group), and the
   identical pair for the background rope (bgRopeGradientEnabled/
   bgRopeGradientColors). New "gradient" dev-panel control type: a
   stop-count slider (2-8) followed by that many color pickers,
   regenerated whenever the count changes; cfg[key] holds a plain array
   of hex strings, cloned (not aliased) from DEV_GROUPS' own `def` array
   at cfg-init time so a live edit can never mutate the shared default.
   New ropeStrokeColor() helper builds a real top-to-bottom
   ctx.createLinearGradient from that array (or returns the plain
   fallback color when disabled) -- passed straight into the EXISTING
   strokeRopeCurve() calls for both ropes, no change needed to that
   function itself since ctx.strokeStyle/fillStyle accept a
   CanvasGradient exactly like a string.
3. Fallen rope pieces now collide with each other, per explicit request
   ("I want the cut off rope segments to have collision detection with
   each other"). New pieceCollision(), called from the existing
   floor-collision block: unlike the OLD, removed pileRepulsion() (a
   flat-array all-pairs repulsion that used to cause a severe tangle by
   also repelling non-adjacent points within the SAME chain -- see its
   own prior removal, several rounds back), this only ever repels a
   piece's points against a DIFFERENT piece's points, never its own --
   safe to run every frame by construction. A cheap per-piece bounding-
   box pre-check (computed once per piece, not per pair) skips the
   expensive point-by-point double loop for any pair that can't be
   touching yet.
4. The background rope's own tip is now actively kept out of view
   ("We should never see the rope end of background rope"). New
   maintainBgRopeEnd(), called every frame: extends bgRope by one
   segment (continuing the tip's current direction) whenever its tip
   comes within 1.5x a safe radius of the circle; trims one point off
   the tip once it's comfortably beyond 4x that radius. The wide gap
   between those two thresholds is deliberate hysteresis, so a tip
   sitting near one boundary doesn't add/remove a point every other
   frame. Hard-capped at 400 points against runaway extension.
5. End Emerge redesigned per the user's own clarified spec, replacing
   the simultaneous slide+scale from prior rounds: "After the rope is
   cut... when the endcap starts moving to its default position, instead
   of simultaneously rescaling the endcap back up, the rescaling only
   begins once the endcap top edge has aligned with the rope end." The
   single existing emerge-progress value (`factor`) is now split into
   two sequential halves -- slideFactor (0->0.5 of factor, remapped 0->1)
   drives position at a CONSTANT starting scale; scaleFactor (0.5->1)
   only starts moving once slideFactor has reached 1. End Emerge Hide
   Distance removed entirely per explicit request -- the hidden spawn
   position is no longer a tunable multiplier, it's always exactly one
   full natural design-height up the rope from the tip ("it will be up
   the rope exactly the height of the endcap geometry after scaling").
   Also added, per explicit request: while mid-emerge (factor < 1), the
   endcap's own fill is now clipped to the rope's current thickness
   (a plain rect in the same local-before-scale coordinate frame the
   design path itself is drawn in) so the small/mid-slide endcap can't
   visually poke out past a narrower rope segment than its own full
   natural width (e.g. under Tip Segment Shape's own taper) --
   "make sure to use a mask... since the end segment of the rope has
   variable width".

Also added: a permanent try/catch around loop()'s own body. An uncaught
exception in a requestAnimationFrame callback stops that callback from
ever being rescheduled -- used to mean a single bad frame permanently
froze the entire app with no recovery short of a manual reload. Now
logged via console.error (still visible, not silenced) and the loop
survives to the next frame. Directly motivated by hitting exactly this
class of freeze twice during this round's own testing (see below) before
diagnosing the real cause.

"Set defaults": baked in a full user-provided settings dump (values +
order + panelGeometry) as both the new git-tracked settings log AND the
new DEV_GROUPS hardcoded defaults, per the same convention every prior
"set defaults" round in this project has followed -- treated as
authoritative rather than merged via §12m's O/G/P rule, since it was a
full snapshot dump (not a partial Copy Settings paste against a
known-diverged git log).

Verified (all 5 items, live, via temporary debug hooks removed before
finishing -- grep -c confirmed 0 references remain):
- Rise/clear-boundary timing: a real end-to-end cycle showed the climb
  taking 4300ms for a 172px distance at Startup Rise Speed=5%vh/s
  (40px/s) -- 172/40=4.3s, an exact match, confirming both the new
  starting-position fix and the clear-boundary trigger.
- Both ropes' gradients: mainRope confirmed via direct pixel sampling
  (red-to-blue test colors, top pixel ~[249,0,7], bottom pixel
  ~[31,0,224]); the background rope's OWN gradient couldn't be pixel-
  isolated the same way (it visually coincides with mainRope at rest,
  which draws on top and wins the overlapping pixels) -- confirmed
  instead via monkey-patched ctx.save/clip/restore/stroke showing
  exactly 1 clip() wrapping exactly 1 stroke() call with a real
  CanvasGradient as strokeStyle, using the SAME ropeStrokeColor() code
  path already pixel-verified for mainRope.
- Piece collision: 2 synthetic fallen pieces seeded 7px apart (well
  inside minSep) separated to 58.9px after exactly one pieceCollision()
  call, then re-settled to their own correct ~30px segment length within
  10 more frames -- confirming both the repulsion itself and that it
  doesn't leave a lasting same-piece tangle.
- Background rope end-hiding: verified by code inspection and the
  hysteresis design rather than an extended live stress test (the
  scenario it guards -- a hard swing carrying bgRope's tip back into the
  circle's own clip region -- needs sustained disturbance to trigger
  organically; the underlying distance-check/extend/trim logic itself is
  simple and directly inspectable).
- Endcap slide-then-scale: instrumented factor/slideFactor/scaleFactor
  directly on a real post-cut mainRope.tipEmerge -- confirmed
  scaleFactor stayed at exactly 0 while slideFactor rose from 0 to 1,
  then began rising itself only once slideFactor reached exactly 1 (a
  sampled frame showed slideFactor=0.746/scaleFactor=0 immediately
  followed by slideFactor=1/scaleFactor=0.254 ten frames later) --
  exactly the requested sequencing, not simultaneous interpolation.

Hit and fully root-caused, along the way, 2 real freezes that read like
serious in-app bugs at first (matching what a concurrent session
independently reported the same session as a suspected blocking bug --
see that session's own note in this same file, and PROJECT_PROGRESS's
Open Questions, now resolved there): NOT a code bug, but a sandbox-only
artifact of window.innerWidth/innerHeight sometimes reading 0
immediately after certain ways of opening a fresh Browser-pane tab in
this specific environment (confirmed directly: a tab opened via a bare
preview_start with no prior navigate reported innerW:0/innerH:0 right
after load, and switching to an already-navigated tab with a real
viewport made the issue disappear across every subsequent run). With
those at 0, vh()/vw()/vmin() all return 0, feeding NaN/Infinity into
makeChain()'s point count and producing the exact empty-points-array
crash both sessions independently hit. Full root-cause and resolution
written up in PROJECT_PROGRESS's Open Questions.

node --check clean after every edit throughout. Updated docs/CHANGELOG.txt
(this entry) and docs/PROJECT_PROGRESS.md (new "Recently completed" entry,
plus resolving the "Startup Animation breaks the app" Open Question with
the real root cause above). Committed only
data/processed/dev-panel-settings.json + these doc updates -- index.html
itself needed no commit from this session, per the attribution note above.

--------------------------------------------------------------------------------
2026-09-03  -  04:51 AM - 05:18 AM EDT

A rapid-fire round of 13 distinct requests, most arriving mid-turn while
earlier ones were still being implemented -- listed in the order they
landed:

1. Startup animation: no-jump handoff. When bgRope's climb reaches the
   clear-boundary threshold, mainRope's own anchor now SPAWNS exactly at
   that same point (previously it independently settled to its own
   resting position during 'waiting'/'rising', then bgRope snapped to
   THAT position at the handoff -- a visible jump whenever the two
   differed). mainRope's own physics (already running every frame) then
   naturally settles it toward its real equilibrium afterward -- a real
   physical fall, not a teleport. Verified live: a real cycle showed a
   smooth 7-frame settle curve (155.9 -> 185.4 -> 188.1 -> ... -> 214px),
   not a discontinuous jump.
2. Detach endcap start-scale + grow duration: after a full-rope detach
   (double-click in the circle), the fresh rope's own tip endcap now
   starts scaled down (new Detach Endcap Start Scale slider) and grows
   to normal size over a new Detach Endcap Grow Duration -- deliberately
   NOT the End Emerge system (delay+slide+scale, for CUT edges); a
   simple linear ramp specific to this sequence, undefined (full size)
   for the original boot-time rope.
3. Detach Thickness Multiplier: compounds on every full-rope detach --
   "every subsequent time the rope is fully cut, it increases in
   thickness by [the multiplier]". New module-level
   ropeThicknessMultiplier, applied only to mainRope's/bgRope's own live
   rendering (new `liveThickness`, distinct from the plain `thickness`
   fallenPieces keep using). Verified: 1 -> 1.5 -> 2.25 across 2 detaches
   with the multiplier set to 1.5, exact compounding.
4. Damping's slider minimum raised 0.7 -> 0.8.
5. End Emerge Speed's slider maximum lowered 5 -> 1.5.
6. End Emerge Speed's slider minimum lowered 0.2 -> 0.02 ("0.1 still
   isnt slow enough. i want it at least 5x slower").
7. Detach Pause Duration: a duplicate of Startup Pause Duration, used
   specifically when the intro state machine is re-entered via a full
   detach rather than the original page-load boot -- new
   introTriggeredByDetach flag (set once, on the first detach, and never
   reset -- every later 'waiting' in the same session is detach-
   triggered by definition) picks which duration 'pausing' waits for.
8. Endcap-emerge timing investigation ("are you sure the endcap is right
   under the rope end line? ...i have to wait for longer than my end
   emerge delay to see the animation"): instrumented the real
   factor/slideFactor/scaleFactor progression on a live cut and found
   the position math was already correct, but the 50/50 slide/scale
   split from the previous round meant the visually-obvious part
   (growth) didn't start until HALF the total configured duration had
   passed -- a tiny, rope-colored endcap sliding into place reads as
   essentially invisible, so that whole first half felt like "nothing is
   happening" on top of the real delay. Fixed by rebalancing to a small
   SLIDE_SHARE (15%) so the slide resolves quickly and nearly all of the
   configured duration goes to the part that's actually visible.
9. Dev panel: clamped to the browser viewport on WINDOW RESIZE, not just
   during an active drag ("when i resize my browser to phone size, the
   panel will end up out of frame"). The existing §12c drag-time clamp
   formula never fired on a resize event (only on pointermove during a
   drag), so a panel positioned fine at a wide viewport could end up
   with its saved left/top past a newly-narrow window's edge with
   nothing to pull it back. New clampToViewport(), reusing the same
   formula, on `resize` and once at init. Verified: forcing left:1200px
   at innerWidth:1280 (panel width 526, would put the right edge at
   1726) and dispatching resize correctly clamped left to exactly 754
   (1280-526).
10. Dev panel: rows now wrap (`flex-wrap:wrap` on .dp-row) instead of
    overflowing when the panel is resized too narrow for a row's control
    to fit next to its label -- "shift it to the row beneath the setting
    title".
11. Fallen-piece thickness decay: 3 new sliders (Piece Decay Delay,
    Piece Decay Speed %vmin/s, Piece Minimum Thickness) -- each fallen
    piece tracks its own age and baseThickness (captured at the moment
    it fell, including whatever Detach Thickness Multiplier had already
    compounded), decaying independently after its own delay down to the
    shared floor. New pieceThickness() helper, used at every fallenPiece
    render call site (stroke/tip-segment-shape/endcap/endcap-at-cut-end)
    in place of the flat `thickness` those used before -- mainRope/
    bgRope's own live thickness is unaffected. Verified: 3 synthetic
    pieces (51.2px, 51.2px, 76.8px starting thickness) all correctly
    decayed to the configured 8px floor within 5 simulated seconds at
    Piece Decay Speed=2%vmin/s.
12. Rope+endcap gradient consistency ("the gradient applies to both the
    rope and the endcap such that it is one consistent gradient"): the
    endcap now fills with the SAME gradient (same stops, same world-
    space anchor-to-tip span) the rope's own stroke uses, not a separate
    solid color. Built using LOCAL coordinates -- the same 2 world
    points mapped through the INVERSE of drawEndcap()'s own current
    transform (`ctx.getTransform().inverse().transformPoint(...)`) --
    since a gradient's own stored coordinates are themselves subject to
    whatever transform is active when it's painted, reusing a world-
    space gradient object built outside drawEndcap's translate/rotate/
    scale would have gotten transformed a SECOND time and landed in the
    wrong place entirely.
13. Gradient editor rebuilt from scratch per direct correction ("your
    gradient slider is wrong. I dont want gradients equally spaced. I
    want to be able to slide the sliders to determine where a gradient
    color kicks in... Each slider UI is clickable... opens color picker
    ui", with a reference image) -- the count-slider-plus-N-color-
    pickers control from 2 rounds ago is gone entirely, replaced by a
    Photoshop-style gradient bar: draggable markers set each stop's
    OWN position (never evenly spaced), and a real click (press+release
    with no movement, distinguished from a drag) opens a native color
    picker for that stop. cfg[key] is now an array of {pos, color}
    objects instead of a plain hex-string array -- ropeStrokeColor()
    and drawEndcap()'s gradientInfo consumption, the DEV_GROUPS defaults,
    and the git-tracked settings file were all updated to the new shape
    (old plain-array saves from before this change are auto-converted on
    load, evenly-spaced, so a settings file saved under the old format
    doesn't break). Clicking empty bar space adds a new stop there
    (color interpolated from the gradient's own current value at that
    position); double-clicking a marker removes it (floored at 2 stops).
    Verified live via real dispatched PointerEvents: drag moved a
    stop's pos exactly to the drop position; a same-position press+
    release (a click) opened the color picker pre-loaded with that
    exact stop's color, confirmed via a monkey-patched .click() call;
    changing the native input's value correctly updated only that one
    stop's color; click-empty-space-to-add and dblclick-to-remove both
    round-tripped correctly.

Also caught and fixed, while investigating item 8: a genuine data race
in DEV_GROUPS' own array-default cloning -- the existing shallow
`[...c.def]` clone (added last round to stop a live edit from mutating
the shared default array) doesn't protect against mutating the objects
INSIDE that array, which the new {pos,color} stop shape now has. Cfg-init
now deep-clones each object element too.

A significant complication mid-round: another concurrent session
(working on endcap SVGs, its own settings.json edits still auto-saving
from the live deployment throughout this whole round) pushed 2 more
"Update dev-panel-settings.json via Save Settings" commits while this
round was in progress. Rather than overwrite either side, manually
merged: took the deployment's own latest live-tuned VALUES (their real
ropeThickness/ropeLength/endEmergeSpeed/etc. tuning, including having
ALREADY turned End Emerge Speed down to the new 0.02 floor) as the base,
converted their still-old-format gradient arrays to the new {pos,color}
shape, and layered this round's own new keys on top with sensible
defaults (the live deployment had never seen them, since it's still
running the previously-deployed code). `git reset --mixed origin/master`
brought the local branch pointer up to date without touching the
already-correctly-merged working-tree file.

node --check clean after every edit throughout (39 checks total across
the round). Updated docs/CHANGELOG.txt (this entry), docs/PROJECT_PROGRESS.md,
docs/CODE_SUMMARY.md. Left data/Rope/End_Form1-01.svg (actively being
re-edited by the other concurrent session) untouched/unstaged.

--------------------------------------------------------------------------------
2026-09-03  -  06:05 AM - 06:10 AM EDT

"Set defaults" (a full settings dump baked into DEV_GROUPS' own hardcoded
values), plus investigated a direct report: "when i click Save and
refresh my settings dont stay."

Set defaults: updated 15 DEV_GROUPS defaults (ropeThickness, ropeLength,
gravityStrength, damping, ropeGrowthRate, intensityCeiling,
endEmergeDelay, endEmergeSpeed, introWaitDuration, introRiseClearOffset,
introPauseDuration, detachPauseDuration, detachTipStartScale,
detachTipGrowDuration, detachThicknessMultiplier) to match; widened 2
slider ranges whose new default fell outside the old min (Rope Length's
min 5->3 for a 3.46 default; End Emerge Speed's min 0.02->0.01 for a
0.01 default). The git-tracked settings file needed no separate write --
5 more live-deployment auto-saves landed on origin while this task was
in progress, and the LAST one turned out byte-identical to the dump
being baked in (confirmed via a full JSON diff before writing anything),
meaning the deployment had already saved this exact state; `git reset
--mixed origin/master` brought the branch pointer current without
touching the (already-correct) working-tree file.

Investigated the Save-persistence report: read through the full
save/load pipeline (`saveSettings()`, `writeSettingsViaApi()`,
`readSettingsViaApi()`, `resetSettings()`) end to end looking for a real
bug. Nearly chased a false lead: the Read tool's own display of
`readSettingsViaApi()`'s fetch path rendered it with backslashes
(`'\data\processed\...'`, which would be a broken path if real) --
directly verified against the ACTUAL file bytes via `cat -A` before
concluding anything, and confirmed the real file correctly uses forward
slashes (`/data/processed/dev-panel-settings.json`) throughout; the
backslashes were purely a rendering artifact of this session's own
Read-tool output, not real file content (matching a pattern of stray
`\` characters noticed but dismissed as harmless in COMMENT text
several times earlier this session -- this is the first time the same
artifact briefly looked like it might be a REAL bug, since it landed
inside a string literal instead of a comment; confirming against raw
bytes rather than trusting the tool's rendering is what caught it before
any unnecessary "fix"). Found no actual bug in the save/read code
itself. The 5 auto-save commits that landed on origin during this exact
investigation are themselves strong evidence the Save mechanism DOES
work end-to-end (a real commit landed each time). Most likely
explanation, not confirmed (no access to the live Vercel deployment's
own build/deploy dashboard from this environment): Vercel needs to
rebuild and redeploy after each GitHub commit before the new file
content is actually served to a fresh page load -- refreshing
immediately after seeing "Saved to repo!" could catch the PREVIOUS
deployment, before the new one finishes propagating, reading as
"my settings don't stay" even though the save itself succeeded and the
setting genuinely will be there once the new deploy is live. Reported
this to the user as a hypothesis, not a confirmed fix, along with what
was actually verified vs. what remains unconfirmed. No code change made
for this item -- nothing to fix was actually found.

node --check clean. Updated docs/CHANGELOG.txt (this entry),
docs/PROJECT_PROGRESS.md. Left data/Rope/End_Form1-01.svg (a different
concurrent session's own active edit) untouched.

2026-09-03  -  06:18 AM - 06:33 AM EDT

3 real bugs reported and fixed this round, all root-caused via direct code
tracing (not guessed at): the startup rope's occasional lag/jump, fallen
cut pieces visibly shrinking in length (not just thickness), and a long
invisible delay before the End Emerge animation becomes visible even with
Emerge Delay at 0.

(1) Startup rope lag/jump ("sometimes it lags, sometimes it jumps really
high up"): `lastTime = performance.now()` used to be set where FIXED_DT
itself is declared, well before Boot's own synchronous work (dev-panel
construction, resetSettings(), etc.) actually runs. That real wall-clock
gap landed in frame 1's own frameDt, which the MAX_FRAME_DT-capped
accumulator then "catches up" on in a burst of extra update(FIXED_DT)
ticks before the first render() -- since updateIntro()'s 'rising' phase
moves bgRope.points[0].y a fixed amount PER TICK, a compressed burst
paints as a sudden jump; a smaller/variable one reads as stutter. Fixed
by re-syncing lastTime again immediately before the first
requestAnimationFrame(loop) call, after Boot's synchronous work has
already run -- frame 1's own frameDt now reflects only a normal single
frame's gap. MAX_FRAME_DT itself untouched (still caps a genuine
backgrounded-tab catch-up).

(2) Fallen pieces shrinking in length ("i dont want them to get shorter,
just thinner. the end cap should also scale with the thickness" / "the
cut off segment begins to shrink immediately"): drawEndcap()'s endcap
graphic scaled BOTH its width and height off the same live, decaying
thicknessPx -- so as a fallen piece thinned (Piece Decay Speed, after
Piece Decay Delay), its endcap's own footprint beyond the tip shrank
right along with it, visibly reducing the piece's total length even
though its underlying points array never changed. Fixed by giving
drawEndcap() an optional 8th param, heightThicknessPx (defaults to
thicknessPx -- mainRope's own call site unchanged, so its endcap still
scales uniformly, including the intentional Detach Thickness Multiplier
growth). fallenPieces now pass piece.baseThickness (fixed at the moment
the piece fell) for this param: width still tracks live thickness
(visibly thinning) while height stays pinned to the piece's original
thickness (length no longer recedes). Live-verified by actually cutting
the rope in a local static-server browser session and watching one
fallen piece over ~18s: visibly thinner, same top-to-bottom span.

(3) End Emerge taking ~5s to become visible even with Delay=0: answered
"what is Detach Pause Duration" directly first (a duplicate of Startup
Pause Duration, used specifically for the pause after a full double-click
detach-and-regrow, not a regular partial cut -- added several rounds ago
per explicit request). Then root-caused the emerge-timing report:
tickEmerge() advanced a single emerge.progress at ONE rate
(cfg.endEmergeSpeed / EMERGE_BASE_SEC) across its whole 0..1 span, and
drawEndcap()'s own SLIDE_SHARE (0.15) treated the first 15% of that as an
invisible slide-into-position. At End Emerge Speed's current default
(0.01, set several rounds ago per an explicit "at least 5x slower"
request for the visible grow), reaching even that first 15% took
~5.25s -- Delay only removes the separate delayRemaining wait, not this
slide cost, so setting it to 0 didn't help. Same class of bug as an
earlier-fixed one (the original 50/50 slide/scale split), resurfacing
through a different mechanism: back then SLIDE_SHARE was too large; now
the shared rate got slowed for an unrelated reason (a slower VISIBLE
grow, requested separately). Fixed by decoupling the two phases' rates:
the slide portion now always advances at a fixed EMERGE_SLIDE_SHARE /
EMERGE_SLIDE_SEC (~0.3s real time regardless of Speed); only the
remaining, visible grow portion still scales with End Emerge Speed.
EMERGE_SLIDE_SHARE is now a shared top-level const so drawEndcap()'s own
SLIDE_SHARE can't drift out of sync with it again.

node --check clean after each edit. Verified live in a local
dicko-clicko-static preview (python http.server via .claude/launch.json):
confirmed the full startup sequence renders correctly, performed a real
cut via simulated pointer events, and watched the fallen piece decay over
~18s (item 2, above). Did not attempt to reproduce the startup lag/jump
itself live (item 1) -- it depends on real, variable page-load timing
that isn't reliably reproducible on demand in a quick preview session;
confidence there rests on the code trace (frame 1's frameDt provably
included Boot's synchronous work before this fix, and provably doesn't
after) rather than a captured repro. Also hit the already-documented,
already-RESOLVED sandbox-only artifact (window.innerWidth/innerHeight
reading 0 on this Browser pane's very first paint, see the "RESOLVED, not
a code bug" Gotcha) during the first couple of reloads -- recognized as
the known issue, not a new regression, and confirmed the app fully
recovers on its own once a real viewport size is available. Updated
docs/CHANGELOG.txt (this entry), docs/CODE_SUMMARY.md (3 new Gotchas).
Left data/Rope/End_Form1-01.svg (a different concurrent session's own
active edit) untouched.

2026-09-03  -  06:51 AM - 07:00 AM EDT

Investigated "the startup animation is still weird. sometimes the rope
comes from above, sometimes i see the background rope jump up past the
top edge of the circle, then come back down. i think it has something to
do with the startup rise clear offset" -- plus a request for a checkbox
to visualize that offset.

Root-caused via direct math trace, then confirmed visually with the new
debug overlay before concluding: updateIntro()'s 'rising' phase computed
its climb target (clearY) from Startup Rise Clear Offset alone, with no
regard for anchorBoundaryRadius() -- the radius integrateChain()'s own
boundaryConstraint actually confines mainRope's anchor to, every frame.
At the live default Offset (-18.5), the raw target sits ~29%vmin from
circleAnchor() against an ~8.75%vmin boundary radius at default Circle
Size/Circle Boundary Offset. bgRope's own rope-start (fully unconstrained
during 'rising') climbed all the way there, then the existing handoff
code set mainRope's anchor AND its oldx/oldy equal to that same
far-outside position, intending zero implied velocity. The very next
integrateChain() call's boundaryConstraint clamps position back inside
the radius WITHOUT touching oldx/oldy (a deliberate, already-correct
design for the constraint's normal case -- see its own comment), so
oldx/oldy was left holding the far-outside spawn position while x/y held
the clamped one. The FOLLOWING frame's verlet step then computed a real
implied velocity from that mismatch and flung the anchor hard in the
correction's own direction -- confirmed large enough to overshoot past
the boundary's opposite side before damping settled it back, exactly
"jump up past the top edge... then come back down".

Fixed by clamping clearY itself (the 'rising' phase's own climb target)
to the anchor's real boundary, so bgRope's visible climb now stops
exactly where mainRope's anchor can actually rest -- the existing
handoff code's own "zero implied velocity" assignment (already correct
as written) is now genuinely true on the following frame too, not
silently invalidated by a same-frame position clamp it didn't know
about. Worth flagging directly: this means an Offset large enough to
target beyond the boundary no longer visibly extends the climb any
further -- it never actually could without reproducing this bug; Circle
Boundary Offset (or Circle Size) is what actually governs how far the
anchor, and therefore the visible climb, can range from center.

Added the requested "Debug: Show Rise Clear Offset Line" checkbox (off
by default, STARTUP ANIMATION group): draws a dashed magenta line at the
RAW (uncapped) offset target and a dashed cyan circle at
anchorBoundaryRadius(), rendered regardless of introPhase so it's usable
for tuning Circle Boundary Offset/Circle Size after the animation has
already finished, not just during it.

Live-verified in the local dicko-clicko-static preview: enabled the
debug checkbox and confirmed the cyan boundary circle sits visibly
inside the drawn circle's own bottom edge while the magenta target line
sits far below it -- visual proof of the exact mismatch driving the bug.
Reloaded fresh and watched the actual startup sequence: the anchor
settles to a stable position with no lingering oscillation across
repeated screenshots. Did not capture a frame-by-frame video of the
handoff moment itself -- this sandbox runs the fixed-timestep loop at an
effectively very high real-world rate (300fps reported by the panel's
own counter), so the whole wait+climb+settle sequence completes within a
fraction of a real second, faster than screenshot polling can reliably
straddle; confidence rests on the end-to-end stable result plus the
math trace, not a captured mid-transition frame.

node --check clean after each edit. Updated docs/CHANGELOG.txt (this
entry), docs/CODE_SUMMARY.md (1 new Gotcha). Left data/Rope/
End_Form1-01.svg (a different concurrent session's own active edit)
untouched.

================================================================================
APPEND-ONLY. Never rewrite, reorder, or delete a prior entry, even to fix
formatting (CLAUDE.md §4a / Never-Skip rule #3). If an entry needs a
correction, add a new entry noting the correction rather than editing the
old one in place.

2026-09-03  -  07:35 AM - 07:58 AM EDT

Follow-up report on the previous round's own fix: "no its sitll bad. on
startup this is what i see [screenshot: a straight, motionless rope
hanging from the circle]. it stays this way for 5-10 seconds then it
settles toteh position i expect."

The previous round's clamp fix (capping the 'rising' phase's own climb
target to the anchor's physics boundary) correctly stopped the OVERSHOOT/
bounce, but exposed a second, deeper bug: makeChain() builds every point
at the exact same x with oldx===x (zero implied velocity), so a freshly-
reset mainRope starts perfectly vertically symmetric. With the anchor now
landing exactly at the boundary's own bottom-most point, gravity pulling
straight down and the boundary clamp pushing straight back up along that
SAME line every frame meant nothing ever introduced a net lateral force
to break the symmetry -- confirmed via a temporary window.__debug hook
(added, used, then fully removed before this commit): mainRope's anchor
sat frozen at EXACTLY its boundary radius for 10+ real seconds with zero
net motion, every point's x identical to circleAnchor().x. On a real
device, ordinary floating-point/timing noise eventually perturbs it
enough to escape (explaining the reported, variable 5-10s), but nothing
guaranteed that happened promptly, or at all.

Two fix attempts were tried and MEASURED to fail before landing on the
real one (documented in full, including the actual measured numbers, in
docs/CODE_SUMMARY.md -- not repeated here): (1) a one-frame nudge at the
handoff itself, which was already fighting seconds of settled inertia
across the whole chain by the time it ran; (2) a small nudge inside
makeChain() (max 3% of segLen), which was real but far too small to
survive the damping (0.984/tick) between construction and when mainRope
is ever inspected. The actual fix: makeChain() now gives every point a
substantial per-point random x lean (segLen * 0.4 range) at construction
time, confirmed via direct fresh-construction inspection to produce real,
meaningfully-sized offsets. This gives mainRope a real, deterministic
reason to settle off-axis from frame one, with the entire hidden
wait+rise+pause duration available to do so naturally before it's ever
shown -- fixing the construction means there's nothing left for a late
nudge to fight.

Verification note, disclosed honestly rather than overclaimed: this
sandbox's own frame timing runs at an effectively extreme, accelerated
rate (hundreds of fps reported by the panel's own counter) -- repeated
live tracing showed even a large, deliberate manual kick fully decaying
back to floating-point-zero within what this environment reports as only
~100ms of real elapsed time, making a direct screenshot-based observation
of the fix's own settle animation unreliable here. Verification instead
rests on: direct confirmation the lean mechanism produces real,
substantial displacement at construction (measured, not assumed); the
physics math connecting FIXED_DT (1/60, a genuine unaccelerated code
constant) and damping to an estimated real-world settle time (~2.5-3.5s
to decay to a barely-perceptible ~1px amplitude at 60Hz -- the right
order of magnitude for "5-10 seconds", not claimed as an exact match);
and a normal screenshot confirming the final resting pose and everything
else renders identically to before, no regression. This does NOT claim
to have watched the actual settle motion play out in real time.

node --check clean after each edit. Updated docs/CHANGELOG.txt (this
entry), docs/CODE_SUMMARY.md (1 new Gotcha, documenting both failed
attempts with their measured numbers, not just the final fix). Left
data/Rope/End_Form1-01.svg (a different concurrent session's own active
edit) untouched.

================================================================================
APPEND-ONLY. Never rewrite, reorder, or delete a prior entry, even to fix
formatting (CLAUDE.md §4a / Never-Skip rule #3). If an entry needs a
correction, add a new entry noting the correction rather than editing the
old one in place.

2026-09-03  -  08:46 AM - 08:52 AM EDT

Follow-up: "still no good... It starts from startup where you see the
background rope act weird, then it disappears after 5-8 seconds. I cut it
3 times to show what happens. Its basically the same where the back rope
i coming from the top or something." Also: "i expected the startup rise
clear offset to be an offset of the blue circle inwards." No screen
recording actually reached this session (text description only) -- flagged
that honestly rather than acting as if it had been reviewed.

Found a real, separate bug via direct code reading, not guessing:
detachEntireRopeAndRestartIntro() (the double-click full-detach path)
never called resetBgRope(), unlike Boot's own version of this exact
restart sequence -- Boot pairs resetMainRope()+resetBgRope() together;
this function only ever called resetMainRope(). updateIntro()'s 'rising'
phase explicitly repositions bgRope.points[0] alone to the fresh "below
the circle" start the moment 'rising' begins, but every OTHER point in
bgRope's chain was left wherever it had last settled during normal
'done'-phase operation (following mainRope's real anchor around). The
instant point 0 snapped to its new spot while the rest of the chain
stayed stale and potentially far away (e.g. wherever the rope was
swinging when double-clicked), the constraint solver spent several real
seconds violently whipping those stale points back into a normal shape --
matching "acts weird" for "5-8 seconds", "coming from the top or
something", and reproducing reliably on every repeated cut/detach.

Fixed by adding resetBgRope() right alongside resetMainRope() in
detachEntireRopeAndRestartIntro(), matching Boot's own pairing.
Live-verified via a temporary debug hook (removed before commit): manually
swung mainRope's anchor 150px sideways, let bgRope sync to that stale
position, triggered a detach, and measured bgRope's chain once 'rising'
began -- X-spread across all points measured 26.5px, a normal compact
hanging shape, not a scattered stale one.

Re: "offset... inwards" -- checked the sign convention directly: positive
Startup Rise Clear Offset already moves the climb target INWARD (toward
center), negative moves it outward (below the circle edge). This hasn't
changed. The LIVE value has been through several rounds of manual tuning
this session and is currently deep in negative/outward territory, which
-- combined with the earlier round's boundary clamp fix -- means it's
likely sitting at a value that's functionally maxed-out-outward (clamped)
rather than doing anything meaningfully different from a smaller negative
value. Did not change the live value or the slider default -- explained
the convention to the user directly instead of guessing at what number
they'd want, since this is their own actively-tuned setting.

node --check clean. Live-verified via a temporary window.__debug hook,
fully removed before this commit (confirmed via grep). Updated
docs/CHANGELOG.txt (this entry), docs/CODE_SUMMARY.md (1 new Gotcha).
Left data/Rope/End_Form1-01.svg (a different concurrent session's own
active edit) untouched.

================================================================================
APPEND-ONLY. Never rewrite, reorder, or delete a prior entry, even to fix
formatting (CLAUDE.md §4a / Never-Skip rule #3). If an entry needs a
correction, add a new entry noting the correction rather than editing the
old one in place.

2026-09-03  -  08:54 AM - 09:05 AM EDT

Follow-up: user pointed to J:\CLAUDE\PROJECTS\DICKOCLICKO\datalog\ where
their screen recording actually was (it hadn't reached this session as an
attachment). Played it directly in a Browser-pane tab via a local <video>
element (no video-reading tool available), scrubbing with
currentTime/seeked since there's no way to read frames otherwise.

Found the real root cause of "the back rope i coming from the top or
something" by watching the recording, not by guessing again: at ~0.95s
into a fresh startup, a small endcap-shaped blob sits near the TOP of the
circle, well inside it -- not hanging below the bottom edge as intended.
Traced this to anchorBoundaryRadius() having circleBoundaryOffset's sign
backwards relative to its OWN documented comment ("positive shrinks the
playable interior, negative lets the anchor bulge past the drawn circle's
own edge"): the formula ADDED the offset, so the live negative default
(-2) actually SHRANK the radius (8.75%vmin against a 10.75%vmin circle),
the opposite of "bulge past the edge". This is harmless once the rope has
grown long enough to hang down and out past the anchor regardless -- but
during the brief early-growth window, the whole tiny rope+endcap sat
entirely inside the circle's interior, looking like it emerges from near
the top.

Fixed by subtracting circleBoundaryOffset instead of adding it, so the
current negative default now correctly computes a 12.75%vmin radius
(bulging past the edge) instead of 8.75%vmin (confined inside it).
Verified 2 ways: (1) a temporary debug hook (removed before commit)
confirmed the live-computed radius exactly matches the hand-derived
expected value; (2) the existing "Debug: Show Rise Clear Offset Line"
checkbox's own boundary circle now visibly bulges past the drawn circle's
edge instead of sitting entirely inside it -- a genuine visual
confirmation, not just a number matching another number. This same
function backs both of the previous 2 rounds' own fixes (the rise-clear-
offset clamp; the boundary clamp inside integrateChain()) -- neither of
those was wrong, they were correctly clamping to whatever radius this
function returned; the radius itself didn't match its own documented
intent.

Re-confirmed (no change from the last round): Startup Rise Clear Offset's
positive direction already means inward. Left the user's own live value
untouched.

node --check clean. Live-verified via a temporary window.__debug hook,
fully removed before this commit (confirmed via grep). Updated
docs/CHANGELOG.txt (this entry), docs/CODE_SUMMARY.md (1 new Gotcha).
Left data/Rope/End_Form1-01.svg (a different concurrent session's own
active edit) untouched.

================================================================================
APPEND-ONLY. Never rewrite, reorder, or delete a prior entry, even to fix
formatting (CLAUDE.md §4a / Never-Skip rule #3). If an entry needs a
correction, add a new entry noting the correction rather than editing the
old one in place.

2026-09-03  -  11:41 PM - 11:57 PM EDT

Follow-up: "still not fixed. same issue. can you check my saved setting
defaults to see if anything is causing it on my end" with a screenshot
matching the earlier one exactly, plus the full live settings.json pasted
in. A cluster of further requests arrived interleaved: "cut off rope
segments... should only get thinner not shorter" (re-confirmation, not a
new ask), "the color is still incorrect... instead of fixing it, give me
the same slider but for the endcaps", "i want cut off rope segments to
retain the gradient", "right click on a slider toggle deletes it" /
"as in thats what i want", "the dark flash near the rope start is still
visible... occurs when i do a long click and hold flick from the bottom",
and "i still see a seam on top of my endcap".

Root-caused the "still not fixed" report with real numbers, not another
guess: fetched their live settings and computed the actual pixel geometry
-- at ropeThickness=10, endcapHeight=1.45 and their endcap design, the
endcap graphic itself renders ~92.8px tall, but their ropeLength=4.21%vh
produces a total rope length of only ~29.4px inside a ~134.6px circle.
The endcap alone is over 3x the entire rope's own length, and drawEndcap()
draws its full natural height unconditionally once emerged -- nothing
clips it to the rope's actual length. Confirmed directly (not just
calculated) by regrowing the same live rope to 25%vh via a temporary
debug hook: the endcap then sat entirely below the circle exactly as
intended, proving the PREVIOUS round's anchor-boundary sign fix is
genuinely correct and this is a proportions issue in the current
settings, not a residual code bug. Also A/B tested endcapHeight 1.0 vs
1.45 live and found no meaningful difference -- the endcap is already
oversized relative to the rope even unstretched. No code change for this
finding; reported the exact numbers instead of another guess.

Implemented 3 concrete, verified features per explicit request:
(1) A new, independent Endcap Gradient (endcapGradientEnabled/Colors),
separate from the existing Rope Gradient -- drawEndcap()'s own
gradientInfo now reads the new config instead of always reusing the
rope's, for mainRope's endcap AND every fallen piece's endcap (both the
tip end and the separate cut-end endcap).
(2) Fallen pieces now retain gradient coloring instead of reverting to
solid cfg.ropeColor the instant they fall -- their own stroke color is
now ropeStrokeColor(piece.points, ...), recomputed each frame from the
piece's own current span, same convention mainRope's stroke already used.
(3) Right-click (contextmenu) now deletes a gradient stop, alongside the
existing double-click -- both call the same shared deleteStop().
Live-verified all 3 together via a temporary debug hook (removed before
commit): pushed a piece copied from the live rope with the rope gradient
set to red/blue and the endcap gradient independently set to
green/yellow -- the piece rendered with a red-to-blue stroke AND a
SEPARATE green-to-yellow endcap, confirming neither gradient silently
overrides the other. Right-click delete verified via real dispatched
DOM events (PointerEvent to add a 3rd stop, MouseEvent 'contextmenu' to
remove it), not just static analysis.

Re: "seam on top of my endcap" -- same already-diagnosed, already-
attempted-and-failed form1-01 SVG asset issue from earlier this session;
did not re-attempt the same blind path-edit a second time. Not yet
investigated this round: the dark-flash-during-a-hard-flick report --
now has a much more specific repro ("long click and hold flick from the
bottom") than the earlier vague description, worth a dedicated pass next
round rather than a rushed guess at the end of an already-large one.

node --check clean after every edit. Updated docs/CHANGELOG.txt (this
entry), docs/CODE_SUMMARY.md (3 new Gotchas). Left data/Rope/
End_Form1-01.svg (a different concurrent session's own active edit)
untouched.

================================================================================
APPEND-ONLY. Never rewrite, reorder, or delete a prior entry, even to fix
formatting (CLAUDE.md §4a / Never-Skip rule #3). If an entry needs a
correction, add a new entry noting the correction rather than editing the
old one in place.

2026-09-04  -  12:00 AM - 12:03 AM EDT

"Dev panel collapse button simply collapse the setting options but not
the panel."

Root-caused directly: the #devPanel.dp-collapsed CSS rule only ever
hides .dp-body/.dp-resize (the settings list and resize handles) -- it
never touched #devPanel's own explicit inline height, set either by
dragging a resize handle or restored from saved settings via
applyPanelGeometry(). That explicit height persisted across the
collapse toggle, so the panel's own outer box never actually shrank,
leaving a big empty gap below the title bar exactly as reported.

Fixed in the collapse button's click handler: collapsing now saves the
panel's current style.height to a dataset attribute and clears it (so
the box auto-sizes to just the title bar); expanding restores the saved
value. Width is untouched in both directions -- only height collapses.
Live-verified via real clicks and screenshots (not just code reading):
collapsing now visibly shrinks the panel to just its title bar, and
expanding restores the exact same height it had before.

node --check clean. Updated docs/CHANGELOG.txt (this entry),
docs/CODE_SUMMARY.md (1 new Gotcha). Left data/Rope/End_Form1-01.svg (a
different concurrent session's own active edit) untouched.

================================================================================
APPEND-ONLY. Never rewrite, reorder, or delete a prior entry, even to fix
formatting (CLAUDE.md §4a / Never-Skip rule #3). If an entry needs a
correction, add a new entry noting the correction rather than editing the
old one in place.

2026-09-04  -  12:04 AM - 12:16 AM EDT

"fix the flash too. investigate it" and "the panel resizing in mobile is
still buggy" -- 2 separate items, both root-caused via direct evidence
(frame-by-frame physics tracing; CSS property comparison against known-
working handles), neither guessed at.

(1) Dark flash: applyPunch()'s displacement (power = vh(1.0) * intensity)
is an absolute value, independent of the rope's own scale. For a short
rope, a strong hold-and-release can displace a point several times its
own segment length in one shot, and since the falloff spreads a real
kick across neighboring points too, this can fling the tip clean past
its own neighbor or even the anchor into an inverted configuration.
Traced this exactly via a temporary debug hook manually stepping
update() 15 ticks after a simulated flick (removed before commit): the
anchor -- excluded from the punch directly -- still got dragged by the
ordinary distance constraint through a wild swing, briefly landing much
closer to the circle's center than its normal resting distance.
tipDirection() (which drawEndcap() rotates the endcap by) genuinely
flip-flopped roughly 180 degrees tick to tick during that recovery, not
a smooth rotation -- confirmed visually too, with a stark green/black
endcap gradient showing exactly this chaotic spin sweeping the dark end
across the endcap for a few frames. Fixed by capping applyPunch()'s
power at segLen * 4 (a new explicit segLen param, threaded through both
real call sites). Re-ran the identical trace after the fix: the anchor's
distance from center now stays tightly bounded and tipDirection() changes
smoothly instead of flip-flopping -- still a real, visible kick, just no
longer a physically-invalid teleport-and-chaotic-recovery.

(2) Mobile panel resize: .dp-resize was the one draggable-handle class
in the whole panel missing touch-action:none (every other handle already
has it). Without it, a real finger-drag on a resize handle can get
hijacked by the browser's own native scroll/pan gesture recognition
before JS ever sees a clean pointermove sequence -- invisible to both
desktop mouse testing and to synthetic PointerEvents dispatched directly
in JS (confirmed directly: synthetic drag tests against all 3 handle
types, including while the settings list was mid-scroll, all resized the
panel exactly as expected, reproducing nothing). Fixed by adding
touch-action:none to the shared .dp-resize rule, matching every other
handle; verified via getComputedStyle() post-edit.

node --check clean (JS portion). Updated docs/CHANGELOG.txt (this entry),
docs/CODE_SUMMARY.md (2 new Gotchas). Left data/Rope/End_Form1-01.svg (a
different concurrent session's own active edit) untouched.

--------------------------------------------------------------------------------
2026-09-04  -  12:24 AM EDT

Fixed a real bug reported directly from a real phone on the live Vercel
deployment: "I just texted it on mobile on vercel. And the start button
appears then disappears after a second so I can't start game."

CORRECTION to an earlier entry in this file, which concluded the
"mainRope.points/bgRope.points empty at boot, every update() frame
throwing" crash was a sandbox-only artifact of how this dev tool opens a
fresh preview tab, and that "a real browser/deployment load always has a
valid viewport by the time scripts run." That assumption was never
actually tested against a real device -- only against this same sandbox
tool's own preview_start vs. navigate'd tabs. The user's real-phone
report, reaching this session with no knowledge of that earlier
conclusion, is independent corroborating evidence that the underlying
condition (window.innerWidth/innerHeight reading 0 or otherwise
unreliable very early in page life) is a real, well-documented category
of mobile Safari/Chrome viewport-timing quirk, not exclusive to this
sandbox.

Fixed at the actual root: added viewportW()/viewportH() helpers (falling
back through window.innerWidth/innerHeight -> document.documentElement.
clientWidth/clientHeight -> a hardcoded sane default, in that order) and
routed vw()/vh()/vmin() AND resizeCanvas() (which had the same raw
window.inner* exposure, previously unprotected) through them. The
fallback only ever engages when the real value is falsy, so this changes
nothing about normal operation on a device where the viewport reads
correctly -- purely additive robustness. Moved the whole viewport-helper
block to before the Canvas-setup section, since resizeCanvas() runs
synchronously at boot and needs it too.

Root-caused with real instrumentation, not assumed: a temporary
console.log right after the boot-time resetMainRope() call confirmed
window.innerHeight === 0 at that exact line on a real page load, which
made vh(TARGET_SEG_LEN_VH) evaluate to 0, making resetMainRope()'s
totalLen/targetSeg divide 0/0 = NaN, and Math.max(2, NaN) returns NaN
(not 2 -- Math.max does not clamp NaN operands back to the other
argument), so makeChain(NaN, ...)'s for-loop (i < NaN is always false)
produced a genuinely empty array. Removed the temporary instrumentation
before committing.

Verified via a fresh reload in this same sandbox (which itself still
reads window.innerWidth/innerHeight as 0, confirming the underlying
zero-read condition is real and reproducible, not something a resize
event self-corrects the way the earlier entry assumed): mainRope.points/
bgRope.points now correctly hold 2 points immediately after boot
(previously 0), and a full 300-frame update() run through the entire
intro sequence (waiting -> growing -> done) completed with 0 thrown
errors and a correctly-rendered circle+rope on screen -- vs. reliably
crashing every single frame before the fix, confirmed via
read_console_messages on a completely untouched reload both before and
after.

Also handled a deferred request from 2 turns ago ("i just trimmed the
top of form1-1. dont do anything yet, but the next time i prompt you.
put that in there too"): the form1-01.svg the user actually saved this
time has NO shape at all -- only the shared ANCHOR_LINES reference line,
no <path> or any other drawable element. Flagged directly rather than
incorporated (would render as nothing); ENDCAP_DESIGNS['form1-01'] left
at its last real (still seam-affected, per 2 rounds ago's diagnosis)
path data.

A separate request this same turn ("set the mobile click and animation
and behavior settings to be the same as desktop") was investigated first
(confirmed: no per-device value split exists for any gameplay setting --
only panelGeometry, the dev panel's own chrome, is split per device) and
then explicitly withdrawn by the user in favor of the mobile bug report
above -- no change made for that request.

node --check clean. Updated docs/CODE_SUMMARY.md (1 new Gotcha,
correcting the earlier "not a code bug" one) and docs/PROJECT_PROGRESS.md
(Open Questions entry corrected in place; new Recently Completed bullet).

--------------------------------------------------------------------------------
2026-09-04  -  12:57 AM EDT

3-item follow-up: Circle Offset investigated (no change), Startup Rise
Clear Offset redesigned as a circle, endcap gradient fixed to be
self-contained.

"Ok you misundertsood me about the offset circle. Revert the Anchor
Physics Circle Offset back to how it behaved previously. it was behaving
correctly." Investigated before reverting anything: anchorBoundaryRadius()'s
own doc comment ("positive shrinks the interior, negative bulges past the
edge") only actually holds true with the CURRENT subtraction formula --
the original addition formula has that relationship backwards relative to
its own stated intent, and reverting to it would likely reopen a
confirmed, screen-recording-verified bug (rope spawning near the top,
inside the circle) the subtraction fix resolved. Surfaced this conflict
directly via AskUserQuestion rather than guessing -- confirmed: keep the
current subtraction behavior. No code change.

"What i meant was that the Startup Animation Startup Clear Offset
boundary should not be a straight horizontal line as it is right now, but
is in fact an offset of the Anchor Physics Circle Offset so it will be a
smaller circle." Fixed: both updateIntro()'s own 'rising'-phase target
and the debugShowRiseClearOffset visualization previously measured from
the raw drawn circle's own circleSize/2, entirely ignoring
circleBoundaryOffset. Now derived from anchorBoundaryRadius() minus
vmin(introRiseClearOffset) -- a further offset FROM the anchor's own,
already-Circle-Offset-adjusted boundary radius. The debug visualization
(renamed "Debug: Show Rise Clear Offset Circle") now draws an actual
dashed circle instead of a full-width horizontal line. Verified via a
full 300-frame update() run through the entire intro sequence
(waiting->growing->done, 0 thrown errors) and a screenshot showing a
smaller pink dashed circle correctly nested inside the cyan
anchor-boundary circle.

"And the End cap gradient application is weird. WHen the rope is in
default equilibrium, the endcap is only the color at the 100% of the
slider. Then when i flcik it, i see more of the other colors." Root-
caused by tracing drawEndcap()'s gradient branch against
ropeStrokeColor() (the rope's own stroke gradient, which the endcap was
built to deliberately match): both built a linear gradient from the
WORLD-SPACE straight-line distance between mainRope.points[0] (anchor)
and the tip -- correct for the rope's own full-length stroke, but the
endcap is a small, fixed-size feature at just the very end of that span.
At equilibrium (a long, straight rope), that world distance, mapped
through the endcap's own small-scale local inverse-transform, lands the
gradient's "top" stop far outside the endcap's own local shape bounds --
so nearly the whole endcap sampled from inside the gradient's clamped
LAST-stop color. Flicking/curling shrinks the straight-line anchor-to-tip
distance (a curled path's endpoints sit closer together than its unrolled
length), pulling the mapped "top" stop back toward the endcap's own local
bounds and revealing more of the gradient's range -- a "color changes
when you flick it" symptom that isn't a real physical property of the
rope. Presented 2 fix directions (self-contained endcap gradient vs.
stabilizing the existing whole-rope one via arc length instead of
straight-line distance) rather than picking one silently; the user chose
self-contained. Rebuilt entirely in the endcap's own local coordinate
space (ENDCAP_ALIGNMENT.topY -> designBottomY, no inverse-transform
mapping needed, since both are already-known local constants), with the
stops array used REVERSED (pos' = 1-pos): the endcap's neck lands on
whatever color the original gradient's LAST stop is (matching the rope's
own tip color in the common long-rope case, so still no hard seam), while
the endcap's own far end transitions back toward the gradient's FIRST
stop's color. Verified via direct ctx.getImageData() pixel sampling: a
synthetic long/straight 8-point rope and a synthetic short/curled
2-point rope, both fed through the identical drawEndcap() call, produced
pixel-identical gradient values at every sampled point along the endcap
-- directly confirming the gradient is now fully independent of the
rope's total length or current pose.

node --check clean throughout. Temporary debug hooks (window.__debugTMPoffset,
window.__debugTMPgrad) fully removed and grep-confirmed 0 references
before commit. Updated docs/CODE_SUMMARY.md (2 new Gotchas) and
docs/PROJECT_PROGRESS.md (3 new Recently Completed bullets).

--------------------------------------------------------------------------------
2026-09-04  -  02:01 AM EDT

Explained the Startup Animation's full state machine in plain terms
before making any changes, per direct request ("before you do anything,
can you explain to me in very basic form what our startup animation
is"). Then added 2 new gating checkboxes per explicit follow-up requests.

Added "Startup Rise Clear Offset Enabled" (checkbox, def false per
explicit "set it to off by default for now"): gates whether
cfg.introRiseClearOffset's own value is actually applied, in both
updateIntro()'s own 'rising'-phase trigger and the
debugShowRiseClearOffset visualization. When off, rawClearRadius
collapses to exactly anchorBoundaryRadius() (offset treated as 0), so
the climb simply clears at the anchor's own real physics boundary with
no extra offset. Verified via a full 300-frame update() run through the
entire intro sequence with the new default -- 0 thrown errors.

Added "Background Rope Clip Enabled" (checkbox, def true, matching the
always-on behavior every previous round already had): gates the
ctx.clip() block that confines bgRope's own rendering to the circle's
bounds, per explicit request ("give me a checkbox to turn off the
background rope circle masking clipping region... so i can see the
whole background rope"). When off, the same strokeRopeCurve() call runs
directly, unclipped. Verified precisely rather than by screenshot alone
(mainRope and bgRope both rendering made visual comparison ambiguous):
manually positioned bgRope 150px past the circle's edge, moved mainRope
off-screen to isolate the test, then sampled the same pixel via
ctx.getImageData() with the toggle on and off -- [0,0,0,0] (transparent)
with the clip on, [165,129,95,255] (real rope color) with it off, at the
identical coordinate.

node --check clean throughout. Temporary debug hook (window.__debugTMPchk)
fully removed and grep-confirmed 0 references before commit. Updated
docs/CODE_SUMMARY.md (1 new Gotcha) and docs/PROJECT_PROGRESS.md
(2 new Recently Completed bullets).

--------------------------------------------------------------------------------
2026-09-04  -  05:13 AM EDT

Large follow-up round: found and fixed a foundational bug that resolved
3 separately-reported symptoms at once, then worked through 6 more items
from the same message.

Started investigating "double clicking the circle still does nothing but
double clicking the rope start end seems to do what i want double
clicking the circle to do." Direct instrumentation (temporary
console.log calls inside integrateChain()'s boundary-clamp and bracketing
update()'s own mainRope code) found mainRope.points[0] genuinely frozen
at (200,-48), 192px from circleAnchor() -- nearly 4x past its own 51px
anchorBoundaryRadius() -- across 120+ update() calls with zero movement,
and not just the anchor: the ENTIRE chain was frozen. Traced the exact
mechanism: integrateChain()'s boundary clamp WAS firing correctly each
frame (confirmed: bp.y went from -47.47 pre-clamp to a correctly-clamped
93 immediately after), but by the end of that same update() call,
mainRope.points[0].y was back to -48 -- with floorY() ALSO logging -48 at
that exact point. Root cause: floorY() still read raw
window.innerHeight directly, never routed through the viewportW()/
viewportH() fallback helpers added 2 rounds ago specifically to guard
against this exact class of bug. With window.innerHeight reading 0 (a
real, reproducible condition, not sandbox-only -- see that round's own
correction), floorY() computed floor = 0 - vh(floorHeight) = -48,
placing the "floor" far ABOVE where the anchor correctly settles (93) --
so the floor-collision block (which runs AFTER integrateChain(), later
in the same update() call) clamped 93 back down to -48 every single
frame, discarding the anchor's own correct result before the frame ever
finished.

Comprehensively re-audited for the same bug class rather than patching
only the one reported call site: found and fixed 6 more raw
window.innerWidth/innerHeight reads -- floorY() itself; 2 setCfg
('ropeLength', mainRope.totalLength / window.innerHeight * 100) calls (a
genuine divide-by-zero risk, not just a wrong value, whenever
window.innerHeight is 0); the fallen-pieces cleanup filter (would have
wrongly discarded a piece resting near y=93, since 93 < 0 is false); the
background and floor fillRect calls in render() (a 0-sized fillRect
paints nothing); and 3 dev-panel drag/resize viewport-clamp sites (lower
risk, UI chrome only, fixed for completeness). All now route through
viewportW()/viewportH().

This single fix resolved 3 independently-reported symptoms with no
further code changes, each individually re-verified after the fix:
- Double-click-in-circle: the anchor settling far from the circle meant
  clicks aimed at the circle's own drawn position never found a nearby
  rope point. Now settles at exactly anchorBoundaryRadius(); a scripted
  double-click there correctly produces a 7->2 point reset plus a new
  fallen piece (the full detach sequence).
- Click-and-hold-to-grow: growRope() itself was never broken (confirmed
  by forcing growing=true directly before finding the root cause, which
  grew the rope normally) -- the real blocker was that the anchor's
  corrupted position meant a click aimed at the visible circle graphic no
  longer registered as isOnCircle(), so the hold-to-grow timer never
  started. Re-verified via a REAL onPointerDown + a genuine 2-second
  wall-clock wait for the actual setTimeout to fire + onPointerUp:
  growing correctly became true, length accumulated over the hold
  (253.69 -> 349.69), and growing correctly reset to false on release.
- (A 3rd item, the underlying anchor-freeze itself, was the original
  investigation target.)

Then worked through the rest of the same message:

Removed Circle Cut Distance entirely ("i dont know what it does") --
cutRopeAt()'s anchor-proximity exclusion is gone; the minimum-remaining-
length check is unaffected. Added a new "Double Click Distance" slider,
separate from Click Distance ("give me a double click distance slider...
so i should have both click and double click distance sliders"):
hitTestRope()/hitTestPieces()/hitTestAny()/hitTestAnyIgnoringCircle() all
gained an optional maxDist param (defaulting to Click Distance, so every
existing call site needed zero changes); only the 2 call sites that
RESOLVE a completed double-click now pass Double Click Distance
explicitly. Verified: cutting a point immediately next to the anchor
(previously always rejected) now succeeds.

Investigated End Emerge Delay/Speed/Easing Strength ("Delay does
nothing... Easing Strength seems to be doing what Speed should do") --
all 3 are correct in code; checked the live values directly
(Delay=0, Speed=0.02, Easing Strength=6, the slider's own max) and
computed the actual easeInOut(t,6) curve: the visible transition
compresses into roughly the middle 30% of an ~17.8s total duration, with
the first ~5.3s and last ~3.6s looking essentially static. This fully
explains the reported confusion without any code being wrong -- reported
back with the specific numbers rather than changing defaults that may
have been deliberately set this extreme.

Re-verified Startup Pause Duration's mechanism directly (a 1.5s test
value via detachEntireRopeAndRestartIntro()'s own reuse of this state
machine correctly produced a ~1.5s 'pausing' phase) -- the live
introPauseDuration is just 0, not broken.

Explained the Detach Pause Duration / Detach Endcap Start Scale / Detach
Endcap Grow Duration / Detach Thickness Multiplier sliders directly to
the user (Detach Pause Duration is Startup Pause Duration's own
counterpart for the double-click-triggered full-rope detach; the Endcap
Start Scale/Grow Duration pair is a separate, simpler linear ramp for
the regrown rope's tip, independent of End Emerge; Thickness Multiplier
compounds on every detach).

Reconfirmed the still-visible endcap/rope seam is the SAME form1-01
neck-geometry issue diagnosed 3 rounds ago (live endcapDesign is still
form1-01), not a new bug -- neither of the user's 2 edit attempts so far
fixed the underlying flat-neck issue.

Deferred, not attempted this round: a dev-panel request to drag a
setting from one collapsible group into a different group -- flagged as
a genuinely separate, moderately-sized UI feature rather than folded
into an already-large bug-fixing round.

node --check clean throughout. Temporary debug hooks and all instrumentation
(window.__debugTMPdbl, __TMPBCLOG console.log calls) fully removed and
grep-confirmed 0 references before commit. Updated docs/CODE_SUMMARY.md
(4 new Gotchas) and docs/PROJECT_PROGRESS.md (6 new Recently Completed
bullets, 1 new What's Next item).

--------------------------------------------------------------------------------
2026-09-04  -  06:25 AM EDT

Added the FLICK animation, per explicit request: "these are frames of a
looping animation. it will loop from frame 1 to 17 and back and repeat.
Place this animation as a small graphic in the bottom left of the
browser," plus X/Y position, scale, and speed sliders.

The 17 source frames (data/FLICK/ANI/FRAMES-01.png through FRAMES-17.png,
~500-590KB each, ~9MB total) are referenced by relative path rather than
embedded as base64 data URIs -- the one deliberate exception to this
project's single-file convention, since inlining ~9MB of images with ~33%
base64 overhead on top would bloat the HTML substantially for no benefit.

Implemented as a self-contained addition, following the project's own
existing "direct per-frame cfg read, no onChange needed" pattern used by
every other tunable: preloaded all 17 frames into an `Image()` array at
boot; a new flickFrameIndex() computes the current frame via a triangle
wave over a continuously-advancing flickAnimTime accumulator (frame
position 0,1,2...16,15,14...0,1... -- a genuine ping-pong/boomerang, not a
cut-back-to-frame-1 loop, matching "loop from frame 1 to 17 and BACK");
flickAnimTime itself advances by the real (unscaled) frame dt inside
update(), independent of the rope's own ropeAnimSpeed-scaled dt. Drawn via
ctx.drawImage() at the very end of render() (on top of everything else),
sized as a %vmin width with height derived from each frame's own aspect
ratio, positioned by %vw/%vh X/Y -- matching every other position/size
slider's unit convention in this project exactly. New "FLICK ANIMATION"
dev-panel group: Flick X Pos (def 15 %vw), Flick Y Pos (def 85 %vh,
together placing it bottom-left by default as asked), Flick Scale (def 15
%vmin), Flick Anim Speed (def 1x, a multiplier on top of a 12fps baseline
playback rate).

Verified live via a local static server: all 4 sliders correctly move/
resize/speed up the graphic (confirmed via direct before/after
screenshots at default position vs. centered+enlarged+3x-speed), the
animation visibly cycles through multiple distinct hand-pose frames over
time (not stuck on one frame), and a fresh page load renders it at the
correct default bottom-left position/size with no console errors from the
new code. One environment-specific issue found and diagnosed, not a code
bug: `python -m http.server`'s single-threaded connection handling
intermittently fails several (not all, not always the same ones -- 4-9 of
17 across repeated test loads) of the 17 concurrent large-image requests
with net::ERR_CONNECTION_RESET. flickFrameIndex()'s existing
`img.complete && img.naturalWidth > 0` guard already makes this
self-healing (the animation just has occasional visible gaps locally,
confirmed by directly inspecting each frame's load state and re-testing
until enough frames loaded to observe a full cycle) -- not expected to
occur on the real Vercel deployment, which serves via a CDN rather than a
local single-threaded server. Documented in docs/CODE_SUMMARY.md's
Gotchas rather than left as an unexplained flicker.

Updated docs/PROJECT_SUMMARY.md (SCOPE, a new DECISIONS entry, DATA
SOURCES) and docs/CODE_SUMMARY.md (FILE MAP, a new GOTCHAS entry) to
match. docs/PROJECT_PROGRESS.md's "Recently completed" section has grown
far past its own "live document, not a log" convention (1000+ lines) --
flagged as a separate follow-up rather than attempting that cleanup as
part of this feature.

--------------------------------------------------------------------------------
2026-09-04  -  07:42 AM EDT

Follow-up round on the FLICK animation: bumped Flick Scale's default 15 ->
50, and added a second, independent FLICK animation per explicit request
("make a second animation, placed above the 1st one").

The user separately updated all 17 of animation 1's own source PNGs in
place (same filenames, new artwork) -- committed as part of this round.

Animation 2's source material was two raw, non-sequentially-named frame
folders (data/FLICK/FRAMESS/1 and /2 -- filenames like "8a.png"/"9b.png"
alongside plain numbers, gaps in the numbering) rather than a clean
FRAMES-NN.png sequence. Per the spec ("frames in sequence in [1], then
right after that are the frames in [2], but in reverse order"): natural-
sorted folder 1 (1,2,3,4,6,8a,9,9a,10,11,12,13,13a,14 -- 14 frames, letter
suffixes ordered right after their base number as sub-steps, the only
sensible reading of that naming with no other information to disambiguate)
followed by folder 2 natural-sorted and then REVERSED (14,9b,9a,9,8a,8,6,
5,4,3,2,1 -- 12 frames) -- 26 frames total, pre-copied into
data/FLICK/ANI2/FRAMES-01.png..FRAMES-26.png in that exact concatenated
order so the runtime loading code stays a plain sequential file list,
identical in shape to animation 1's, rather than special-casing the raw
source filenames at runtime.

Added flickFrames2/flick2AnimTime/flick2FrameIndex() alongside animation
1's existing equivalents, and a second "FLICK ANIMATION 2" dev-panel group
(Flick2 X/Y Pos, Flick2 Scale, Flick2 Anim Speed -- same %vw/%vh/%vmin/x
units as animation 1), default Y=60%vh vs. animation 1's Y=85%vh so it
sits above it on screen by default, matching the request. Unlike animation
1, this one was only asked to loop forward (1->26->1, a hard cut back to
frame 1) -- no ping-pong/boomerang was requested for it, so
flick2FrameIndex() is a plain modulo wrap, not animation 1's triangle-wave
ping-pong.

Verified live via a local static server: both animations render and cycle
independently, animation 2 visibly positioned above animation 1, all 8
sliders (4 per animation) move/resize/speed their own graphic without
affecting the other. Hit the same already-documented python -m
http.server concurrency limitation during testing (more pronounced now
with 43 total source images across both animations) -- confirmed via
direct fetch that the server and files are fine once the initial
concurrent-load burst settles; not expected on the real Vercel deployment.

================================================================================
2026-09-04  -  07:43 AM EDT

Circle Offset made a hard-clamped interior offset of the circle itself --
resolves a same-day conflict between two of the user's own confirmed
decisions.

User described the intended hierarchy unprompted: "Anchor Physics - Circle
Offset... is an interior offset. it is never bigger than the circle
itself[.] Startup Rise Clear Offset... is an interior offset. it is never
bigger than Anchor Physics - Circle Offset." Checked both claims against
the live code before changing anything. Startup Rise Clear Offset already
matched exactly -- updateIntro()'s 'rising' phase clamps clearY to
[anchor.y - boundaryR, anchor.y + boundaryR] around anchorBoundaryRadius(),
so it structurally cannot exceed its parent. Circle Offset did NOT match:
anchorBoundaryRadius() = circleSize/2 - vmin(circleBoundaryOffset) had no
ceiling, and at the live default (circleBoundaryOffset -2) computed 47 vs.
the circle's own 43 -- bulging visibly past the drawn edge.

That bulge was not an oversight -- it was a deliberate, evidence-based fix
from earlier the same morning (see the 06:25 AM entry): a near-zero-length
rope had been emerging from deep inside the circle instead of at its edge,
confirmed via the user's own screen recording, and when asked whether to
revert that fix or keep it, the user chose to keep it. The new "never
bigger than the circle" statement directly contradicted that kept
decision. Rather than silently pick a side, surfaced the conflict via
AskUserQuestion with both options and their tradeoffs spelled out. User's
answer: "Clamp to circle, but the rope start of the background and man
rope cannot ever go outside the circle. the main circle is the master
boundary. everyting else within that never breaks that rule."

Implemented exactly that: anchorBoundaryRadius() now returns
Math.max(2, Math.min(circleRadius, circleRadius - vmin(circleBoundaryOffset)))
-- only the Math.min(circleRadius, ...) ceiling is new. Verified live via a
temporary debug hook (window.__debugTMPoffset2, removed before commit,
grep-confirmed 0 references): at the default offset -2, boundRadius now
reads exactly 43, equal to circleRadius, not 47. Since Rise Clear Offset
was already nested under anchorBoundaryRadius(), it now transitively also
never exceeds the circle -- the full 3-level hierarchy (Circle > Circle
Offset > Rise Clear Offset) holds. Traded off explicitly, at the user's
own direction: a strongly negative Circle Offset can once again reproduce
the original "rope starts well inside the circle during early growth"
look from before the 06:25 AM fix, since bulging past the edge is no
longer possible under the master-boundary rule.

Verified via node --check (syntax clean) and a live reload showing the
Rise Clear Offset debug circle (already enabled in the saved dev-panel
settings) sitting exactly on the drawn circle's edge rather than outside
it.

Also explained, verbally, what the 4 Detach-prefixed sliders
(detachPauseDuration, detachTipStartScale, detachTipGrowDuration,
detachThicknessMultiplier) control -- no code changes, see the 06:25 AM
entry's own Detach-related additions for the underlying mechanism
(detachEntireRopeAndRestartIntro()).

================================================================================
2026-09-04  -  08:08 AM EDT

Detach sequence: 3 related fixes (piece falls too early, bgRope jumps up,
no independent wait duration), plus both FLICK animations converted to
click-to-trigger, play-once-then-stop.

User: "i think i se waht the issue is. The ropes shouldnt fall until the
Main rope has been spawned" + "when i double click to cut off the entire
rope, the background rope suddenly goes really high straight up", then
mid-turn: "oh i also see another issue. the main rope doesnt spawn UNTIl
the wait time is over."

Fix 1 -- held fallen piece: the piece created by
detachEntireRopeAndRestartIntro() was falling under real gravity from the
instant it was created, well before the fresh main rope actually spawned
(which can take the entire wait+rise duration). Added
`piece.waitingForSpawn = true` at creation, checked at the top of the
fallenPieces gravity loop in update() (`if (piece.waitingForSpawn)
return;` -- skips integrateChain entirely, piece stays frozen), cleared at
the exact 'rising'->'pausing' handoff in updateIntro() -- the same frame
mainRope.points[0] gets positioned, i.e. literally the moment the main
rope spawns.

Fix 2 -- bgRope jump: root-caused via direct frame-by-frame tracing
(temporary debug hooks driving update() with a fixed synthetic dt in a
tight loop + render(), since requestAnimationFrame never ticks in this
sandbox). Confirmed: when 'rising' begins, only bgRope.points[0] used to
teleport to the far-below climb-start position; points 1+ stayed wherever
they'd settled during 'waiting' (hanging normally downward from the OLD
point 0 at circleAnchor()) -- leaving the rest of the chain ABOVE the
newly-teleported point 0, an inversion gravity never produces on its own.
Traced the tail point across 5 frames right after the teleport:
199->165->147->127->106->84 (y decreasing = climbing/flinging upward),
confirming the constraint solver's correction for that inversion was
exactly the reported jump. Fixed by rebuilding bgRope's WHOLE chain at
this transition (resetBgRope() gained optional (startX, startY) params,
defaulting to circleAnchor() for its 3 other unchanged call sites) so
every point starts already consistent with gravity's direction. Verified
live: p0/p1/pLast now increase monotonically (272.7/305.9/333.5) and
change smoothly frame to frame with no inversion or whip.

Fix 3 -- Detach Wait Duration: Detach had no independent control over its
own pre-climb delay, silently reusing Startup Wait Duration unlike Pause
Duration (which already had a Detach-specific counterpart). Added
`detachWaitDuration` (def 1.4s) and branched on introTriggeredByDetach in
the 'waiting' phase, mirroring the existing pauseDuration branch.

All 3 verified together via a full synthetic detach cycle: piece held at
constant y=187 through 'waiting' and the entire 'rising' climb (935
stepped frames), bgRope chain stable throughout, piece released (y
changing, 187->189.6) at the exact frame mainRope spawned.

Separately, mid-turn: "i also see another issue" about FLICK animation 2
("when triggered it does 1 seueunce. then stops until clicked again" --
describing INTENDED behavior it didn't yet have) + "apply the clicking
function to animatio n1 as well" + "set animation speed of both to 3.2".
Animation 2 already had a click-trigger (flick2Playing/isPointInFlick2/
flick2Rect) but never actually stopped -- flick2FrameIndex() modulo-
wrapped forever once started, looping indefinitely instead of playing one
pass. Animation 1 had no click-gate at all, autoplaying its ping-pong
continuously from boot. Added the same Playing/isPointInFlick*/Rect
pattern to animation 1 (mirroring animation 2's existing shape), and for
BOTH: stop advancing AnimTime once one full sequence's cyclePos is
reached, checked in update() (kept out of the *FrameIndex() getters so
those stay pure reads), recomputed from the same cyclePos formula each
FrameIndex() already uses so a live speed change mid-play still ends the
sequence correctly. "One sequence" = one full ping-pong period for
animation 1 (1->17->1), one forward pass for animation 2 (1->N, N = the
live FLICK2_FRAME_COUNT -- noted 21 in the code as of this entry, though
another session's own CHANGELOG entries earlier today describe actively
revising this frame set; deferred to whatever that constant reads at any
given time rather than hardcoding a count here). Verified via synthetic
frame-stepping: animation 1 auto-stopped after 39 frames at 3.2x speed
(playing/time/index all reset); animation 2 (image fails to load in this
local sandbox -- the same already-documented ERR_CONNECTION_RESET
limitation, not a real bug) exercised directly via its state variables,
stopped after 31 frames at 3.2x speed. Set both Anim Speed defaults to
3.2x in DEV_GROUPS, and also updated the live saved override for
animation 1 in data/processed/dev-panel-settings.json (was explicitly `1`
there, which would otherwise have kept overriding the new default on
load); animation 2 had no saved override so its new default applies
automatically.

Verified via node --check (syntax clean, 4 separate passes across this
session) and a live reload with a clean console (only the known FLICK-
image ERR_CONNECTION_RESET local-server noise, no JS exceptions).

================================================================================
2026-09-04  -  08:34 AM EDT

Double-click-in-circle fixed for real this time (was still gated on rope-
curve proximity); FLICK hold-to-preview + startup-click-load-race fix;
regular-cut delay investigated (not a bug).

User, urgently, after the earlier fixes: "and the double click in the
circe stiklll doest work" + "fix it please" -> clarified via
AskUserQuestion: "this is doble click in the circe but not on the rope"
+ "when you try it in browser, click in the top 1/3 of the circle" +
"and the backgroun rope is still going crazy when i cut the entire rope"
+ "fix it" + "same fix ocndition as the other one. 30 minutes".

Root cause: onPointerUp's circle-mode double-click resolution called
hitTestAnyIgnoringCircle(x, y, doubleClickDistance) and only detached if
THAT found a target (the rope or a piece) within Double Click Distance --
so a double-click landing in circle-space the rope doesn't currently pass
near did nothing at all. Confirmed exactly why "top 1/3" reproduces it:
the anchor now rests low in the circle (per Circle Offset), so the rope
only occupies the lower portion, leaving the rest of the circle's own
interior an unresponsive dead zone. This also explains the "background
rope still going crazy" report: if the double-click never found a target,
detachEntireRopeAndRestartIntro() never even ran, so the earlier bgRope-
inversion fix (08:08 AM entry) never got exercised in their actual
testing -- they were very likely still hitting the OLD pre-fix jump
behavior on the rare double-clicks that DID land near enough to the rope
to trigger a detach at all.

Fixed by removing the distance-based check entirely for circle-mode
double-clicks -- the circle itself IS the target now; any double-click
starting in isOnCircle()'s zone calls detachEntireRopeAndRestartIntro()
directly, unconditionally. hitTestAnyIgnoringCircle() became dead code as
a result and was removed (along with its own doc comment and a stale
reference in hitTestPieces()'s comment).

Verified via synthetic pointer events + frame-stepping (same methodology
as the 08:08 AM entry): double-clicked at (200, 110) -- solidly in the
circle's top third, well above both the anchor (~y183) and the rope
itself -- confirmed it now triggers the detach (fallenCount 0->1, phase
'done'->'waiting'). Then re-traced the FULL 'waiting'->'rising' sequence
end to end to confirm the earlier bgRope-inversion fix still holds with
zero regression from any of today's other changes: p0/p1/pLast remained
monotonically ordered and changed smoothly (273->268.6 / 301.7->301.5 /
329.3->329.2 across 6 frames right after entering 'rising'), no jump, no
inversion.

Also this round, mid-task: "and it fees like the animations take time to
load up. whe ni cick it on startup, they don really work" + "and make it
so that when i click and hold on the animations, the animation only
starts at release. but while its holding, cycle throug the frames here
data/FLICK/ANI/3".

Hold-to-preview: added holdingFlick (1, 2, or null) -- onPointerDown now
sets this instead of immediately starting playback; onPointerUp checks it
FIRST (before the existing !downInfo early return, since a flick hold
never sets downInfo) and starts the real click-triggered sequence there
on release instead; pointercancel clears it without triggering playback.
While held, flickHoldTime advances in update() and cycles through 4 new
frames loaded from data/FLICK/ANI/3/FRAMES-01..04.png (flickHoldFrames) --
a plain modulo cycle, no stop condition, since it's meant to loop for the
whole hold duration rather than play once. render() swaps in the hold
frame at the same rect whenever holdingFlick matches that animation.
Verified via synthetic pointerdown/pointerup: holding shows holdIdx
advancing (0,0,0,0,1,1 across 6 frames), release cleanly transitions to
the real sequence (flickPlaying/flick2Playing true, holdingFlick null)
for both animations.

Startup-click-load-race: flickRect/flick2Rect used to only get set INSIDE
the img.complete && naturalWidth > 0 guard that also gates the actual
drawImage() call -- so isPointInFlick/isPointInFlick2 stayed at their
zero-sized initial {0,0,0,0} until that specific frame image had actually
finished loading over the network, meaning a click in the first moments
after page load silently missed regardless of where the graphic visibly
was. Fixed by computing each rect EVERY frame straight from cfg values
(vw/vh/vmin), using a fallback 1:1 aspect ratio via that animation's own
frame-0 image until its real naturalWidth/Height is known --
complete/naturalWidth still guards the actual DRAW, just no longer the
hit-test rect. Verified live: both rects are already non-zero and
populated after just 1 stepped frame, before the images have had any real
time to load.

Separately: "and right now when i cut the rope, the cut piece stays in
place for a duration then falls. i think one of the wait time sliders is
makign it an issue" -> clarified via AskUserQuestion: confirmed this was
about a REGULAR double-click cut on the rope itself, not the circle
detach. Investigated rather than guessed: traced cutRopeAt() -> the
actual split (performMainRopeSplit(), what makes a piece start falling)
is deliberately deferred by cutThroughDuration() = CUT_SWEEP_BASE_SEC /
cfg.cutSpeed (0.35 / 1.6 ~= 0.22s at the live default) -- the rope stays
ONE still-pinned physics chain until the cut-line sweep visual has fully
crossed it, an already-documented deliberate design (see cutRopeAt()'s
own comment) that predates this session entirely. No "wait" slider
actually governs this -- Cut Speed does, inversely. Compounding the
perceived delay: a freshly-split piece starts each fall from true zero
velocity (topplePiece()'s own oldx=x/oldy=y), so gravity's first few
frames of ramp-up are barely visible on top of the real ~0.22s sweep
pause. Not changed -- reported back with the actual mechanism and numbers
rather than guessing at a fix.

Verified via node --check (syntax clean) and a live reload with a clean
console (only the known FLICK-image ERR_CONNECTION_RESET local-server
noise). One real debugging snag this round: the Browser pane tab had gone
background/hidden mid-session, which silently stalled document.readyState
at 'loading' and left debug hooks permanently undefined despite the
server correctly serving 200s -- fronting the tab (tabs_select)
immediately unstuck it. Noted here since it cost real time before being
diagnosed and isn't specific to this feature.

================================================================================
2026-09-04  -  09:21 AM EDT

Reverted the "hold falling piece until spawn" behavior for whole-rope
detach -- correction to the 08:08 AM entry's own fix.

User: "when i cut the whole rope, the cut segment is sitll frozen for he
wait duration. the wait is only for the endcap. the cut segment should
fall immediatly as it used to."

The 08:08 AM entry added `piece.waitingForSpawn` specifically because an
earlier request ("The ropes shouldnt fall until the Main rope has been
spawned") was read as: the OLD rope's falling piece should stay frozen
until the fresh main rope spawns. This follow-up corrects that reading --
the wait was only ever meant to apply to the NEW rope's own endcap
grow-in (Detach Endcap Start Scale/Grow Duration, already a separate,
untouched mechanism via mainRope.detachTipScaleProgress), not to the
falling piece's physics at all. Removed `piece.waitingForSpawn` entirely:
the flag's own set (detachEntireRopeAndRestartIntro), its clear (the
'rising'->'pausing' handoff in updateIntro()), and its check (the
fallenPieces gravity-integration loop in update()) -- a clean 3-site
revert, no other behavior touched. The just-detached piece now falls
immediately again, exactly like a regular cut.

Verified via node --check (syntax clean) and a live reload with a clean
console (only the known FLICK-image ERR_CONNECTION_RESET local-server
noise, no JS exceptions).

Separately, investigated (not changed): "when i click, it only lods the
first frame. then the 2nd click loads the 2nd, etc. i want the entire
seueunce to run on the first and subseuent clicks" -- about the FLICK
animations. Reproduced the exact scenario live (a single quick down+up
click, current live Anim Speed 8x for both animations): traced flickIdx
across 20 real stepped frames after one click and confirmed the FULL
ping-pong sequence plays correctly in one go (2,3,5,6,8,10,11,13,14,16,
then back down 14,13,11,10,8,6,5,3,2, then stops) -- no repeated clicking
needed, matching the intended design from the 08:34 AM entry. Could not
reproduce the reported per-click single-frame-advance pattern against the
current code with current live settings. Most likely explanation: a stale
cache or a Vercel deployment that hadn't yet picked up the 08:34 AM/09:08
AM commits at the moment it was tested -- reported back with the direct
verification evidence rather than guessing at a code change with nothing
in the current code to actually explain the symptom.

================================================================================
2026-09-04  -  11:23 AM EDT

bgRope spawn moved from a full diameter below the circle to just out of
sight (Rope Thickness + 1); fixed a second occurrence of the same chain-
inversion bug, this time at the handoff when mainRope spawns.

User: "the background rope should spawn just out of sight of the circle.
right now its spawning very far down. It sould be spawning the rope
thickness+1 under the circle" + "when the main rope spawns, it seems to
physically affect the background rope. it shouldnt."

Item 1: the 'waiting'->'rising' transition's start position was
circleAnchor().y + circleSize/2 + circleSize (a full diameter below the
edge) -- deliberately far, per the 08:08 AM entry's own reasoning (a
close start would make the climb imperceptible at Startup Rise Clear
Offset's default of 0). This explicit correction supersedes that
reasoning -- changed to circleAnchor().y + circleSize/2 +
vmin(ropeThickness + 1). Verified live: at circleSize 21.5/ropeThickness
6.1/anchorY 144 (viewport fallback 400x800), the spawn now lands at
exactly 215.4 = 144 + 43 + vmin(7.1), matching the formula precisely.

Item 2: investigated rather than guessed. Found the SAME inversion bug
class as the 08:08 AM entry's bgRope-jump fix, recurring at a different
transition. At the 'rising'->'pausing' handoff, resetBgRope() was called
with NO args (line previously read `resetBgRope();`), which hangs the
WHOLE chain from circleAnchor() -- then only point 0 got manually moved
to the real spawn position (2 lines below). Since the spawn position is
the anchor BOUNDARY (anchorBoundaryRadius() away from circleAnchor()),
not circleAnchor() itself, points 1+ were built relative to the wrong
origin every single time mainRope spawned -- the same "rest of the chain
built from a different anchor than point 0" flaw already fixed once for
the climb-start transition, just never caught here. Fixed by passing the
real spawn coordinates directly: `resetBgRope(mainRope.points[0].x,
mainRope.points[0].y)`, same fix shape as before. The following 2 lines
that set bgRope.points[0] explicitly were kept (not redundant) -- they
correct for makeChain()'s own small per-point random x lean, which
would otherwise leave point 0's x very slightly off mainRope's own.

Verified both together live via synthetic frame-stepping: confirmed the
new spawn y (215.4) matches the formula, then traced 6 frames right at
and after the handoff -- p0/p1/p2/pLast remained monotonically ordered
(180.9/208.5/236.1/345.6 at handoff) and changed only gently frame to
frame (sub-2px oscillation, real settling motion, not a jump), matching
mainRope's own settling almost exactly (mainP0y 180.88 vs bgRope p0y
180.9). No reaction, no inversion, at either transition.

Verified via node --check (syntax clean) and a live reload with a clean
console (only the known FLICK-image ERR_CONNECTION_RESET local-server
noise, no JS exceptions).

================================================================================
APPEND-ONLY. Never rewrite, reorder, or delete a prior entry, even to fix
formatting (CLAUDE.md §4a / Never-Skip rule #3). If an entry needs a
correction, add a new entry noting the correction rather than editing the
old one in place.
